# Decision Log (INDEX) — weewx-rtldavis

**Status:** Source of truth (index) · **Tier 1 session read** (DEC-0030)
**Last updated:** 2026-09-03 (S121 — DEC-0138; full bodies in DECISIONS-FULL.md)

One row per decision — scan this at session start; **do not re-litigate** anything listed. The
complete append-only bodies live in `docs/DECISIONS-FULL.md`; grep the DEC id there whenever a task
touches a settled decision's actual text (*"working near it" means read it*). New decision = append
the full body there + add a row here. A decision is never edited in place — it is **superseded** by
a later entry.

> **Provenance note:** entries DEC-0001…DEC-0009 are *reconstructed* from project history (the
> pre-governance chat sessions, roughly Apr–Jun 2026); their dates are approximate. DEC-0010 and
> up were made live under governance (S16 onward) and are exact. This log covers the
> **weewx-rtldavis driver/Docker** only; dashboard decisions belong to `eaglehunt-weather-dashboard`'s
> own log. **DEC-0018–0020 were never assigned** (numbering gap, no bodies exist).

## Index

| ID | Title | Status | Date |
|----|-------|--------|------|
| DEC-0001 | Passive 915 MHz interception via RTL-SDR | Accepted | ~2026-04 (S1) |
| DEC-0002 | Ubuntu 26.04 / Python 3.14 multistage Docker build | Accepted | ~2026-05 |
| DEC-0003 | Same-repo tags for versioning (Option 2) | Accepted | ~2026-05-26 |
| DEC-0004 | Volume-mount extensions for hot iteration; bake stable code | Accepted · **driver half superseded** by DEC-0031 | ~2026-06 |
| DEC-0005 | Custom `loop_json_writer.py`; weewx-loopdata rejected | Accepted | ~2026-06 |
| DEC-0006 | Null-on-rejection filter philosophy (**runtime only** — see DEC-0032) | Accepted · **clarified** by DEC-0032 | ~2026-06 |
| DEC-0007 | Upload services use the WeeWX RESTThread pattern | Accepted | ~2026-05 |
| DEC-0008 | `docker kill`, never `docker stop` | Accepted | ~2026-05 |
| DEC-0009 | Dedicated limited user for the NAS-side monitor | Accepted | ~2026-06 |
| DEC-0010 | Adopt Eagle Hunt nine-file governance model | Accepted | 2026-07-04 (S16/S17) |
| DEC-0011 | Branch model: `main` = production truth, `dev` = work | Accepted | 2026-07-04 (S16) |
| DEC-0012 | Public repo: structural secret hygiene | Accepted | 2026-07-04 (S16) |
| DEC-0013 | Session numbering continues the shared lineage at S16 | **Superseded** by DEC-0023 | 2026-07-04 (S16) |
| DEC-0014 | No-Rewrite Rule | Accepted | 2026-07-04 (S16) |
| DEC-0015 | Graft Python tooling from hyperlocal-forecast | Accepted | 2026-07-04 (S17) |
| DEC-0016 | Claude Opus 4.8 at high/xhigh as the Claude Code driver | Accepted | 2026-07-04 (S16) |
| DEC-0017 | Gain held at 372 pending an averaged re-test | Accepted (interim) · open, but **absorbed into DEC-0048's designed RX experiment** | 2026-07-04 (S16) |
| DEC-0021 | Rain-counter glitch filter (the false-rain fix) | Accepted · released v2.0.3 | 2026-07-04 (S18) |
| DEC-0022 | Sensor-QC hardening deferred to a dedicated pass | **Resolved** by DEC-0029 (S33) | 2026-07-04 (S18) |
| DEC-0023 | Independent per-repo session counter | Accepted · **supersedes** DEC-0013 | 2026-07-04 (S20) |
| DEC-0024 | RF-reception metric ~150%: freqError channel packets published as loop packets | **Fully resolved** — Layer A (S22/S31) + **Layer B shipped (S43)**: freqError merged onto the next real DATA packet instead of yielded standalone | 2026-07-04 (S21; upd. S22/S31/S43) |
| DEC-0025 | Known-bad data: preserve-and-flag, never delete | Accepted | 2026-07-05 (S29) |
| DEC-0026 | v2.0.3 confidence gate waived: cut on tests + live evidence | Accepted | 2026-07-05 (S29) |
| DEC-0027 | Lint scope: enforce `ruff check`, not `ruff format`; exclude vendored uploaders | Decided · local pre-commit **brought back in sync** (S43 — `ruff-format` had silently contradicted this since S31) | 2026-07-08 (S31; upd. S43) |
| DEC-0028 | Leaked credential in pushed public history: rotate immediately, don't rewrite | Decided | 2026-07-08 (S32) |
| DEC-0029 | Decode-layer sensor plausibility filter (temp/humidity/wind/UV/radiation) | Accepted · **resolves** DEC-0022 · cause **confirmed** by DEC-0033 | 2026-07-08 (S33) |
| DEC-0030 | Docs diet: tiered session read, DEC index+full split, CHANGELOG roll, STATUS prune | Accepted | 2026-07-09 (S35) |
| DEC-0031 | The driver is BAKED into the image, never bind-mounted (supersedes DEC-0004's driver half) | Accepted | 2026-07-12 (S36) |
| DEC-0032 | Retrospective correction: correct to the KNOWN value, flag it in-band (`rain_qc`) | Accepted · **clarifies** DEC-0006 | 2026-07-12 (S36) |
| DEC-0033 | Glitches are CRC-valid multi-bit corruption from spurious duplicate frames (upstream #15) | Accepted · **confirms** DEC-0029 · **confirmed locally** by DEC-0035 | 2026-07-12 (S36) |
| DEC-0034 | State the fork honestly: modification notices, `+ws` version, CHANGES-FROM-UPSTREAM | Accepted | 2026-07-12 (S37) |
| DEC-0035 | Duplicate-frame mechanism CONFIRMED here (~722/day); the test that said otherwise was broken | Accepted · **confirms** DEC-0033 · **permanent counter shipped (S43)** | 2026-07-12 (S37; upd. S43) |
| DEC-0036 | The 7h18m freeze: trigger known (bare `docker logs` wedged the daemon), **mechanism OPEN**; mitigations banked | Accepted · **mechanism open** | 2026-07-13 (S37) |
| DEC-0037 | A retrospective correction must propagate to every derived field | Accepted · **extends** DEC-0032 | 2026-07-13 (S37) |
| DEC-0038 | An image tag denotes exactly one tree: publish **v2.0.5**, don't rebuild "v2.0.4" | Accepted · **extends** DEC-0031/0034 · *prod deliberately one patch behind* | 2026-07-13 (S38) |
| DEC-0039 | Secret gate: every allow term **anchored or positioned**; a gate ships with its planted-payload test | Accepted · **extends** DEC-0012 · adopts dash DEC-0063/0100 | 2026-07-13 (S38) |
| DEC-0040 | The cross-repo gap is an **enforcement** gap, not a documentation gap — **no master repo** | Accepted · owner-confirmed (S38) | 2026-07-13 (S38) |
| DEC-0041 | **StdPrint removed** — the v2.0.5 console-handler fix was necessary but NOT sufficient | Accepted · **completes** DEC-0036 · corrects DEC-0038 | 2026-07-13 (S38) |
| DEC-0042 | The phantom **rainRate is ISS-side** (condensation trips the reed switch, never tips the bucket) — not RF, not the driver | Accepted · **bounds** DEC-0033/0035 · closes the rainRate thread | 2026-07-13 (S38) |
| DEC-0043 | Override the **root** logger, not just `weewx`/`user` — weewx's default root points at a syslog socket a container does not have | Accepted · **completes** DEC-0036/0041 | 2026-07-13 (S39) |
| DEC-0044 | The **nibble theory is not supported** by the archive, and the archive can never settle it — **instrument, don't filter** | Accepted · **bounds** DEC-0029 · **parks** the coupling filter | 2026-07-13 (S39) |
| DEC-0045 | **A comment is not an exemption** — the secret gate scans comments like code. A commented-out credential in a public repo is still leaked, and the gate's *own test* had asserted it was fine | Accepted · **amends** DEC-0039 · **extends** DEC-0012 | 2026-07-13 (S40) |
| DEC-0046 | **The baked config is shadowed by the prod bind-mount** — an image-only config fix reaches downstream users and *never* reaches prod. The exact mirror of DEC-0031 (where the *image* wins and the mount is the no-op) | Accepted · **mirrors** DEC-0031 · completes the delivery half of DEC-0043 | 2026-07-13 (S41) |
| DEC-0047 | **The secret gate guards commits, not reads — the transcript is an egress path.** A `sed -n '…,+44p'` overran its section and dumped credentials into the transcript; no control existed because nobody had modeled reading as a path. Guard + redacting reader + transcript scanner, all in `~/.claude/`. *(Rotation specifics: gitignored local-infra doc.)* | Accepted · **extends** DEC-0012 · completes DEC-0039/0045 (write path only) · applies DEC-0040 | 2026-07-13 (S41) |
| DEC-0048 | **Reception testing is a designed experiment, not a pile of image tags.** The ad-hoc `rw*-test` images are retired; a proper RX test (hypothesis, control arm, averaged window) is deferred and will settle DEC-0017's gain question in the same run | Accepted · **absorbs** DEC-0017's pending sweep · applies DEC-0038 | 2026-07-13 (S41) |
| DEC-0049 | **The ISS hardware is new and inspected — the phantom rainRate is not a broken part.** DEC-0042's "inspect the bucket + reed switch" action is CLOSED and came back clean, which excludes a defective part and sharpens DEC-0042: it is working hardware reacting to condensation. Anemometer replaced ~16–17 Jun 2026 | Accepted · **bounds** DEC-0042 | 2026-07-13 (S41) |
| DEC-0050 | **The station gets a master for its IDENTITY (and only that): `eaglehunt-ops`** — DEC-0040's revisit triggers fired (5 shared executables versioned nowhere; the gate fix re-derived 4×; dash DEC-0106 = the predicted casualty). Private coordination repo per the S38 §Etiquette litmus test: canonical `station-identity.env` + drift check (first run 8/9 within 19 m, the 9th a real HLF finding), NAS runtime contract, the `~/.claude/` guards versioned with tests, issues as the cross-repo inbox. NOT a master repo; deletion clause. Also: `soak_check.sh` identifiers scrubbed to fail-fast placeholders (they were live on public `dev`; CI is structurally identifier-blind — `.identifiers` is gitignored), and **pre-commit was configured but never installed in ANY of the three clones** — now actually installed | Accepted · **executes** DEC-0040's revisit clause · owner-approved (cross-repo round, dash S74) | 2026-07-14 (S42) |
| DEC-0051 | Cold-load Fix B ships (`current.json` alongside `loop-data.txt`); `windchill` added — closes issue #44 | Accepted | 2026-07-15 (S43) |
| DEC-0052 | Adopt eaglehunt-ops' shared closeout skeleton, adapted — docs-diet + DEC log kept as-is, model-tier restore check added as step 5 | Accepted · **adapts** OPS-DEC-0016 · closes weewx#56 | 2026-07-19 (S44) |
| DEC-0053 | **Provenance audit:** loop-JSON cache bounded per-field (was unbounded — a dead sensor looked live); InfluxDB station identity + SQLite correction state documented as deliberate gaps, not closed | Accepted · **applies** DEC-0006 to the real-time surface · closes weewx#45 | 2026-07-25 (S48) |
| DEC-0054 | **Frame-level co-rejection:** a SensorQC *bounds* failure (positive corruption proof) nulls every weather field of the same frame and skips the rain-counter without resync; a *delta* trip never triggers it. Zero free parameters — NOT the parked DEC-0044 coupling filter. Motivated by ERR-0004 (phantom 39 mph gust rode a frame whose own humidity decoded to 144.9%) | Accepted · **extends** DEC-0029 · ships v2.0.9 · closes #76 | 2026-07-27 (S52) |
| DEC-0055 | **The outside-temperature field is SIGNED** — decoded unsigned, a −5 °F reading became 404.6 °F, tripped the −40…65 °C bounds and (post-v2.0.9) co-rejected the whole frame, so ordinary winter would have saturated the corruption alarm. Ships true two's complement (`− 0x1000`), **not** meteostick's one's-complement `^ 0xFFF` (0.1 °F warm; maps `0xFFF` and `0x000` both to 0.0 °F) · adopts meteostick's missing `0xFF8` sentinel | Accepted · **fixes** ops#105 R1 · **bounded by** DEC-0054 · **released v2.0.10, live in prod (S55)** | 2026-07-28 (S54) |
| DEC-0056 | **`MAX_PLAUSIBLE_TIPS` 60 → 16 (ops#105 R2)** — evidence-bounded (70-day archive: worst real minute 7 tips, worst 3-min window exactly 16, gaps during rain never > 60 s, filter idle at 60; StdQC already capped the system at 30, so the newly exposed band is 17–30, never occupied). Ships as a package: cap + reframed rejection email as tripwire + WeatherLink recovery playbook + predefined revisit trigger (any rejection on a wet day). Confirm-on-reject held as the designed escalation | Accepted · **amends** DEC-0021 · rides `dev` until the next image cut | 2026-07-28 (S55) |
| DEC-0057 | **ROADMAP.md joins the closeout ritual** — a same-session update whenever a DEC ships/closes/reprioritizes a ROADMAP line (closeout step 5), plus a next-check-due tripwire inside ROADMAP.md itself, after it was found 20 sessions / 8 releases stale (S35 → S56) | Accepted · **extends** DEC-0052 | 2026-07-28 (S56) |
| DEC-0059 | *(**AMENDED S58**: its "no detectable diurnal cycle" is true at 6 h resolution, **false at 1 h** — a reproducible ~2-pt notch at hours 07 and 19 was averaged away. Campaign validity unaffected; the Latin square balances it. See BACKLOG §Durable RF findings.)* **The RX experiment gets an apparatus** (`ops/rx_experiment.sh` + tests). Key finding: **`-ex N` ≡ `receiveWindow 300+N`** (upstream sums them), so the window axis is mounted-config-cheap and **no arm needs an image rebuild** — the `rw*` images were redundant, not just misnamed. Measured baseline replaces the stale "~67–70%": **73.3%, sd 4.67**, zero autocorrelation, no diurnal cycle → 24h/arm resolves 1.1 pts, so DEC-0017's "1–2 weeks" was ~7× overkill. Design: 2×2 factorial (gain × ex) as a Latin square, 6h blocks, run twice — LNA in, then LNA physically removed (blocked, not bracketed — owner's call; drift measured from campaign A instead). Adopt only on ≥2 pts with no duplicate-frame regression. All 7 pre-governance sweep scripts deleted (two were silently broken) | Accepted · **executes** DEC-0048 · **amends** its "same run" clause · **absorbs** DEC-0017 · **supersedes** DEC-0008's `set_gain.sh` exemplar · **DEPLOYED, campaign A running (S57)** | 2026-07-28 (S56; upd. S57) |
| DEC-0060 | **`debug_rtld` alone doesn't enable driver debug logging** — the `[Logging][[[user]]]` logger must also be at DEBUG, or `dbg_rtld()`'s `log.debug()` calls are silently dropped regardless of verbosity. Explains Phase 0's first ~7h attempt producing nothing. Fix: a scoped `[[[user.rtldavis]]]` entry, not the broader `[[[user]]]` (keeps other `user.*` services at INFO) | Accepted · **extends** DEC-0043 · **explains** DEC-0059's Phase 0 first attempt | 2026-07-29 (S57) |
| DEC-0062 | **Logs are an egress path the read-guard does not cover — never log key material.** `pressure_service.py` logged `api_key[:8]` at INFO on every startup, into `weewx.log` + 30 rotations; DEC-0047's guard covers *configs*, not logs, so the most routine operation here (tail the log to check a restart) walks past it into a transcript — twice on 2026-07-29. Log the *answer* (`present`/`MISSING`), never a fragment; resolve flags into locals before the call so no credential appears in a log argument at all (**a checker with exceptions is a weaker checker** — the fix belonged in the code, not the test). AST-guarded with a positive control. Also re-proved DEC-0046's layer question: the file is **BAKED** (`Dockerfile:117`), NOT mounted as first assumed — an `scp` would have been a silent no-op. **Deploy deferred to the next image release: rebuilding mid-campaign would confound DEC-0059's arms** | Accepted · **extends** DEC-0047 to logs · **applies** DEC-0046 · **fixed in repo, not deployed** | 2026-07-29 (S57b) |
| DEC-0061 | **Campaign A died of two defects a hermetic suite could not see.** (1) `health_ok`'s 90s budget was too small BY CONSTRUCTION — a restart needs boot (~25s) + up to a full 60s archive interval + ~30s write lag ≈ 115s; measured, the abort fired **3 seconds** before the record landed. Arm B had won the same coin flip. Now 180s, with the *arithmetic* asserted, not the literal. (2) `send_mail` sourced the env file without exporting, so its `python3` child died on `KeyError` — every alert the script could ever send was inert, same shape as DEC-0060 a day earlier: **configured-looking, never exercised, therefore never disproved.** Schedule regenerated for a clean 07-30 start (the 07-29 run damaged 3 Latin-square cells); stale state reset so the first tick couldn't record baseline config as arm-B data; a comment promising a `schedule --generate` mode that never existed corrected | Accepted · **fixes** DEC-0059's apparatus · **extends** DEC-0045 to runtime budgets | 2026-07-29 (S57b) |
| DEC-0064 | **Campaign B: the no-LNA RX campaign** — pre-registered end to end so the swap night is execution, not derivation. Owner-gated swap sequence (nothing touches the container/bias tee until an in-chat GO with the owner at the dongle; antenna-disconnected window = the 20–40 s SMA swap); overnight pilot 08-07 00:35–04:20 (gain-only, 45 min/arm, HIGH→LOW 496…328, **arm-selection input only, never adoption evidence**; an abort = the cliff found, high arms already harvested); H hold (arm-A settings, own harvest tag) through Friday; square 08-08→08-16, gain **{372, 496}** (372 = cross-campaign anchor; 496 = R820T max, pilot-adjustable) × ex {0, 50}, `-fc/-ppm 0` unchanged from A to keep the LNA contrast clean; abort floor 55→**50%**. Archive forensics replaced the assumed baseline: two flat plateaus (Jun 67.45 sd 3.22 @207; Jul 74.83 sd 4.13 @372), **both LNA-in (owner-confirmed)** — S29's "pre-LNA baseline" label was wrong, no honest no-LNA telemetry exists anywhere, the pilot is the first real measurement; bonus: the plateau contrast retroactively answers DEC-0017 (207→372 = +7.4 pts, same hardware, uncontrolled). v2.0.12 ships `BIAS_TEE` env (default 1, public image unchanged) + DEC-0062's deferred redaction, landing *between* campaigns so B runs on one image. A stays unread/unadopted through the gap | Accepted (design) · **executes** DEC-0059's second campaign · **carries** DEC-0062's deploy · runbook `docs/CAMPAIGN-B-RUNBOOK.md` | 2026-08-01 (S61) |
| DEC-0063 | **Adopt the eaglehunt-ops session-context tiering standard** (`BOOT.md`/`CONSTANTS.md`/`MANIFEST.md`/`ARCHIVE/`), migration to execute next session. ops#130 offered it while arguing *against* it ("the case here is genuinely weak"); the premise failed measurement — the tree is at **~25.5K not ~21K**, the saving is **~19K not 6–8K**, and decisively Tier-1 grows **~1.1K tokens per session close** by ritual (DEC-0052 steps 2–3), so "leanest in the forum" describes a moment, not a trajectory. Both siblings already migrated. **Addendum, this repo only:** ops is PRIVATE and this repo is PUBLIC, so `CONSTANTS.md` is self-sufficient for anyone who can clone it and any `ops/CONSTANTS.md` pointer is an owner-only supplement, never a prerequisite — §3's trio rule would strand every external contributor | Accepted · **supersedes** DEC-0030's Tier-1 table · **executes** ops#130 · **MIGRATED S60** — always-load 91,806 B (~25.5K tok) → 25,819 B (~7.2K), `docs/STATUS.md` retired | 2026-08-01 (S59; exec. S60) |
| DEC-0058 | **ROADMAP.md trimmed to P0–P3 only** — P4 + "Longer horizon" (credential hygiene, multi-source adaptability, template harvest, ops#110 winter build) moved to a new "Long-term direction" section in BACKLOG.md, so the actively-sequenced plan isn't buried under uncalendared/aspirational items | Accepted · **extends** DEC-0057's same-session doc discipline | 2026-07-28 (S56) |
| DEC-0065 | **The watchdog escalates and hands off; it does not acquire a bigger hammer.** ERR-0005 measured the old loop failing in a specific way: **9 USB resets, 0 effective**, 17 emails in 80 minutes, and not one distinguishing the 9th attempt from the 1st — while reset #10 preceded a strictly worse failure mode by 46 s (dongle still enumerating for `rtl_biast`, `rtldavis` unable to claim it). **Detection was never the deficiency** and is unchanged: RECEPTION ALERT fired correctly 8 minutes in. Now: `watchdog_poll()` judges each reset by whether reception recovered (`RESET_VERIFY_S` 180 s, deliberately **shorter** than `RESET_CD` 300 s so no attempt is counted before its verdict); 3 ineffective resets → stop resetting, escalate **once**, loudly; `watchdog_not_running()` never resets on that mode (a USB reset can't fix a binary that dies on startup and plausibly caused it) and escalates immediately; only the first reset of an outage emails. Escalation lands ~18 min after the first reset — for ERR-0005, ~00:29 instead of reset #10 at 01:27. **Auto-recreate deliberately NOT built**, against the owner's initial ask and applying the owner's own "proven fix" bar: n=1, and we cannot explain why `kill;rm;run` worked at 01:48 when the apparatus's own `kill;start` at 00:08:33 did not — and `rm` is not reversible. `build_recreate_cmd()` derives the command from **live `docker inspect`** (never the stale NAS compose file), returns `None` rather than a half-right line, redacts credential-shaped env values (public repo, DEC-0062), and ships it in the escalation email. **Related thread checked and CLEARED same session:** the apparent campaign-A abort near-miss (arm D read 71% when declared dead) was not one — `health_ok()` waits for an *archive* record, and the last before the abort was 00:04:20 with the next at 01:24:24, 80 min later; the ~180 s budget ran against a genuine absence. RapidFire loop publications are not archive records. DEC-0061's arithmetic holds; **campaign B is not gated on it** | Accepted · **caused by** ERR-0005 · **applies** DEC-0062 · **upholds** DEC-0061 · deploys by `scp`, not the image | 2026-08-02 (S62) |

| DEC-0066 | **Campaign B is HELD until the instrument is trusted** — prepared to launch 08-03 (LNA already out, schedule shifted −4 days, v2.0.12 built and verified, apparatus green), then held after prod went deaf **three times on 08-02**: 105 min (ERR-0005, unexplained), 3 min at 13:47 (unexplained), 10 min at 19:45 (`database is locked` + three uploader threads refusing to shut down). **Held, not cancelled; DEC-0064's design is unchanged.** The load-bearing argument is *not* abort risk — an abort is a safe failure that restores baseline. It is that campaign B **measures reception**, and a receiver intermittently losing 50–100% of packets for unexplained reasons yields **noise shaped like a result**: arms differ, means compute, and the difference may be an artifact of when the deafness fell. A contaminated dataset is worse than an abort because it does not announce itself. Cost of holding is honest: schedule slips and the `SCHEDULE=` literals now sit in the **past** (an `install` against them would jump into the middle of the square — regenerate first). Against that, prod stays LNA-out so the accidental H-hold keeps accruing (n=1106, mean **72.0%** vs A's pooled 72.4%; still not a clean comparison, A pools the gain-207 arms). **Do not treat "the apparatus is ready" as the launch condition** — it has been since S61 | Accepted · **defers** DEC-0064's execution, design untouched · **caused by** ERR-0005 | 2026-08-02 (S62) |
| DEC-0067 | **The recurring dropouts are process freezes, not RF loss — and the driver's own watchdog proves it.** `get_stderr()` is bounded at 10 s, so a *running* main thread that hears no RF returns to `genLoopPackets()`'s `while` top and raises `rtldavis process stalled` at 150 s — as it did **21 times during ERR-0005**. Across the silent 208–218 s gaps it **never fired**, which means the main thread was not executing: these are **process freezes, and the receiver was fine**. Measured: genuine RF loss is confined **entirely to ERR-0005** (21 detections on 08-02, **0 on every other day**); freezes recur ~1/day at ~3.5 min, independent of it. **One occurred 07-30 with the LNA still in — so they are NOT new to the no-LNA regime** and that standing watch closes. Three consequences: (1) the monitor's metric counts *published output*, so a frozen process and a deaf receiver both read `WINDOW: 0/21` — every "unexplained dropout" was scored by an instrument that cannot make the distinction the watch was about; (2) packets are stamped at **parse** time, so a freeze *misdates* what it recovers — 03:01–03:03 have no records and the next record absorbs ~3.5 min, distorting exactly the counters campaign B measures, down then up; (3) DEC-0066's bar is now partly met — the recurring class is explained in kind, **bounded** (~0.4 % of wall-clock), and pre-dates the LNA, while ERR-0005 is demonstrably a **single incident**, not the tip of a pattern. **B stays held**, but the launch condition is now mechanical (detect and exclude freezes) rather than open-ended. Ruled out with evidence: NAS-wide stall (influxdb's timer fired mid-freeze, sub-ms on schedule), the S37 stdout wedge (live config has **no console handler**), CPU-quota throttling (DSM 4.4 exposes no `cfs_quota_us`), `pressure_service` (82 fetches, worst 8.99 s), the monitor's 6-hourly read, and the HH:04 cluster (campaign-A swaps). **Open: why it freezes** — all threads stop and nothing is logged, consistent with a thread blocking on the bind-mounted log volume while holding the logging lock (box is at **18.6 % cumulative iowait**); unproven, `D`-vs-`S` capture pending. Upstream saw it and worked around it undiagnosed (`get_stderr()`: *"a hangup will occur regularly, sometimes of more than a minute"*). **Lesson: a watchdog that does not fire is telling you something** | Accepted · **reclassifies** DEC-0066's evidence · **corrects** ERR-0005's framing of the 13:47 event · design of DEC-0064 untouched | 2026-08-03 (S63) |
| DEC-0068 | **Coffee-radar is a confirmed contributor to some process freezes, not a full explanation.** This NAS also runs coffee-radar and hyperlocal-forecast, and HLF has its own measured incident class here (its DEC-0162: coffee-radar's scheduled runs cause page-cache-eviction stalls). An overnight watcher (`ops/freeze_watch.sh`, now committed) caught two distinct freezes the same night: **freeze #1** (17:48:59–17:52:37 EDT, ~4 min) with coffee-radar not running and loadavg normal (0.3–0.7); **freeze #2** (19:13:43–19:15:41 EDT, ~2 min) with **loadavg spiking to 12.39** and coffee-radar **confirmed running the entire time** via `nasctl inspect` — its scheduled job never sets `--name`, so it runs Docker-auto-named and is invisible to any check that greps `docker ps` by name instead of image, a gotcha this session's own first check fell into. Its 19:00:16 EDT start matches coffee-radar's documented 19:00 daily run almost exactly once corrected to **local time (EDT), not UTC**. **Not the sole cause**: freeze #1 shows the identical symptom (all `S`, brief `R` ticks, `weewxd` never `D`) with neither signal present, so this is n=1 correlated out of 3 total captured freezes, not a base rate, and the 4 historical freeze timestamps still show no clean match against the corrected schedule. Mechanism unconfirmed — `weewxd`'s main thread stays `S`, never `D`, even during the load-12 freeze, so this isn't literal I/O-blocking. Campaign B's DEC-0066 gates are unchanged either way | Accepted · **extends** DEC-0067's open question · design of DEC-0064 untouched | 2026-08-05 (S65) |
| DEC-0069 | **The campaign metric moves to per-minute `rxCheckPercent`; freeze exclusion is structural, not magnitude-based.** DEC-0066's second gate, closed — and the estimate that made it a gate was ~60× too large. `harvest()` scrapes the monitor's **5-minute** `RECEPTION:` aggregate, so one frozen minute drags a whole bucket (**measured 16 % and 27 %** against a ~72 % neighbourhood), destroying four good minutes with the one bad one — `(72−16)/72 ≈ 0.78` pts on a 6 h block, exactly BOOT's ~0.8. The same measurement exists **per minute** in the archive DB as the driver's own `rxCheckPercent` (S31's honest metric), where the same freeze damages **one** record: ~0.18 pts on a block, **±0.03 measured** on a pooled arm against a **2.0** adoption bar. **The source change is ~4× of the fix; the exclusion rule is the remaining sliver.** Measured signature over 10 988 records / 33 gaps: three non-overlapping classes — *freeze* (`rx` before the gap 4–17, vs population p01 **56.5**; after, normal), *arm swap* and *lock/outage* (both: `rx` before normal, after **NULL**). **The contaminated record is the one ADJACENT to the gap, not the minutes inside it** — those are simply *absent rows*, which is what shrank the design (BOOT had assumed they scored as zeros). `interval` stays `1` on the bad record, so it cannot self-identify and detection must be structural. Rule: drop the record either side of any gap (symmetric — catches truncation *and* absorption without knowing which), plus NULL, plus non-physical `rx > 100`; ~3.5 % of records. **A magnitude threshold is rejected outright** — it would discard genuine deep fades and bias every arm upward, the exact confound the Latin square removes (positive-controlled in tests). **Corrects DEC-0067 in both directions:** its "up" is real — 2026-07-29 03:10 reads **200.0 %**, a record that absorbed 2 min while stamped `interval=1`, and `summarize_reception_rows()` applies no cap so it pushes a block **up** ~0.35 pts — but it is *conditional*, ~1 in 8 days against ~10 freezes, and an initial two-freeze reading that concluded "no up" was too small a sample. **Campaign A recomputed** (12 balanced blocks): A 74.81 / C 74.37 / D 74.17 / B 73.87, spread **0.94 pts**, no arm near adoption; the ~1.9-pt offset from BOOT's 72.4 matches `weewx_monitor.py`'s own documented "~1–2 pts optimistic" note, so the two metrics validate each other — **and A-vs-B must be read on the same one**. *Sealing note:* DEC-0066 sealed A's arm winner until after B; validating the tool necessarily unsealed it. Design uncompromised (DEC-0064/0059 pre-date this), but it was a side effect, not a decision. Two build-time defects, both of which would have printed confident wrong numbers: a bare run **pooled campaign A's aborted 07-29 attempt** with the campaign proper (arm B +60 records, unbalanced square, tidy table, no indication) — now detected mechanically, since a docstring would not execute (DEC-0040); and deriving the query bound locally dragged the **entire archive** over ssh (does not finish in 120 s) — now bounded NAS-side. New tool `ops/campaign_analyze.py` + 14 tests; **`ops/rx_experiment.sh` deliberately untouched** | Accepted · **closes** DEC-0066's metric gate · **corrects** DEC-0067's impact framing · **applies** DEC-0045's positive-control rule · design of DEC-0064 untouched | 2026-08-05 (S66) |
| DEC-0070 | **The `database is locked` defect is a 5-second default with a 10-minute penalty; WAL is the real fix and a cross-repo mount blocks it.** Not a weewx bug — `weedb/sqlite.py:136` reads `timeout = to_int(argv.get('timeout', 5))`, the live config set none, and the archive runs `journal_mode=delete` where a reader's SHARED lock blocks the writer. A reader holding it **six seconds** therefore yields `CRITICAL Database OperationalError`, weewx's **hardcoded 120 s wait**, and a restart = **5–10 min of outage**; the penalty is three orders of magnitude past the cause. (The CRITICAL logs *after* the teardown — weewx shuts services down in the exception path first, which is why the 08-02 trace reads "Unable to shut down OWM thread" → `rtldavis killed` → CRITICAL.) **Shipped: `timeout = 30`** in the live `weewx.conf` `[[SQLite]]` — 30 not 60 so it stays under the 60 s archive interval and records can't pile up behind a wait. Verified *in the running system* (DEC-0046), not the artifact: resolved `database_dict` carries `'timeout': '30'`; restart healthy with a new record in **106 s**, inside DEC-0061's ~115 s worst case. **Honest cost:** weewx now *blocks* ≤30 s instead of erroring — a good trade (a stall loses one record, a restart loses ten minutes) but such a stall is indistinguishable from a DEC-0067 freeze to `freeze_watch.sh` and gets excluded by `campaign_analyze.py`'s gap-adjacent rule. Both correct; neither a bug to chase. **WAL blocked by `hyperlocal-forecast-api`'s single-FILE mount** (`DST=/data/weewx/weewx.sdb RW=false`) — WAL's `-wal`/`-shm` siblings can never appear beside a single-file bind. **A first reading of this was wrong and the correction matters:** the assumption that the mount must also become *writable* (granting a read-only consumer write access to the archive dir — a real regression) was **tested and disproved** on the container's own SQLite 3.46.1 against a live writer: directory mount read-only works with `-shm` present *or absent*; only the single-file case fails, and it fails as **`no such table: archive`**, not as stale data. So `RW=false` stays and the fix is *only* file → directory, with HLF's in-container path unchanged and **no HLF code change**. Filed **ops#141** (`repo:hlf`) with a staged order — change the mount and verify HLF under the existing `delete` journal *first*, so a failure there is unambiguously the mount; then flip WAL (reversible via `PRAGMA journal_mode=DELETE`). **Drift risk recorded:** `weewx.conf` is the mounted layer and is never committed, so this setting lives **only on the NAS** with no repo artifact and no CI — a stock recreate would silently revert it; hence a `CONSTANTS.md` row. **Also corrects DEC-0067's reader list** — it named "the dashboard"; a scan of every container mounting a weewx path finds only `hyperlocal-forecast-api`, `eh-proxy` (parent dir, RO), and weewx itself. **Guard finding:** the NAS mutation that wrote live prod config **did not trip the Class C guard** — `ssh <nas> "python3 -" < script` hides the mutation in stdin while the guard matches the command *string*, and that is the shape CONVENTIONS actively recommends; meanwhile the *read* guard fired three times on `grep`/`tail` against files with no secrets. Owner-authorized in chat, so nothing improper — but the mechanism didn't enforce it. Belongs to ops (OPS-DEC-0060) | Accepted · **bounds** DEC-0066's last gate (capped, not closed) · **applies** DEC-0046 · **corrects** DEC-0067's reader list · real fix filed as ops#141 | 2026-08-05 (S66) |
| DEC-0071 | **WAL was tried and rolled back — the mount was never the only blocker, and DEC-0070's own test that said otherwise was structurally blind.** HLF shipped the directory mount (its S235); WAL went live 06:56 EDT and was **rolled back 07:24**. DEC-0070's four-scenario table concluded a **read-only** directory mount works with WAL, so `RW=false` could stay. Wrong: the test ran `os.chmod(tmp, S_IRUSR|S_IXUSR)` on the **directory only**, leaving the files inside read-write, so SQLite could still write `weewx.sdb-shm`. A Docker `:ro` bind makes the **files** read-only too; a WAL reader must write `-shm` to join the WAL, so HLF fell back to the main DB — which in WAL mode stops advancing except at auto-checkpoints — and **froze on a stale snapshot within minutes**. DEC-0035's lesson recurring: *a passing test proves nothing if it is structurally blind to what it tests.* **Second blocker no mount change can fix:** SQLite creates `weewx.sdb-wal` mode **0555** (vs 0777 on the DB), so even a read-write mount leaves a non-root reader unable to write it — also why a non-root SSH user got `attempt to write a readonly database` trying to checkpoint. **Rollback was the hard part:** `PRAGMA journal_mode=DELETE` needs an EXCLUSIVE lock, weewx holds a persistent connection (`database is locked`), and with the container stopped there is no `docker exec` either — resolved by making weewx do it via `[[[pragmas]]] journal_mode = DELETE`, applied by weedb on connect as root at startup. **Pragma left in place deliberately** — re-pins `delete` every start so an accidental flip can never again silently strand a reader. **Self-inflicted outage:** that pragma was first written as the *scalar* `pragmas = journal_mode = DELETE`; weedb iterates it as a **mapping**, so configobj needs a subsection — the scalar yields `TypeError: string indices must be integers` and weewxd **crash-looped ~6 min** (CRITICALs 07:18:58, 07:20:22). Two process failures behind it, both this session's: the first rollback opened with a `SELECT COUNT(*)` that **had already timed out once the same session**, and the config shape was assumed from the field name rather than checked against the consumer whose code had been read an hour earlier. **Consequences:** WAL not viable as scoped (ops#141); the DB lock stays **bounded not cured** by DEC-0070's `timeout = 30`, which was most of WAL's practical benefit at none of this risk; HLF did **not** self-recover and needs a container restart (left to an HLF session); campaign B unaffected. **Lesson: two of three failures repeated lessons already on file here — having them written down is not the same as applying them on an incident path** | Accepted · **bounds** DEC-0070's WAL recommendation · **corrects** its published test · **re-proves** DEC-0035 | 2026-08-06 (S66) |
| DEC-0072 | **The MANIFEST indexes classes, not instances — and that makes every script's header a load-bearing artifact.** One row per artifact cannot hold a fixed cap: the row count tracks a population that only grows, so the file crosses any cap *by construction*, not by neglect (measured S67: **1948 tok against a 1000 cap, 197%** — worst of the four repos in the ops sweep). Fix is neither a bigger cap nor fewer artifacts: where a set shares a load-time convention, **one row names the set and the convention** and the instances self-describe at their source. `ops/*` + `scripts/*` went from five per-artifact rows to one — *the script's header comment is its manual*. **Verified before being relied on** (rule 9 is only sound if the convention actually holds): the existing headers already state why each script exists and which lying symptom it catches — **richer than the rows that duplicated them**, i.e. STANDARD rule 5 applied to the index itself. **Coverage went UP**: the old index carried 5 of 11 harness scripts; six had no row at all. **The obligation this creates, and the reason it is a DEC rather than a chore:** a class row is a promise about every *future* member of the set — a new `ops/`/`scripts/` file must ship with a header answering "why does this exist, when would I load it?", because a bare shebang silently falsifies the index and **nothing will fail to say so**. Four facts that had drifted into the index were moved back to their scripts (campaign A's `--since`; campaign B being the loaded schedule + `install` refusing a stale one; `EXPECT_IMAGE` tracking the deploy). **Not done:** `docs/` rows stay per-artifact — their "when to load" genuinely can't be inferred from name or location, which is rule 9's own reserved exception. **Result:** BOOT 3734 → 2161 tok, MANIFEST 1948 → 970, verified by running `checks/tier-sweep.sh` itself against fixtures — the tool that files the issue is the tool that confirms the fix. Landed at **3% headroom deliberately**, not at 100% of cap: the dashboard's S193 diet hit exactly the cap and was over again within a session | Accepted · **amends** DEC-0063's tier-file structure · **applies** ops STANDARD rule 9 (OPS-DEC-0098) · closes ops#145 | 2026-08-06 (S67) |

| DEC-0073 | **Supervise the USB watchdog, make its absence LOUD, and model its resets before campaign B.** `ops/usb_watchdog.sh` was deployed 2026-05-22, hand-started once, never supervised (no crontab entry, no pidfile, no boot task) — dead by the 07-08 reboot at the latest, absent for three qualifying stalls on 08-06, while `BOOT.md` recorded it "deployed and live" for ~2.5 months. **Two defects, and the second is the one that mattered:** it isn't supervised, *and* its absence is invisible — a watchdog that isn't running writes exactly the same log as one with nothing to do, so no observation distinguishes them and it could only ever be found by accident, which is how it was. **Decision: (a)** adopt `weewx_monitor.py:102-115`'s proven PID guard (read pidfile → exit 0 if `/proc/<pid>` lives → else claim, release via `atexit`) plus a 5-minute scheduled re-launch; the guard makes re-launch idempotent so the scheduler carries no state. **(b)** heartbeat file touched each tick, so liveness is an mtime check not an inference (*not* a periodic log line — unbounded growth, buries real events). **(c) the structural half — `ops/soak_check.sh` asserts it**, because that script exists to ask "healthy, or does it just look Up?" and did not ask it of the watchdog; without this we fix one instance and leave the class. **(d)** rising reset rate reaches the alert path — a watchdog that papers over a degrading dongle is worse than none. **Campaign-B interaction, the real call:** `campaign_analyze.py`'s three-class gap taxonomy (freeze / arm swap / lock-outage) was validated over 07-29→08-05 — **a window in which the watchdog was dead** — so a USB-reset gap is a FOURTH class it has never seen, and by shape would be absorbed into `freeze` and silently excluded. Possibly the right treatment, but right *by accident*, which is DEC-0035/DEC-0071's exact failure. Enabling it also alters a pre-registered apparatus (DEC-0048). **Owner call: watchdog ON, analyzer taught the fourth class**, reading the watchdog log and excluding reset-adjacent minutes explicitly. Rejected: unmodelled resets (B's headline number could be shaped by an intervention nobody accounted for) and watchdog off (an unattended multi-night run with no USB recovery is what it exists for). **Verified first:** dongle still on `1-3` (`0bda:2838`), `syno_vbus_reset` present, script byte-identical to repo, monitor's guard read not remembered. **Does NOT explain why the stalls happen** — mitigation, not diagnosis. **Lesson: a sha match proves the FILE, never the PROCESS** | Accepted (design; implementation follows) · **gates** campaign B · **extends** DEC-0069's taxonomy · **re-proves** DEC-0035 | 2026-08-06 (S67) |

| DEC-0074 | **Retire `ops/usb_watchdog.sh` — the monitor IS the watchdog, and DEC-0073 solved a problem that did not exist.** DEC-0073 said the watchdog was dead *and* that three stalls on 08-06 went unhandled. **The second half was false.** `weewx_monitor.py` (pid 5015, alive) carries `reset_dongle()` (l.342) and `watchdog_stall()` with escalation (l.354), wired at l.692, and handled all three within seconds — `09:53:40 / 10:11:13 / 10:32:52 STALL detected → RESET → done, idVendor=0bda`. The script is a **superseded standalone predecessor**; the monitor does strictly more (verifies whether a reset *worked*, escalates after 3 ineffective ones, distinguishes 'stalled' from 'not running', records that a reset may have caused ERR-0005). **Deploying it would have added a second uncoordinated resetter to the same dongle**, unshared cooldown, beside a monitor whose source records nine resets in 75 minutes on 08-02 — a regression, not a fix. **How the error happened, since the shape recurs:** the "not running" evidence was sound and stands; what was never checked was whether anything ELSE did the job. Three sources were consulted — the watchdog log, `weewx.log`, the process table — all silent about the monitor; `weewx_monitor.log` was never read and holds the answer in plain text. **DEC-0031's lesson turned on its author: "this component is dead" and "this capability is missing" are different claims needing different evidence.** **Survives from DEC-0073:** *a sha match proves the FILE, never the PROCESS* — now generalized to absence-of-function too; and the `soak_check.sh` criterion, **repointed at the monitor** (live pid + log <300 s; a live pid with a stale log means *wedged*, not dead). **The real defect DEC-0073 walked past:** all three resets FAILED — `RESET ineffective (1/3)` each time, bad windows climbing 8→10→15. The monitor works and is reporting that **the remedy does not**. New `USB RESETS INEFFECTIVE` criterion; open and unexplained. **Bigger consequence for campaign B:** reset gaps are not a *new* class B would introduce — the monitor fired **nine on 08-02, inside the 07-29→08-05 window `campaign_analyze.py`'s taxonomy was validated against**, so reset-adjacent gaps are already inside campaign A's recomputed numbers. That makes the fourth-class question one about a result already published in DEC-0069, not a pre-launch nicety. **No NAS change made or needed; blocker 4's deploy gate is void**. ⚠️ **AMENDED S68b (#147): the lesson holds, the INSTRUMENT does not.** This DEC's replacement probe — process start via `nasctl ls /proc/<newpid>` vs file mtime — is unsound: proc-dir mtime is **access** time, measured reporting 17 s for a 2.88-day-old process. Use a **startup log line** after the file mtime (what actually carried S67's and S68's verifications), `/proc/<pid>/stat` field 22 vs `/proc/uptime`, and new-pid-with-old-pid-gone. *A DEC written because logs named an operation that never ran adopted a probe that would report a restart that never happened* | Accepted · **supersedes DEC-0073** · **corrects** the S67 blocker-4 finding · **re-proves** DEC-0031 · **probe amended S68b (#147)** | 2026-08-07 (S67) |

| DEC-0075 | **Photograph the reset instead of reasoning about it — capture from inside the sudo grant, host-side through `/proc`.** The resets fire and do not work (3/3 on 08-06, 9/9 on 08-02) and the hypothesis is explicitly unestablished: the reset may treat the **device** while the fault is the **consumer's grip** — unbind/rebind is not a power cycle, so the dongle stays enumerated and nothing makes the stalled `rtldavis` drop its libusb handle. `ops/usb_forensics.sh` is built to see either predicted signature — **stale container view** (host gains a new `devnum`, container still shows the old node) or **surviving grip** (an fd on the pre-reset node afterwards) — and, equally, to return the negative: both clean means the stall is **not a USB fault at all** and the reset treats the wrong thing entirely. **Built before the evidence exists, necessarily:** the capture needs a live stall (~1/day, unpredictable), and there has been **none since the corrected reset code went live 08-07 19:28** — zero `RESET`/`stalled` lines across the 08-07 and 08-08 monitor logs, both greps positive-controlled against 1440/521-hit `WINDOW` counts. Nothing to read retroactively. **Three design calls. (1) Host-side `/proc`, not `docker exec`** — this fires *during a stall* and a wedged container can block an exec indefinitely, so the capture would hang on the very event it records; `/proc/<pid>/root` and `/proc/<pid>/fd` cannot block, one pid found by `comm` yields everything, and `rtldavis` being **absent** is itself a finding. **(2) Pre/post fire from inside `usb_reset.sh`** — both decisive reads are root-only, `weewx_monitor.py` runs as the unprivileged `weewx-monitor` user (which is *why* the reset goes through sudo), and the sudoers grant is scoped to `usb_reset.sh` alone, so capturing there needs **no new grant**; the monitor fires only the `+RESET_VERIFY_S` capture, host-side and **self-labelled DEGRADED**, because an unreadable fd section must never be mistaken for a released handle — DEC-0074's absence-is-not-evidence trap. **(3) Capture-only** — DEC-0065's refusal to automate the container recreate is untouched; mixing a remedy change into the measurement meant to justify it would confound both. **An escalation was introduced and closed:** executing a helper from `usb_reset.sh` runs it as root under the NOPASSWD grant, and mode 777 is common on this NAS, so a helper writable by `weewx-monitor` would convert that narrow grant into arbitrary root execution. The script now verifies the helper is root-owned and root-only-writable, **refuses loudly** otherwise while still resetting, and `do_reset()` logs its output on a zero exit so the refusal cannot become a silent no-op — checked, not documented (DEC-0040), and positive-controlled by neutering the check and watching the helper run. **Evidence is never load-bearing over the watchdog it observes:** missing, broken or refused, the reset survives intact. **DEPLOYED + verified 2026-08-09** from merged tip `ad7e5a4` (root-owned as required, monitor 3870 → 8810, process evidence not a sha). The post-deploy smoke test then caught a defect in the shipped script — `/proc/<pid>` mtime is **access** time, so `started=` claimed a 2.88-day-old `rtldavis` was 17 s old; fixed in PR #146, and **DEC-0074's own documented probe has the same flaw (#147)** | Accepted · **advances** DEC-0074's blocker 4 · **corrects DEC-0074's verification probe (#147)** · **applies** DEC-0040 · **leaves DEC-0065 untouched** | 2026-08-08 (S68) |

| DEC-0076 | **The secret gate missed `GMAIL_PASS`-shaped keys — the fifth hole, and the first found by routine control rather than by audit.** The key list held `password` and `passcode` but nothing for the `_PASS` abbreviation, so `GMAIL_PASS = "..."` was undetected **in every spelling** (spaced, unspaced, colon-separated) — and `GMAIL_PASS` is the exact variable `weewx_monitor.py` uses for its Gmail credential, so the hole sat squarely under this repo's own secret. **Nothing was ever leaked through it:** no `_PASS` literal in the tracked tree (the sole match is README's documented `NOPASSWD:` sudoers line) and no `GMAIL_PASS = "<literal>"` on any ref in the full history. A future hole, closed. **How it surfaced is the part worth keeping** — not an audit, but DEC-0045's routine positive control before an *unrelated* commit: the planted payload carried an `api_key` line and a `GMAIL_PASS` line, the gate reported only the first, and the asymmetry was free. The rule that costs one command per commit found the fifth instance of this gate being wrong. **Two detectors, each proven necessary by removing it and watching its payloads leak: (a) bare `pass`** in the key list — deliberately *not* `passwd`, which matches README's `NOPASSWD: /volume1/...` and would report the binary path as a credential; bare `pass` cannot, because the detector requires `[:=]` immediately after the key and `NOPASSWD` has `WD` there (same reason Python's `pass` statement and `passed = True` do not trip it); **(b) a literal matcher for the four-group app-password form**, which slips past the assignment detector *even with* `pass` in the key list, because that detector needs **8+ consecutive** value characters and the four-group form breaks every 4 — the shape Google displays and people paste. **The false-positive half nearly shipped and is half the fix:** `PASS` is listed separately from `pass` because detection is case-insensitive and the allow-list deliberately is **not** (that asymmetry is what distinguishes a constant reference from a literal, bug class 1); without the uppercase spelling, widening the key list made the gate report `weewx_monitor.py`'s own `os.environ` lookup and README's placeholder as credentials, and the tracked-tree check failed. The harness caught it pre-ship — a gate that cries wolf on its own repo's source gets switched off. Harness **41 → 51** cases, tracked tree still clean | Accepted · **extends** DEC-0039/DEC-0045 · closes a future hole, not a live one | 2026-08-08 (S68) |

| DEC-0077 | **Reset gaps do NOT contaminate campaign A's figures — the exclusion is class-independent, and the outage left no rows to mis-average.** Blocker 5, closed on measurement. Every rotated monitor log spanning campaign A (`.11`=07-29 … `.4`=08-05) grepped: **11 resets, all on 08-02**, 00:11:23 → 01:27:20, seven of eight days empty — independently corroborating DEC-0067's "0 detections on every other day". The archive across the incident reads **00:04 = 72.73% (normal) → 80 rows ABSENT → 01:24 NULL → 25 rows ABSENT → 01:51 NULL → 01:52 back in range**, which is exactly the tool's documented **lock/outage** signature (*rx before normal, rx after NULL*). **The load-bearing point: classification is descriptive, exclusion is structural.** DEC-0069 drops the record either side of *any* gap plus every NULL plus `rx > 100`, never consulting the class — so 00:04, 01:24 and 01:51 were already excluded and the 105 absent minutes contribute nothing (absent rows are not zeros; that is what shrank the DEC-0069 design in the first place). **DEC-0074's framing was that reset gaps got "sorted into freeze/swap/lock"; they did — into lock/outage — and it changes nothing.** The real exposure was never mislabeling but a reset outage producing **present-but-low** rows, which nothing would exclude because the tool refuses magnitude thresholds by design (a threshold discards genuine deep fades and biases every arm upward). **No such rows exist** — the transition is normal → absent → NULL → normal with nothing low-but-present at either edge. **What IS wrong, narrowly:** the taxonomy's completeness claim holds for *shapes* but not *causes* — a USB reset is a fourth cause producing the lock/outage shape. Treatment keys on shape, so **no analyzer change is warranted**; recorded so campaign B's reading does not re-open it. **Two bounded residuals, neither gating:** 01:52 (57.14%) survives the rule because it neighbours a NULL *row* not a gap, worth ≈**0.04 pts** on a 6 h block against a 0.94-pt observed spread and a 2.0-pt bar; and 105 min vanished from one arm's block (~2.4% of 12 × 6 h), costing **precision, not bias** — during a receiver outage the arm's RF performance is unmeasurable and a USB failure is not a property of the arm, so exclusion is correct treatment rather than concession. **Correction to the record:** the log shows **11** resets, not nine — ERR-0005 and DEC-0065 both say "nine in 75 minutes" and call 01:27:17 "reset #10"; it is the **11th**, span 76 min. Nothing downstream depended on it (DEC-0065's argument is about unbounded retry, which 11 strengthens), but it appears in two decision entries | Accepted · **closes blocker 5** · **bounds** DEC-0074's concern · **amends** DEC-0069's completeness claim (shapes yes, causes no) · **corrects** ERR-0005 / DEC-0065's count | 2026-08-09 (S68d) |
| DEC-0078 | **Image builds move to the NAS** — the arm64 laptop's linux/amd64 cross-build fails deterministically (tar ENOSYS under emulation), and the failure hid behind a pipeline exit 0. Build natively on the NAS from a `git archive` of the merged tip, success = the explicit `BUILD-EXIT` marker (v2.0.3 precedent); the deploy consumes the NAS-local image; Hub publish decoupled — `save` → laptop → `push` after prod proof, so **Hub lags prod until pushed** (CONSTANTS documents the window). CI builds = the structural fix, backlogged | Accepted · **amends** release mechanics (RUNBOOK §Release, CONSTANTS §Release) | 2026-08-10 (S70) |
| DEC-0079 | **Opt into the ops-wide `.claude/transient-state` convention (ops#113)** — a tracked, one-line-per-entry file (`<revert-by-epoch> <tracking-ref> <description>`) for anything intentionally put into a non-default, reversible mode with a planned revert; SessionStart surfaces overdue entries, and deleting the line is the whole close mechanism. Force-added past the local `.git/info/exclude`, same precedent as `settings.json`. Left empty at creation — no current state meets the motivating shape (a short, easily-forgotten flip) that isn't already carried prominently in BOOT.md/CONSTANTS.md; first real entry waits for the next genuine short-lived flip | Accepted · **adopts** ops#113 (commit e5094a4, OPS-DEC-0067) | 2026-08-10 (S71) |
| DEC-0080 | **Solar diode-floor correction: exact-code `StdCalibrate` zero at the config layer — not a driver patch, not an almanac service.** At true zero irradiance the VP2+ solar sensor decodes to exactly `sr_raw=1 × 1.757936 ≈ 1.758 W/m²` (only 0 / 1.758 / 3.516 representable near zero), inflating every dark hour into the archive, InfluxDB and all nine uploads (~76 kJ/m²/day); the June 2026 fix was dashboard-only and regressed at the chart layer on refactor — the second per-path filter loss. Fix at the source (owner, S71): `radiation = radiation if radiation is None else (0 if 1.75 < radiation < 1.77 else radiation)` — exact-window, None-guarded, range form (only `math.*` confirmed in the eval namespace). Lives in the live NAS `weewx.conf` AND `weewx.conf.example` (the versioned public artifact — the anti-regression mechanism the June fix lacked) AND a DEC-0070 deviations row at apply time. Option B (almanac elevation-gated service) rejected: it also needs a `process_services` live-config edit so it escapes no fragility, it bakes one station's calibration into the public image, and it buys correctness below the sensor's own resolution (~minutes/day, one quantization step) for a service + tests + rebuild — design preserved in the S71 handoff, could ride the #144 rebuild if ever wanted. **APPLIED 2026-08-11 (S73), post-GATE 2 as planned — to the live conf AND `weewx.conf.rx-baseline`**: the apply step surfaced that `restore_baseline` copies the snapshot over the live conf at every campaign abort/end, so a live-only apply would be silently wiped — both-files is the durable form (CONSTANTS deviations row carries it). Runtime-verified same morning: weewx 5.4.0 boots clean with the line and publishes. Dark-hours-read-0 verification due S74; if 3.516 appears, extend per-code, never a loose threshold. Historical step-change at cutover accepted, no retro rewrite; the dashboard's surviving `eh-ui.js` filter goes vestigial — retirement is dashboard-repo work, note filed at apply | Accepted · **resolves** the S71 radiation-floor handoff · **relates to** DEC-0029 · **applies** DEC-0070 at apply time | 2026-08-10 (S72) |
| DEC-0081 | **The stall class is RF-dead episodes, not USB faults: resets demoted to one hedge, the driver self-classifies every event, and an episode ledger feeds the LNA verdict (ws.5 / v2.0.13).** The S73 forensics differential (three capture sets from DEC-0075's apparatus + the full night timeline + cross-repo load correlation, three read-only subagents + main-thread synthesis) established: the rtldavis child goes **silent** while the device stays enumerated (devnum unchanged across every reset since Aug 2 — DEC-0075's stale-devnum hypothesis was a measurement-design error, driver unbind/rebind never re-enumerates); the driver's own 150 s watchdog **fires and respawns correctly** (kill → ~60 s weewx retry → fresh child, three times overnight 08-10/11); during an episode the respawned children stay silent across multiple gain configs and recovery correlates with **time, not with any reset or restart**. Across ~17 captured reset attempts (08-02 ×11, 08-06 ×3, 08-10/11 ×3) none demonstrably fixed anything; ERR-0005 suspects one caused the worse dies-on-startup mode, and its recreate-fixed-it now reads as coincidence with episode end — **DEC-0065's refusal to automate the recreate is vindicated**. Changes: `RESET_MAX_TRIES` 3→1 (one hedge for the never-yet-observed genuine dongle wedge, then escalate); driver ws.5 adds **child reaping** (ProcManager killed by pidof+SIGKILL and never wait()ed — the engine's per-retry fresh ProcManager stranded every corpse; three zombies under one weewxd captured), a **STALL DIAGNOSIS** line at every 150 s raise (raw-stderr count 0 = child mute → process/USB class; >0 = emitting → RF class) + stderr tail, and a paced **DATA DROUGHT** line for the RF-quiet mode that never trips the watchdog (hop packets reset it); monitor writes an **episode ledger** (`logs/episodes.log`, one row per ALERT→RECOVERY: onset/duration/stalls/resets/respawns/droughts/worst-avg/cmd) — the pre-registered datum for whether episode susceptibility differs LNA-in vs LNA-out (owner's public write-up for ~50–70 m/trees/walls sites). `soak_check.sh` ineffective-reset criterion reframed FAIL→WARN (an ineffective hedge during an RF episode is expected; alarming = a mute-class diagnosis). Guard aborts during episodes are **the system working**; auto-restart rungs rejected — restarts show the same evidence pattern as resets. Root cause of the episodes themselves (interference vs front-end vs site) deliberately **left open** for post-campaign characterization with A×B data. Reaping + diagnosis upstreamable (CHANGES-FROM-UPSTREAM rows 12–13). Deployed same day: v2.0.13 built on the NAS from `1530971`, swapped mid-H-hold, ws.5 banner verified live; monitor deployed, respawn pending the owner's path-scoped-sudo kill. ⚠️ **AMENDED S76 (DEC-0083): the LNA dates are wrong** — "08-02 and 08-06 were LNA-in" should read **08-06 was LNA-OUT** and 08-02 only *straddles* the removal (the LNA came out mid-ERR-0005, early 08-02). The clause's point survives on 08-02 alone; don't carry the 08-06 claim into the LNA characterization | Accepted · **supersedes** DEC-0074's open question and DEC-0075's hypothesis set · **vindicates** DEC-0065 · **amends** DEC-0073's framing · **relates to** DEC-0067/0068 (freezes remain a separate phenomenon), ERR-0005 · **LNA dates amended S76 (DEC-0083)** | 2026-08-11 (S73) |
| DEC-0082 | **Campaign B square recovery: shift the whole schedule +24h rather than accept a lost block.** A third same-day RF-dead-episode abort (18:05, 08-11) left the STOP sentinel blocking every tick overnight, straight through the scheduled 00:05 A-arm swap — `due_arm()` would have silently joined the square wherever the fixed grid says "now" on any clear, with no error. `test_schedule_is_a_balanced_latin_square` rules out a same-day off-grid restart (every arm must land on exactly 00:05/06:05/12:05/18:05 twice each); the earliest valid slot for arm A is one day out. Shifted all 33 square rows +24h verbatim, preserving the exact arm sequence and full 8/8 balance — 17/17 tests pass unmodified, none loosened to fit. Deployed to the NAS and STOP cleared same session, after the `scp` hit three independent guard/classifier layers (one looks like a bug: `secret-read-guard.sh` re-blocked with its own documented `command`-prefix escape hatch already applied) | Accepted · **relates to** DEC-0064 (campaign B design), DEC-0081 (RF-dead episodes) | 2026-08-12 (S75) |

| DEC-0083 | **The stall rate is measured, not eyeballed — the alarm was right and its evidence was not.** S75's "trending hot" (`episodes.log` 2→4 rows in ~18 h) **survives measurement**: over 30.5 d / 31 rotations, the 48 h and 72 h windows ending now hold **6 episodes each — the record maximum** (98.3rd/98.2nd pct); 24 h is 96.1st pct but off its peak of 5, so the burst may be easing. **The unit had to be fixed first:** a stall *line* is not an event — the 150 s watchdog re-raises every ~3 m 40 s for an episode's whole duration, so 08-02 is **21 lines and one event**; clustering gives 15 episodes, stable at 30/45/60 min, and **reproduces DEC-0081's independently-derived boundaries** for the 08-10/11 night exactly. `rtldavis process is not running` excluded as a separate class (20 lines, all 08-02, none on any burst day). **Three corrections to S75's reasoning: (1)** onset is **08-10 23:56**, not ws.5 — the v2.0.13 container started 18:05 local 08-11, so **5 of 6 burst episodes predate it**; the ledger's field of view was mistaken for the phenomenon's onset. **(2)** not a simple LNA effect — LNA-in 0.40/day → LNA-out 08-02→08-10 **0.13/day, the quietest stretch in the record** → 08-10→now **2.43/day**. **(3)** the "2→4 rows" evidence compared **two instruments**: row 3 is drought-only and `DATA DROUGHT` appears **zero times in every pre-ws.5 log**, so RF-quiet episodes were structurally invisible before ws.5. **New sanctioned readout `ops/stall_baseline.py`** — states its left-censored window and threshold sensitivity on every run; building it exposed a bias in the first cut (anchoring "current" on the last stall guarantees the window contains it), fixed to anchor on now. **The transferable lesson: a red signal resting on wrong evidence that happened to be TRUE** — the ops#147 catalog's third variant and the most dangerous, since being right retires the question that would expose the method. **Secondary sweep (job 3):** the **freeze rate is measured at 1.49/day, median 240 s** (45 freezes / 30.3 d) against BOOT/DEC-0067's inherited *"~once/day, ~3.5 min"* — right order of magnitude, **understates both by ~40%**, refines rather than overturns; **a confounder worth 60% had to be removed first** — the S37 backfill wrote `interval=15` rows that read as 28 phantom 900 s "freezes", caught only because individual events were printed instead of just the rate. **Co-rejection (DEC-0054) still 0, re-verified through 08-12 and positive-controlled** (same pipeline returns 2308 for a present token) against a stale "through 08-01". Phantom-rainRate already has a live instrument in `soak_check.sh`; humidity-spike correctly left pinned to DEC-0044 | Accepted · **amends** DEC-0081 (LNA dates, ws.5 attribution) · **executes** ops#160 · **answers** ops#159's weewx bullet | 2026-08-12 (S76) |

| DEC-0084 | **Secret gate hole class 6 — the app-password detector required quotes.** `_assign` needs **8+ consecutive** value chars and a Google app password breaks that run every 4; `_apppw` (added S68 for exactly this shape) required **quotes**. So a `GMAIL_PASS` assigned the bare four-group form was missed in every spelling — spaced `=`, env-style, and ConfigObj-style indented — all verified MISSED before the fix (the literal is described, never written: a decision log earns no exemption, DEC-0045 — and the new detector went red on this row's first draft, proving itself live). **Unquoted is the NATIVE form of the two files this repo must never commit**: `weewx.conf` is ConfigObj (bare values are the norm) and `monitor.env` is an env file, so the gate would have missed this project's own credential in the format its own config writes it. Gitignored (`.gitignore:5`), so nothing leaked — a future hole, as DEC-0076 was. **Why it survived S68:** that fix planted the QUOTED literal (holes 25/26), went green, and never asked the neighbouring spelling — **the fix certified its own blind spot**, DEC-0045 one level down and the sixth time this gate has been wrong. **Fix:** new `_apppw_assign` with quotes optional but **anchored on `_key` + `[:=]`** — the anchor is load-bearing, since four consecutive lowercase 4-letter words occur in ordinary English and an unanchored shape match would flag prose repo-wide (ops#147 item 6). **One allow-list widening refused:** the rule flagged `monitor.env.example`'s own `xxxx xxxx xxxx xxxx` placeholder; since **five of the six historical holes were allow-list defects**, the placeholder moved to `YOUR_GMAIL_APP_PASSWORD` (the convention CONSTANTS already mandates) rather than trading a good detector for a new excuse path. Harness: holes 27–29 added, **54 passed / 0 failed**, tree clean | Accepted · **extends** DEC-0012 · **continues** DEC-0039/0045/0076 | 2026-08-12 (S76) |

| DEC-0085 | **The freeze-rate one-off becomes a tool — `ops/freeze_baseline.py`.** DEC-0083 measured the freeze rate (1.49/day, median 240 s) as scratch work, and both BOOT.md and BACKLOG.md flagged the same risk in the same words: the number decays unless someone re-derives it. The new tool classifies archive gaps >150s by BACKLOG's rule (RF-dead checked first: stall line within ±5min; else a scheduled campaign swap slot, :05 past 00/06/12/18 per `rx_experiment.sh`'s own `SCHEDULE`; else freeze), **reusing** `stall_baseline.fetch()`/`.stamps()` for stall data and `campaign_analyze`'s DB constants rather than re-deriving either — the abandoned prototype this replaces (found in S76's own scratchpad, never shipped) had instead loaded stalls from a static file that would go stale. `window_start()` extracted out of `stall_baseline.py` (+2 tests) so both tools share the same left-censoring boundary. Both of DEC-0083's traps carry forward: `interval != 1` rows dropped (S37 backfill trap), individual events always printed. **New:** a rolling-window placement section for the freeze side, which DEC-0083 never produced (only the stall side got that treatment) — first read: unremarkable across 24h/36h/48h/72h (36.6–78.3rd pct), moving independently of the same-day record-max stall reading. **Validated**: live run reproduces DEC-0083 almost exactly — 21 RF-dead/12 arm-swap/45 freeze exact match, median 240s exact, rate 1.48 vs the recorded 1.49/day (the small drift is ~14h more window, not a method difference) | Accepted · **completes** DEC-0083's flagged follow-up · **executes** BOOT S77 job 3 | 2026-08-12 (S77) |
| DEC-0086 | **`barometer_inHg` is an unflagged, already-corrected WeatherLink passthrough — documented for the first time.** Owner question: does barometer carry a correction we apply and flag, or is it raw? Neither. The VP2+ ISS never transmits pressure over 915 MHz at all — `pressure_service.py`'s `DavisPressureFetcher` polls WeatherLink's own cloud API hourly and prefers `bar_sea_level`, **already sea-level-corrected by WeatherLink's own cloud side**; this repo applies no correction of its own, it relays the value as-is. **No `_qc` flag marks this** (DEC-0032's mechanism covers only `rain`/`rainRate` today), so the field is indistinguishable on the wire from every RF-derived field beside it, even though its correction happened entirely upstream and invisibly. Secondary finding: the same fetched value backfills the internal `pressure` and `altimeter` loop-packet keys when null (`pressure_service.py:92-97`) — collapsing three normally-distinct quantities into one borrowed number; harmless externally since only `barometer_inHg` is in the published contract, but a trap for a future reader of the driver internals. Documented in `docs/INTERFACES.md` §1; cross-posted as a heads-up (not a change request) to `eaglehunt-weather-dashboard#377` and `eaglehunt-ops#162`. Whether barometer ever warrants its own `_qc`-style flag is left open, deliberately undecided here | Accepted · **documents** `docs/INTERFACES.md` §1 · **relates to** DEC-0032, DEC-0053 | 2026-08-12 (S77) |
| DEC-0087 | **RF-dead reception dips PAUSE the campaign instead of hard-aborting.** Twice now (DEC-0082's S75 incident, and an S79 arm-A abort fully reconstructed from the logs: clean swap `00:05:02`, a genuine 11-min RF-dead episode `01:40`–`01:51` with the monitor's own `RECEPTION RECOVERY` line at `01:51:33`, the lagging 30-min mean tripping `trip_abort()` four minutes later anyway) an ordinary, self-resolving RF-dead episode has produced a sticky STOP that then sat unattended for hours, spanning a scheduled swap slot. **Scoped to RF-dead only** (not freezes — "RF re-established" isn't a meaningful resume condition for a process-wedge event) **and to the guard's reception-floor path only** (`tick`'s own abort-on-write-failure / abort-on-unhealthy-swap are genuine operational failures, untouched). A floor trip now writes a non-sticky `rx_experiment.PAUSE` marker instead of hard-aborting — no config/container touched, since there's nothing to receive during a blackout regardless of which arm's gain is active. Every guard tick while paused checks for the monitor's own `RECEPTION RECOVERY` line newer than the pause start (→ auto-resume, no human needed) or a **120-minute ceiling** with no recovery (→ escalate to the unchanged `trip_abort()`) — the ceiling clears the 75.8-min ERR-0005 outlier, the longest RF-dead episode on record, with ~60% margin. **Schedule slots stay fixed** (a paused arm just gets fewer live minutes that block, captured automatically by existing `harvest()` accounting) rather than extending block end times, which would re-drift the whole square off its diurnal-control grid — the exact confound DEC-0082's fixed-slot design exists to prevent. No email on an ordinary pause/resume (DEC-0081 already called these "gates nothing"); the escalation path keeps its email. 9 new tests (pause-not-abort, healthy no-op, resume-on-fresh-recovery, non-resume-on-stale-recovery, ceiling escalation with baseline verified restored, STOP-precedence, `recovered_since()` in isolation), 26/26 `test_rx_experiment.py`, 233/233 full suite. Shipped as PR #173, independent of PR #171's schedule-shift fix for the same incident (disjoint regions of the same file) | Accepted · **relates to** DEC-0081, DEC-0082, DEC-0083, DEC-0085, DEC-0014 | 2026-08-13 (S79) |
| DEC-0088 | **`freeze_baseline.py`'s swap detection was schedule-only; ad hoc restarts were inflating the freeze rate.** S80's scheduled corroboration re-run (BOOT S80 job 3) found the 48h window S79 flagged had cooled, but 24h/36h had newly gone "elevated" — until the freshest event (2026-08-13 10:24-10:27) turned out to line up almost exactly with this session's own tick log (`10:25:01 tick: swapping A -> H`), the S79 abort's self-heal restart, not a freeze. `classify()`'s swap check (DEC-0085) only knew the four *scheduled* hours; it had no way to see a restart `rx_experiment.sh` triggers off-schedule — every abort's baseline restore, every DEC-0087 pause escalation, every tick self-heal. **Verified, not just inferred**: `logs/rx_experiment.log` confirms the 2026-08-12 "19:55→20:02 (420s)" event BACKLOG.md had counted as an independent freeze was actually `19:55:35 ABORT` → `19:55:36 RESTORING baseline snapshot`'s own restart footprint. Fix: `classify()` now also cross-references every logged `tick: swapping`/`RESTORING baseline snapshot` line (ground truth) alongside the fixed schedule, padded 3min back / 12min forward (`SWAP_SLACK_MIN`, reused); RF-dead precedence unchanged and re-tested against the new path. **Effect**: 7 of 47 "freezes" reclassified as swap — rate 1.54/day → 1.31/day, all four rolling windows (24h/36h/48h/72h) flip from elevated/85th-95th-pct to unremarkable (49th-67th pct). Not a one-off: DEC-0087 guarantees more ad hoc restarts going forward, so the bug's main damage was still ahead of it. 5 new tests (ad hoc detection, pad boundaries, RF-dead precedence, a positive control encoding the exact 10:24 event), 17/17 `test_freeze_baseline.py`, 238/238 full suite | Accepted · **corrects** a blind spot in DEC-0085's tool · **same shape as** DEC-0083 · **relates to** DEC-0087, DEC-0082, DEC-0045 | 2026-08-13 (S80) |

| DEC-0089 | **`recovered_since()`'s RECOVERY-edge check had the same lagging-signal blind spot DEC-0087 was built to fix.** DEC-0087's first live exercise (2026-08-13, 19:40:05 PAUSE on arm H, three short reception dips): reception then read healthy `[OK]` continuously for ~2 hours, but `recovered_since()` only checks for a `RECEPTION RECOVERY` line — an ALERT→RECOVERY *edge* — and reception never dropped low enough again to produce a fresh one. Zero RECOVERY lines confirmed in the whole window despite dozens of healthy reads. The pause rode the full 120-min ceiling into a needless `ABORT` at 21:45:01, and the resulting STOP blocked every tick for 10.5+ hours, straight through arm-A's `00:05` swap — discovered next session start. **Fix**: `recovered_since()` also checks the monitor's periodic `RECEPTION: NN% ... [OK]` line (a level signal, logged every ~5min regardless of ALERT state) as an independent, additive path — the edge check still resolves a sharp recovery faster; the level check catches a gradual one that never re-alerts. 4 new tests, including the exact incident fixture with its assertion flipped (the regression test). 30/30 `test_rx_experiment.py`, 242/242 full suite. **Recovery**: schedule shifted +24h a third time (DEC-0082's unchanged mechanism) — arm A now due `2026-08-15T00:05`, square `08-15 → 08-23T00:05`; fix + shift deployed together, STOP cleared only after | Accepted · **corrects** a blind spot in DEC-0087 · **applies** DEC-0082's recovery mechanism · **same lesson as** DEC-0088, DEC-0045 | 2026-08-14 (S81) |

| DEC-0090 | **The S82 state-machine audit: the DEC-0088/0089 blind-spot pattern was a class with more members — five apparatus fixes shipped** (resume aligned to the pause floor, closing the occupied [50,60) trap band; rotated-log reads in `recovered_since()` + the guard's floor mean; swaps defer during an active pause instead of swapping into the episode's health-check abort, BASELINE exempt; guard stands down after the self-terminator; tick/guard/abort serialize behind a lock — the 08-11 02:05:03 interleave was live evidence, and a full-budget health_ok outlives the cron period) **+ soak_check's reset counter dead since S67's rename** ("1 of 0" was the tell). Monitor-side trio (memory-only episode state, rotation-falsified reset verdicts, email-less reset-exception path) specced to #180, tier:mid. Every finding log-verified before fixing; two clean checks and one deliberate reliance recorded. Deployed same session (PR #179, sha-verified), before the square's first block | Accepted · **extends** DEC-0087/0088/0089 · **applies** DEC-0069/0077 · **continues** DEC-0074's rename lesson | 2026-08-14 (S82) |

| DEC-0091 | **The pre-square window used (owner's reframe: block 1 was still hours out) — the audit's remaining fixes shipped and deployed same day, plus two deliberate contract changes queued for v2.0.14.** Monitor trio (PR #182) live pre-square: episode state mirrored to disk and restored at startup (row-first-then-clear; `wu_in_alert` re-derived from onset), rotation voids pending reset verdicts instead of faking "effective", reset-exception path emails. Pressure package (PR #183) on `dev`: **`pressure`/`altimeter` become honest nulls — archive columns NULL from v2.0.14** (they carried sea-level values mislabeled as station pressure; hlf#302, DEC-0006) — and **`barometer_fetch_epoch`** (last *successful* fetch, published outside the TTL machinery: a staleness signal must never be omitted for being old). Image deliberately still held to post-campaign (~08-23): one-image-per-square holds either way, and a pre-midnight recreate risks the CONSTANTS deviations for a field that can wait. v2.0.14 queue: #158 + #172 + #144 + `:latest`. Mechanical fact: a second same-session PR branched pre-merge sits BLOCKED by branch protection until `gh api .../update-branch`. Offset quantification = the one open sliver (method in #144) | Accepted · **executes** DEC-0090's spec · **applies** DEC-0006 · **instruments** DEC-0086 · **upholds** DEC-0064 | 2026-08-14 (S82b) |
| DEC-0092 | **Answering the shared-NAS lease proposal (ops#169): our yield is a near-no-op, the box has a nightly heavy window, and both sessions were reasoning from the wrong filesystem.** Measured before replying: `binding` defaults to `archive`, so InfluxDB gets **1 record/60 s** and total weewx write bandwidth is order **tens of MB/day** — our shape is metadata-heavy (~50–85k renames/day), not bandwidth-heavy, so downshifting frees almost nothing; the counterpart accepted a near-no-op courtesy side as an honest answer. **Data-integrity line drawn where ops#169 asked:** InfluxDB deferral is safe (live config verified — `stale=None`, `max_backlog=1e6`, so a 30-min defer queues ~30 against 1M and loses none), but the **SQLite archive write is the red line** (engine waits a hardcoded 120 s then restarts; `timeout=30` exists because a *reader* holding the lock 6 s once cost 5–10 min of prod), and loop-JSON is contractually fixed (INTERFACES §1). **Correction that needed verifying rather than inheriting: `/volume1` and all 25 mounts under it are btrfs; only DSM's `/` is ext4** — there is no `jbd2` in either tenant's data path. The conclusion survives (`btrfs-transaction` is equally outside ioprio) but write amplification is higher than the ext4 model predicts and the mount is `relatime`. **Attribution confirmed impossible, with the reason**: `blkio/` holds only `reset_stats`, and cgroup v2's `io.*` postdates this 4.4.302+ kernel. **The finding that outranks the protocol:** a sibling tenant's nightly maintenance runs **00:10→~03:00–05:10 every night** (6 nights verified, median ~4h20m), so **~72% of every 00:05 campaign block** sits under it, plus two more jobs at 00:05 including our own logrotate. **Comparability is safe** — the 4×4 Latin square run twice absorbs a slot-level confound by construction; the exposure is midnight *swap reliability* and *variance*. **Hands DEC-0067/0068 a testable lead**: split freeze timestamps by hour-of-day against that window, using rotated logs we already hold (deferred post-square — the script is itself a heavy sweep). Coordination worked before any constant was locked and the mechanism was **schedule disclosure, not throttling**: the counterpart held its 12–20 h sweep past 08-22 and moved its 6-hourly job off :00, both verified here by process evidence (DEC-0074), not relayed. **Deliberately not acted on:** SQLite-on-CoW favors WAL, but the ~300% figure is single-writer and ours is the multi-process shape that bit us — **DEC-0071 stays closed**; `chattr +C` queued instead | Accepted · **extends** DEC-0068 · **informs** DEC-0067 · **applies** DEC-0074 · **does NOT reopen** DEC-0071 · **upholds** DEC-0059/0064 · answers ops#169 | 2026-08-14 (S83) |
| DEC-0093 | **The dataless-loop-JSON proposal was answered three months ago; the real write amplification is `current.json`, which nothing reads.** Asked (out of ops#169) whether the writer should skip dataless packets: **it already does, one level up** — DEC-0024 Layer B (S43) stashes freq-hop packets and `continue`s (`rtldavis.py:1507-1517`), so `new_loop()` cannot fire on one; `PacketFactory.create()` still *yields* them, which is what the reading saw, but `genLoopPackets` filters them first. The `~40%` figure is `66/166` = DEC-0024's **pre-fix** 1.66×. **Verified live, not from source** (DEC-0074): monitor reads `WINDOW: 12–18/21 (57–86%)`, `RECEPTION: 72–74%` — the post-Layer-B signature; the inflation's signature is this metric pinning near 100%. **Measured ~22,500 loop packets/day → ~45,000 renames/day**, refining DEC-0092's `50–85k` (whose upper bound was the pre-Layer-B rate). **The real finding: `current.json` has no consumer anywhere** — the eh-proxy's only `/weewx-data` read is `loop-data.txt` (`server.js:889`), `serveStatic` never touches that path, and no runtime reference exists in the dashboard, hyperlocal-forecast, or this repo outside the writer + its tests; the dashboard's roadmap still carries Fix B's consumer half **open at P0**. So **half of all writes go to a file nothing reads** — the whole 40% the proposal chased, but real. **Direction (gated, not shipped): decouple `current.json`'s cadence** to 30–60 s (a cold-load snapshot, not a live feed) = **~47% of renames removed**; not deletion, and **gated on the dashboard confirming** per INTERFACES change discipline — the window is open *because* Fix B's consumer was never built, and closes the moment it is. **Content-based suppression rejected and recorded so it is not re-proposed:** the eh-proxy 503s at `now - dateTime > 30` (`server.js:896`) and the dashboard reads that 503 as its one proof the station is down (dash DEC-0154), while `wind_speed` is set unconditionally including `0.0` when calm (`rtldavis.py:1579`) — so a calm night would report a **healthy station as offline**. The "suppression is more honest" argument **inverts**: DEC-0006/0053 keep per-field freshness (TTL/omission) and feed liveness (`dateTime`) as **two independent axes**, and republishing under a new `dateTime` is honest *because* the TTL bounds it; collapsing them restores the exact ambiguity DEC-0006 removed. S82b's `barometer_fetch_epoch` states the principle — a staleness signal must not be omitted for being old, so a liveness signal must not be withheld for being unchanged. Residual dataless case (DEC-0054 frame co-rejection) is one where suppressing would be **actively wrong**: the packet's arrival proves RF is alive. **Link declined:** DEC-0068 measured the main thread `S`, never `D`, during a load-12 freeze — less writer I/O is **not** evidence toward DEC-0067/0068. **Doc contradiction corrected:** INTERFACES §1 + the writer docstring claimed since S43 that the dashboard fetches `current.json` at boot; it never did. INTERFACES §1 now also records the **30 s liveness gate** — DEC-0092 called loop-JSON "contractually fixed" without the number that makes it so. Cross-repo reconciliation still owed. **No code changed** (PRINCIPLES §8, DEC-0014); the only `.py` edit is a docstring | Accepted (finding + direction) · **answers and retires** DEC-0092's last queue item · **refines** DEC-0092 · **does NOT reopen** DEC-0024 · **upholds** DEC-0006/0053 · **relates to** DEC-0051, DEC-0054 · **declines a link to** DEC-0067/0068 | 2026-08-15 (S84) |
| DEC-0094 | **The nightly-window freeze lead is refuted; the evening cluster is real; the test cost prod nothing.** DEC-0092 deferred the hour-of-day split post-square because `freeze_baseline.py` "is itself a heavy sweep" — true of a **fresh run**, false of the **split**: the script prints every individual event by design (an S37 anti-confounder) and those listings survive in session transcripts, so the analysis is arithmetic over already-collected data — **no ssh, no archive query, zero load on the running square.** Generalisable: *before deferring an analysis for its collection cost, check whether collection already happened* (S39's transcript lesson applies to data, not just reasoning). Used the **DEC-0088-corrected run only** (n=40, 1.31/day, 07-14→08-13); the three other runs in the transcripts (1.48/1.54/1.57) pre-date that fix and would have been miscounted data. **Two integrity checks passed first:** parsed count = claimed count (40=40, so the block isn't truncated), and a **positive control** — DEC-0088's documented "19:55 on 08-12 was a restart, not a freeze" is **absent** from the n=40 set and **present** in the 1.54/1.57 sets, proving which run was used. Timestamps are naive-local (`fromtimestamp`), so they align with the local window. **Result: the nightly maintenance window (00:10–04:30, 18.1% of the day) holds 9 of 40 freezes against 7.2 expected, P(X≥9)=0.29 — it explains nothing**, and median duration inside (240 s) equals outside (240 s). Blocker 1 must stop advertising it as a pending lead. **The signal is the evening instead: 18:00–21:00 holds 12 vs 5.0 expected, P=0.0027; coffee-radar's ~19:00 run window (18:30–20:00) holds 7 vs 2.5, P=0.011; across 10 distinct dates**, so not one bad night. This is **not a new hypothesis** — DEC-0068 named that job as a confirmed contributor from process evidence but could only say "n=1, not a base rate"; **this is the base rate: 30% of freezes in 12.5% of the day.** **Stated honestly:** the cluster was found post hoc and the **omnibus X²=30.8, df=23 (crit 35.2) does NOT reject uniformity** — it counts as corroboration of a pre-existing mechanistic hypothesis, not proof, and the window pre-dates the campaign, so a fresh run post-square should confirm. **Side result closing S84b's open question:** the 08-15 02:00–02:22 blackout was **RF-dead, not a freeze** — `weewx.log` carries three `rtldavis process stalled` lines inside it (02:05:00/02:16:16/02:20:19), and DEC-0067's rule makes a >150 s gap *with* a stall line RF-dead. **Mechanism still unproven** (DEC-0068: main thread `S`, never `D`) — this narrows *when* to look, not *what for* | Accepted (measurement) · **answers and closes** DEC-0092's lead · **upgrades** DEC-0068 to a base rate · **feeds** DEC-0067 · **applies** DEC-0088 · **does NOT close** blocker 1 | 2026-08-15 (S84d) |
| DEC-0095 | **Retention: the archive is accepted and monitored, not pruned — and the monitor executes.** ops#175 asked whether weewx carries the unbounded-growth shape HLF's DEC-0156/0174 fixed. It does, and it does not matter yet: **measured 2026-08-17, neither constraint binds by 2–3 orders of magnitude** — archive **33.61 MB = 0.89% of MemTotal 3.69 GiB**, 5.1 TB free disk, **125,613 rows / 90.2 d = 1,392 rows/day at 275 B = 0.37 MB/day, ~7.3 yr to 1 GB**; InfluxDB engine 14 MB; `dbstat` puts 32.94 of 33.61 MB in the single `archive` table, so there is no second offender. **HLF's METHOD transfers, its CONCLUSION does not:** DEC-0174 justified retention on the working set at ~8.0 M hot rows against *this same 3.69 GiB box* — we have 66× fewer rows. Three further grounds: **the `archive` table is the deliverable, not a diagnostic** (HLF's records are regenerable; a passively intercepted station cannot backfill its history, and this repo's contract IS the data it emits); **upstream already solved the working set by aggregation** (114 `archive_day_*` tables, 10,624 rows, ~0.1 MB, so long reads never scan the archive); and **the one documented cost here is CoW fragmentation, for which retention is the wrong lever** — `chattr +C` is queued (DEC-0092) and confirmed unapplied (`lsattr` shows no `C`). **Rejected:** HLF-shape archive-then-prune (right machinery, wrong problem, and it prunes the product), a fixed-MB tripwire (names a number, not the constraint), `VACUUM` (freelist is 0 and it is the fragmentation-maximizing op on CoW), deferring again. **Accept-and-monitor is worthless as prose (DEC-0040), so the reversal condition ships as code:** `soak_check.sh` reports `archive within retention budget — N MB = X% of RAM`, and crossing **10% of MemTotal** (~386 MB, ~2.6 yr out) **reopens this DEC**; an unmeasurable tripwire says so rather than reading green; all three states test-pinned with positive controls. **The InfluxDB half is NOT ours** (DEC-0010) — it is the dashboard's history; weewx proposes no horizon and will not prune a shared bucket, requiring only that `docs/INTERFACES.md`'s contract is unaffected; that half of ops#175 stays open against the dashboard. **Stated unmeasured:** `filefrag` is unavailable, so the extent count is unknown and this DEC claims nothing about actual fragmentation | Accepted · **answers ops#175 (weewx half)** · **adopts the method of, departs from the conclusion of, HLF DEC-0156/0174** · **defers to DEC-0092** on fragmentation · **applies DEC-0040** · **respects DEC-0010** | 2026-08-17 (S87) |
| DEC-0096 | **The campaign harness gains an explicit stand-down state: an empty SCHEDULE is the shipped between-campaigns form.** The staleness guard (S73-corrected form of DEC-0066's trap) requires the shipped terminator to be in the future — right while a campaign is planned, but `tests` is a **required check on both branches**, so from the moment a terminator passes EVERY pull request is red until the block is regenerated, and between campaigns there is nothing honest to regenerate it TO (a placeholder future table is a live landmine `install` would run). Found at S88 staging the v2.0.14 window: campaign B's terminator (08-23T00:05) IS the window's opening moment — the cut's own PRs would have landed red-blocked. Shipped: `schedule_has_rows()` + a loud `install` refusal on the empty block ("no campaign scheduled"); six structural tests skip on emptiness via `_require_campaign()`; staleness classification extracted to `_schedule_state()` (stand-down/live/stale) with the **stale branch positively controlled** (DEC-0045) so age can never slip through the emptiness gate — a stale non-empty schedule fails exactly as before; 5 new tests incl. end-to-end install-refusal on the exact emptied form a post-campaign commit ships, 299/299. **The dev schedule is untouched** — campaign B runs to completion; the post-square emptying PR is a trivial deletion and must be the FIRST PR of the v2.0.14 window | Accepted · **extends** DEC-0066's guard family · **applies** DEC-0045 · **unblocks** the v2.0.14 window | 2026-08-18 (S88) |
| DEC-0097 | **The "reception-floor dip" is not a reception measurement; it is RF-dead episodes, and they cluster post-midnight.** BOOT carried a four-session watch on a nightly reception dip with a "drifting later" window, flagged as the one timing lead nobody had tested. Three of its premises fail. **(1) The record was wrong:** PAUSE/RESUME lines pair one-to-one, so 08-18 is onset **02:55, five cycles**, not "03:30, two" — corrected onsets 02:15/02:15/03:25/**02:55** are not monotonic, and the drift was the stated reason for rejecting a fixed-clock artifact. The four nights are arms **A, B, C, D** — every gain × receive-window cell, so not an arm effect. **(2) Reception does not dip:** tested on per-minute `rxCheckPercent` (DEC-0069/S31, not the monitor's 30-min mean — different instruments, the 50% floor does not transfer) over **31 pre-campaign nights the hypothesis was not derived from**, contrasting the window against its flanks *within one arm block* so gain/window/arm are constant: mean d = **−0.01 pts**, Wilcoxon p=0.60, permutation p=0.47. Deepest 30-min mean on campaign nights **68.4%** vs DEC-0059's 73.3% baseline; **0 of 35 nights** under 50% while the monitor reported 20–45%. **The mechanism is truncated records:** every episode reads one pathological value (3–22%), then minutes **absent**, then NULL, then normal 65–90% — `campaign_analyze.py`'s documented artifact, a record assembled from a partial period but divided by the full interval, which cannot self-identify (`interval` stays 1). Those artifacts feed the laggy 30-min mean and trip the floor. The null held-out result *is* the mechanism's confirmation: `partition()` already excludes them. **(3) Night 1 was already classified — DEC-0094 recorded 08-15 02:00–02:22 as RF-dead, not a freeze**, three sessions before the watch was flagged untested. **What survives, on the right unit** (ledger rows re-clustered per DEC-0083's unit lesson): RF-dead episodes concentrate in **00:00–04:00 — 8/19 vs 3.17 expected, P=0.0079**, stable at 30/45/60-min clustering; on stall-bearing rows only, **7/9 vs 1.50, P=0.00009**, and **0/9** in DEC-0094's evening freeze window. Different clocks ⇒ independent support for freezes and RF-dead episodes being separate phenomena. **7 of 7 ledger dates**, including 08-12/13/14 which **predate the square** — campaign B did not cause it and n=4 undercounted it. **Against itself:** ledger is 6.5 d and left-censored at ws.5; omnibus does **not** reject uniformity (X²=27.7, df=23, crit 35.2) — corroboration of a pre-registered window, not proof; and DEC-0092's sibling-tenant maintenance (00:10→~03:00–05:10 nightly) overlaps the window and is **not** discriminated against. **No code changes** — DEC-0087 scoped the PAUSE to RF-dead episodes and that is exactly what fires it; the flapping is S82's accepted entry/exit-share-a-floor behavior. Consequences: retire the dip watch (job 2 closes); **blocker 2 gains a timing signature**; job 6's probe gains a second target window (00:00–04:00), since host I/O starvation could starve the driver's packet path into its 150 s watchdog and read as RF-dead while the RF is fine. Lesson, re-earned: **read a DEC's resolution before treating its headline finding as live** | Accepted (measurement) · **retires** BOOT job 2's watch · **feeds** blocker 2 / DEC-0081 / DEC-0083 · **extends** DEC-0094's side result · **applies** DEC-0069, DEC-0083 · **does NOT close** blocker 2 | 2026-08-18 (S89) |
| DEC-0098 | **The mechanism probe runs ON the NAS: a laptop-side overnight probe is not a limitation, it is an infeasible design.** BOOT job 6 scoped the probe "read-only from the laptop over ssh, no NAS write"; `ops/proc_probe.py` was built to that scope and hardened inside it (per-batch ssh so a drop costs one batch, `proc_probe_watch.sh` relaunching on process death, idempotent `--resume`, gap-guarded deltas so a resume cannot charge hours of iowait to one hour — all retained). **None of it addresses the real failure mode:** the design required the owner's laptop awake **12+ hours**, correctly rejected as infeasible — nothing local runs while asleep, and `caffeinate -i` does not survive a closed lid. **Worse than inconvenient: DEC-0097's second window is 00:00–04:00, which a laptop-side probe can never sample at all** — the inherited constraint made half the measurement impossible, and it was a prior session's scoping choice, not a rule. **Design:** `ops/proc_probe_nas.sh` runs under `nohup` on the NAS emitting the **same pipe-delimited stream** `proc_probe.py` already parses; `--ingest` reuses `parse_line()` so there is **one** parser for both paths, and merging is idempotent (`--analyze` de-dupes on `(ts,kind,pid,tid)`). CSV column padding deliberately **not** reinvented — the laptop version's first cut mis-aligned system rows by two columns while the header made it look right. **Footprint goes DOWN:** ~2,700 ssh round-trips replaced by `/proc` reads (which never touch disk) plus an append. **Costs, stated:** a Class C NAS write approved in chat first; a resident process on prod during a live campaign, bounded by an end epoch (2026-08-19 05:00) rather than running until noticed, with a `proc_probe.STOP` sentinel checked each cycle; **cleanup owed** (script, pidfile, two logs) once analyzed — tracked in BOOT job 6. **Verified by process evidence (DEC-0074)**, `/proc/28699/stat` = `28699 (proc_probe_nas.) S` plus log growth. **New trap recorded:** `nasctl cat /proc/<pid>/cmdline` returns **empty for a live process** — a false negative caught only by positive-controlling the method against weewxd's own known-live pid. Third instance of *a zero from a look-alike tool is a claim, not a result* | Accepted · **overrides** BOOT job 6's no-NAS-write scoping · **required by** DEC-0097 · **applies** DEC-0074 | 2026-08-18 (S89) |
| DEC-0099 | **NAS-LEASE adoption deferred to the v2.0.14 window, bundled with weewx's first holder exercise.** OPS-DEC-0107 landed 2026-08-15; HLF adopted (DEC-0177, live since 08-16, first real acquire/release cycle logged 08-18). weewx has **zero live levers today** — the one committed-unbuilt lever (InfluxDB `post_interval` deferral, safe to ~30 min per DEC-0092) needs `influx.py` inside the container to see `LEASE_DIR`, which isn't mounted and can't be without a release-class recreate. **Bundling beats building a partial piece now:** v2.0.14 already recreates the container (weewx 5.5.0, #183, DEC-0096), so that's the no-extra-cost moment to add the mount. Plan recorded for that window: mount `LEASE_DIR` read-only; `influx.py` checks it at its own poll and raises `post_interval` while held; the NAS image build (DEC-0078) becomes weewx's first HOLDER, wrapping `docker build` with acquire→flock→release mirroring `rx_experiment.lock`; any lease write we ever do is in-place (seek+write+truncate), **never** `loop_json_writer.py`'s tmp+`os.replace` idiom (DEC-0051), which the spec names as silently stranding a flock on an unlinked inode. **Not adopting a participant role today** — nothing built, nothing to yield, ops#169 stays open against weewx. Separately, reading the world-readable attribution log for our own analysis needs no adoption: first read done S90, one real lease window exists (too small to test anything — see BACKLOG's watch entry) | Accepted · **⚠ its GATING PREMISE is corrected by DEC-0104 (S94) — adoption is NOT blocked on v2.0.14; the plan's content stands** · **extends** DEC-0092 · **answers** ops#169 / OPS-DEC-0107 · **applies** DEC-0051's forbidden-idiom warning | 2026-08-18 (S90) |
| DEC-0100 | **The InfluxDB rollup is dashboard's to build — a Task, not a weewx write-path change.** ops#175's mutual wait (weewx: "dashboard's call"; dashboard: "waiting on weewx to propose a shape") broke when ops posted a strawman: accept-and-monitor for Influx plus a permanent daily rollup (hi/lo/mean + running extremes) so dashboard's `card-records` all-time windows (dash DEC-0111) survive whatever eventually happens to raw, "who builds it" left open. **weewx declines; recommend dashboard builds it as an InfluxDB 2.x Task** (confirmed 2.x, `docs/INTERFACES.md` §2) — Tasks are the native scheduled-Flux mechanism, and dashboard already runs arbitrary Flux against this bucket while `influx.py` has never written a line of it (Python `RESTThread`, line protocol, DEC-0007). `docs/INTERFACES.md` already drew this boundary before this thread existed: *"our responsibility ends at writing the documented schema."* A Task reads raw on its own schedule and writes a derived measurement — **neither write path changes**, no schema update, nothing for INTERFACES to record. Not a mandate: if dashboard's build finds it needs something from our write side, that reopens as a fresh DEC-0010 interface conversation | Accepted · **completes** DEC-0095's open InfluxDB half · **answers** ops#175 · **applies** DEC-0010 | 2026-08-18 (S90) |
| DEC-0101 | **SMTP connections skipped TLS certificate verification at both alert-mail call sites; the WeatherLink API key could leak into `weewx.log` via exception text** — both fixed, found by the S91 full-repo security audit (four DEC-primed finders + an Opus verification pass) | Accepted · **extends** DEC-0062's log-egress rule to a runtime-only leak · **fixes an unverified-TLS regression** against the pattern `influx.py` already uses · **applies** DEC-0045's positive-control rule | 2026-08-18 (S91) |
| DEC-0102 | **Job 6's overnight probe measures DEC-0092's already-flagged confound directly: 11.80x iowait, 5.82x D-state-hit ratio (00:00–05:00) vs. a clean daytime baseline** — far exceeds the evening window's 1.82x (itself corrected from a control-bucketing bug the same ingest exposed, now fixed in `ops/proc_probe.py`). A minute-level cross-check against last night's 4 actual stall timestamps is mixed, not confirmatory (the single highest-iowait hour has zero stalls). ops#169's concurrent coffee-radar full-refresh event is a possible but unconfirmed additional contributor, now held until Campaign B closes | Accepted (measurement) · **extends** DEC-0092/DEC-0097's flagged confound with a direct measurement · **corrects** a control-bucketing bug in `ops/proc_probe.py` · **applies** DEC-0074 · **does NOT close** blocker 2 / DEC-0081 | 2026-08-19 (S92) |
| DEC-0103 | **The bounds/delta split is this repo's QC convention, and it is PORTED into `dewpoint_service.py`, not imported from the driver.** Issue #223's four defects were one gap: `_filter_wind` never adopted the resync-on-reject + co-null behavior `SensorQC` already established here. **The split does the work** — a **bounds** reject (impossible reading, or gust < its own speed) is positive proof of corruption so the baseline is left untouched; a **delta** reject may be a genuine gust front so the baseline **always resyncs, even on reject**. Without that resync a rejected step froze the baseline permanently and every later reading was nulled against it until weewx restarted; a 300 s TTL (= `QC_RESEED_SECONDS`) adds a second, independent escape. **Import was rejected on purpose:** `dewpoint_service.py` has zero driver imports and `docs/INTERFACES.md` commits it to being re-pointable at non-Davis WeeWX — coupling a driver-agnostic service to a vendored fork with USB/subprocess concerns, to reuse ~20 lines for one field, costs more than the duplication. **Consumer-visible consequence:** `windDir` is now co-nulled with `windSpeed` in every reject branch (a bare heading previously reached loop-JSON, InfluxDB and every uploader), matching the driver's own two precedents and narrower than DEC-0054's frame-level co-rejection, which delta still correctly never triggers. Warmup samples bounds-checked before seeding; `windGust` bounds-checked independently of `windSpeed`. **The first pre-fix proof was worthless and looked fine** — all 10 tests failed on `TypeError` from the signature change, not the defects; re-run with the old signature shimmed, 6 of 8 behavioral checks fail pre-fix with the predicted symptom, 0 after. 339/339, mypy 57 files (up from 56 — the count is the only proof the new file was not skipped), secret gate positive-controlled. **BAKED, not mounted** (verified by `nasctl inspect`, positive-controlled, not assumed from `pressure_service.py`) — ships on an image rebuild behind v2.0.14; `CONSTANTS.md` gains the row it was missing | Accepted · **extends** DEC-0054/DEC-0029 · **applies** DEC-0006, DEC-0045 · **closes** #223 · 5 of 8 in #227's sequence | 2026-08-19 (S94) |
| DEC-0104 | **DEC-0099's gating premise was wrong: weewx's NAS-LEASE client is host-side and NOT blocked on v2.0.14.** DEC-0099 deferred adoption to the v2.0.14 container recreate because `influx.py` runs inside the container and cannot see `LEASE_DIR` — true for that one lever, over-generalized into a gate on the whole client (and briefly amplified into `BOOT.md` as a hard deadline). `NAS-LEASE.md` §9 had already settled it: weewx's client's **"natural home is host-side"**, chosen precisely to avoid a release-class recreate. **Holder** (wrap the NAS image build, DEC-0078) runs on the host via `docker build`; **observer** (read lease, append `heavy-io.log`) is `weewx_monitor.py`, already resident, 30 s poll, sees the volume natively — **neither needs a container change**. Only the InfluxDB `post_interval` **yield** lever does, and that is all the v2.0.14 mount buys: worth taking opportunistically, not a blocker. **The "two strands" are one** — coffee-radar's disk-contention handshake IS this lease (their DEC-0181 Stage 2 landed *as* OPS-DEC-0107); Stage 1's `--blkio-weight` caps need nothing from us, and the question nearly posted to ops#169 was already answered in coffee-radar's own `BACKLOG.md`. **★ weewx's adoption LOCKS §5's constants for every tenant** (unlocked "until the second adopting DEC"; HLF's DEC-0177 was first) — a governance act arriving disguised as merge order. **Verified, not assumed:** `LEASE_DIR` exists at `/volume1/docker/nas-lease/` mode 1777, owner step done; `heavy-io.log` live with HLF renewing in production (~8.7 h held 08-19). **Owed before any client ships:** runtime-user create/rename in `LEASE_DIR`, `O_CREAT|O_EXCL` atomicity on btrfs, cross-tenant-visible log append, and a declared renewal floor (§5: weewx "none declared"). §8 ranks the ~08-23 build the protocol's first cross-tenant exercise | Accepted (correction of record) · **corrects** DEC-0099's framing, not its plan · **applies** DEC-0074 · **answers** the weewx half of ops#169 · **does NOT adopt** — the adopting DEC is S95's | 2026-08-19 (S94) |
| DEC-0105 | **`docs/GOTCHAS.md`: traps are durable, so they leave the always-load tier.** `BOOT.md` hit **4,866 tok vs a 2,500 cap (195%)** — four prior rule-1 trimming passes (S84) had moved it ~50 tok net, because deleting resolved items cannot keep pace with a file that absorbs a session's state every close. The gotchas block was **41% of the file** and none of it was session state. **The test:** BOOT holds what expires at a session close; a trap in reading a signal never expires — so it was misfiled, not stale. Moved to an on-demand artifact with a `MANIFEST.md` row, in four sections so the row names a **trigger**, not a topic (an index saying "read this sometimes" is unread, not lazily loaded). Same reasoning DEC-0072 used for standing watches. **Rejected:** distributing the entries into `CONVENTIONS.md`/`ARCHITECTURE.md`/`CAMPAIGN-B-RUNBOOK.md` by topic — each would land in a defensible home but the *class* would stop existing, and the class is the point (those docs say what things ARE; this says how they MISLEAD). **Rule-5 trap recorded:** the first draft also copied in the gate commands, the deploy table and the pyc cache — it read as completeness, but a second copy is what drifts; 8 entries removed and the exclusion stated inline. **Generalizable: moving content OUT of a tier-1 file invites duplicating the canonical docs INTO the new one, because the new file feels like it should stand alone.** **Cost taken deliberately:** `MANIFEST.md` 1,096 → 1,226 (+130, incl. correcting its own stale "~1.1K" self-figure) — 130 tok bought ~1,700 out of the always-load tier, and an unindexed artifact is a lost one. **Result: 4,866 → 2,406, under cap for the first time since before S83**, measured on the pushed `dev` tip. **The cap holds only if new traps are appended to `GOTCHAS.md`, never `BOOT.md`** — `MANIFEST.md`'s row is the load-bearing copy of that rule, since BOOT is rewritten every session. ops#173 left **open on purpose** for the sweep to close | Accepted · **follows** DEC-0072 · **serves** DEC-0063's tiering · **applies** STANDARD rules 1 and 5 · **answers** ops#173 (weewx half) | 2026-08-19 (S94) |
| DEC-0106 | **The reported crash-loop was routine scheduled restarts seen through an undated, accumulating stdout tail — and no check we had could have told the difference.** #245/ops#184 (tier:frontier) reported `weewx-rtldavis-v2` crash-looping, with the owner's own framing the sharper half: *"a lot more crashes of weewx lately, it used to be super stable."* **Measured, Aug 15–19: exactly 4 weewxd startups a day, every one at HH:05 ±30 s** — the four scheduled Campaign B arm swaps, 6 h apart, zero off-schedule. **The contrast case is in our own history:** 2026-08-06's `journal_mode` crash-loop ran **7 starts in 7 minutes** (60–90 s apart). The two signatures differ by three orders of magnitude. **Three things compounded to hide that:** container stdout carries no timestamps; **`docker restart` never increments `RestartCount`**, so the reported `RestartCount: 0` was truthful *and* useless; and the container object was 8 days old, so its stdout accumulated **every** restart into one stream (~32 of them; all three restart markers read exactly 27 in a `--tail 200` window, itself truncation at ~7 lines/start). A 30-line tail therefore shows ~4 restarts back-to-back that are **6 hours apart**. **The owner's perception was correct and the cause is benign:** the rate is **0/day between campaigns** (Jul 20–24, Aug 3–14) and **4/day during them** — every elevated non-campaign day has a known cause (Aug 6 = 7, the WAL loop; Aug 11 = 10, the v2.0.13 deploy; Aug 10 = 3, v2.0.12). Zeros positive-controlled against Aug 3, whose log is full of data and holds zero `weewxd` events. **HLF's staleness is not ours** — through 19:45–20:07Z weewx added archive rows *and* published to InfluxDB every minute, successfully; the day's four stalls sat at 02:55–05:29 EDT, DEC-0097's cluster. **The defect this exposed is structural, not cosmetic:** `soak_check.sh` counted banners with `grep -c` and then tested only non-zero (1 and 50 read identically green) — but fixing the count alone would not have worked, because **`entrypoint.sh` `exec`s weewxd, making it pid 1, so its death takes the container with it and every container lifetime holds exactly ONE banner.** A loop is invisible inside a per-container window and only exists *across* container lifetimes. **So the detector reads a fixed 6 h window and fails when consecutive starts are <1800 s apart** — 7 cases positive-controlled including both boundaries, against the real Aug-6 timestamps. **A rotation trap was caught during verification and proven in prod:** `weewx.log` rotates daily, so a 6 h window run after midnight spans two files; the first live run (00:0x EDT) found its only start in the *rotated* file, meaning the unfixed version would have returned 0 — a false green, at exactly the hour DEC-0097's stalls cluster | Accepted (measurement + fix) · **refutes** #245/ops#184's crash-loop reading, **not** the owner's trend observation · **applies** DEC-0045's positive control and DEC-0074's file-vs-process rule · **extends** DEC-0067's freeze taxonomy with a fourth class · **does NOT touch** blockers 1/2 | 2026-08-20 (S95) |
| DEC-0107 | **ops#169 pre-flight: both weewx lease roles run non-root, and the spec is corrected before adoption locks it.** DEC-0104 left four items owed before any client ships; this session ran them against the live box and settled the corrections directly with coffee-radar (their S205). **The decisive finding:** coffee-radar reasoned both uid gaps were moot for weewx *"if your client runs as root like your rx_experiment tasks"* — those **are** DSM root tasks, but neither is the lease client. Both real halves are non-root and are **different accounts**: observer = `weewx-monitor` uid 1031 (DEC-0009 least-privilege, sudo scoped to `usb_reset.sh`); holder = the owner's own NAS account (**name in the gitignored local-infra doc only**), read off the ownership of every `build-v2.0.*` dir. **So weewx is the first tenant the gaps actually bite** — HLF and coffee-radar are both uid 0, which is why three clean nights proved nothing. Widening the monitor's sudo grant to dodge a file mode: **rejected outright** (trades DEC-0009 for a chmod someone else can make). **Gap A** (`heavy-io.log` root:root 0644) blocks our holder's *mandatory* §4 events; worse than EACCES sounds, because HLF appends under `\|\| :` (`hlf_capture.sh:837`) so a non-root tenant **silently writes nothing and exits green** — DEC-0045's shape exactly. **Gap B** (`LEASE_DIR` 1777) makes §3's break clause unexecutable for us: a stale root-owned lease blocks acquire for the full 8 h TTL, and HLF released `step-failures` on 2 of 3 nights, so that shape is real. **Positions: (a) drop sticky to 0777**; **`chattr +a` on the log** as optional hardening — sticky was *incidentally* the only thing protecting the log from unlink, unpriced because it had only been weighed against the break clause (**unverified on this DSM/btrfs**, self-diagnosing, (a) stands alone if unsupported); **spec edits + both chmods land BEFORE either client DEC**, which also dissolves the who-goes-second question rather than letting date coincidence settle it. **Cost of our own recommendation, priced in writing:** 0777 widens §11's accepted wrong-lease-deletion race from same-owner to any-tenant — a buggy unlink of a *valid* lease leaves the holder renewing into an unlinked inode while everyone else reads the slot free and a second heavy job starts. Still the right trade (low-probability failure leaving evidence > guaranteed silent stranding), but recorded, because naming your own option's cost is the only way it gets weighed. **Discarded so it stays discarded:** break-by-takeover-in-place — fixes Gap B without touching sticky, but a non-root taker-over can never unlink afterward, so the file becomes permanent and every later `O_CREAT\|O_EXCL` acquire fails protocol-wide (checked against `hlf_capture.sh:864-876`). **§5 floor declared as dated DATA:** `docker build` is one invocation with **no mid-job checkpoint** — a third holder shape §5 doesn't describe — so `renewal_floor_s` = whole job; **600 s / TTL 3600 s** from DEC-0078's ~10 min build, **to be re-pinned against the real ~08-23 build**. **Secret-hygiene near-miss, caught by another tenant:** verifying the holder's uid put the owner's personal login (also the local part of the personal email) into three peer transcripts; coffee-radar flagged it unprompted. Verified 0 tracked-file hits and commits under a GitHub `users.noreply` address — **the hazard was ahead, not behind**, since the adopting DEC lands in a public permanent repo. Working line: **build account never; `weewx-monitor` and its uid whenever useful** (already documented). Nothing to rotate. **Both chmods LANDED the same session and were verified independently, not taken on a peer's report** — `nasctl ls` shows `drwxrwxrwx` on `LEASE_DIR` and `-rw-rw-rw-` on the log, so **both gaps are closed on the box before our holder exercise ever runs**; `chattr +a` is **confirmed set** by owner-run `lsattr` (`-----a------------`) — deliberately not closed on its no-error exit, because DSM is the origin of our accepts-and-silently-ignores precedent (DEC-0036's `max-size`); converting that inference to observation cost one read-only command, and **all three fixes now rest on observation, none on report**. **The spec diff also landed the same session** — `NAS-LEASE.md` **v1.4 / OPS-DEC-0110** (`63e2afc`), all six edits, identities generic — and **HLF's `hlf_capture.sh` explicit create mode landed and deployed the same session** (their PR #388, `066bbf9f`) — they also found **a second create we had not flagged**, the steal-then-acquire retry, which only runs after a crash, i.e. exactly when the lease matters most. **All three items closed inside one session; the round is fully closed and weewx's adoption is no longer blocked on anyone else.** **Not the adopting DEC** — that is the §5 lock event and must lock a *corrected* spec; it takes the next free number when our client ships at the ~08-23 build | Accepted (pre-flight + negotiated positions) · **follows** DEC-0104 · **answers** ops#169's second-tenant round · **applies** DEC-0074 (verify, don't recall) to *identity*, DEC-0045 (a green from an unexercised path is not evidence), DEC-0047 (the gate guards commits, not reads) · **defers** the adopting DEC deliberately | 2026-08-20 (S96) |
| DEC-0108 | **weewx's NAS-LEASE holder client ships as `ops/nas_build.py` — a generic lease-wrapper, scoped to the holder side only.** Built ahead of the ~08-23 v2.0.14 build so the mechanism is tested before it runs for real. Mechanics: `O_CREAT\|O_EXCL` acquire, explicit `fchmod 0644` (§3 v1.4 — never trust umask, the exact near-miss DEC-0107 found on the box), exclusive `flock` held for the wrapped command's run, stale-break only when flock is free **and** `expires_at` has passed (both, not either), release-with-truthful-outcome (`clean`/`build-failed`/`crashed`) wrapped in `try/finally` so a crash still releases. Generic over any command, not build-specific — serves both of weewx's named holder cases (§6: the image build, and manual bulk historical analysis) from one script. Python, matching §9's own stated assumption and this repo's existing `ops/` split (Python for protocol/data logic, bash for process orchestration). Preserves the existing `BUILD-EXIT` marker convention (`CONSTANTS.md`) but reads the exit code off `subprocess.Popen` directly, sidestepping the `cmd \| tee log; echo $?` pipeline-exit trap that convention exists to guard against. **Scope decision: the observer/downshift side (§3's read protocol) is recorded, not built** — weewx has no live downshift lever to act on a "held" verdict yet (§6: current.json's lever is spent, the InfluxDB deferral lever is committed-unbuilt and needs container access this host-side client lacks), so a courtesy read with nothing to act on has unclear benefit today; revisit once the InfluxDB lever actually ships. Floor/TTL ship as §5's provisional 600s/3600s literals verbatim, to be re-pinned against the real ~08-23 build's measured duration — the one piece that can't happen before the event. Does not touch the rest of the release sequence (tag regen, `EXPECT_IMAGE` bumps, Hub push stay manual) and is **not** the adopting DEC — §5 stays unlocked until that DEC lands with real numbers. 14 new tests against a real `flock()` on a `tmp_path` dir (not mocked), 373/373 full suite, ruff/mypy/secret-gate clean | Accepted (design + build) · **follows** DEC-0107 · **implements** the holder half of `NAS-LEASE.md` §6/§9 · **not** the adopting DEC | 2026-08-20 (S97) |
| DEC-0109 | **ROADMAP's last P0.5 follow-on — Keep-a-Changelog headings + DECISIONS entry-skeleton convergence (proposed S25) — is retired, not picked up.** The original S25 rationale is unrecoverable: no transcript survives (local retention starts 2026-07-21, 16 days after S25's 2026-07-05), and S25's own `CHANGELOG-ARCHIVE.md` entry is entirely unrelated code-quality fixes — the one-line ROADMAP bullet is the entire surviving record. Checked whether a family standard has since emerged that this repo is merely behind on: dashboard's `CHANGELOG.md` is equally free-form prose (no Keep-a-Changelog headings); HLF has no separate `CHANGELOG.md` at all (`DECISIONS.md`/`-FULL.md` serve that role). Nobody adopted it — there is no target to converge toward. The DECISIONS half is separately moot: `DECISIONS-FULL.md` already grew its own working skeleton since S25 (`**Status:**`/`**Date:**`/`**Follows:**`/`**Answers:**` + `### Context`), entirely independent of `CHANGELOG.md` — it converged with itself. **Judgment call, not just absence of evidence:** retrofitting Keep-a-Changelog's Added/Changed/Fixed/Removed categories onto this repo's entries would likely make them worse — entries here are dense, cross-referencing narratives (a single bullet routinely spans a shipped fix, its measurement, the PR, and a cross-repo pointer) that a rigid single-facet schema would fragment rather than clarify. 72 sessions unclaimed, family-wide non-adoption, and an architectural reason it likely never took hold, read together as a signal, not a gap to fill. P0.5 is now fully closed | Accepted (retirement) · **closes** ROADMAP.md's P0.5 | 2026-08-20 (S98) |
| DEC-0110 | **Archive-level reception-quality wind guard, closing the ERR-0004/ERR-0006 blind spot.** Both incidents are the same gap recurring independently: a mid-magnitude phantom wind reading (37–39 mph from calm) that passes both the bounds check and the 75 mph delta cap because a genuine squall gust of similar size is real and routine — and neither incident had another field in the same frame fail bounds, so DEC-0054's frame co-rejection had nothing to trigger on. Both share a signal neither filter uses: `rxCheckPercent` collapsed to 13.2%/9.2% in the exact bad minute, vs. 60–90%+ every surrounding minute. **Measured before designing anything** (93 days, 129,607 records): genuine high wind and severe reception collapse have never co-occurred at this station — the lowest `rxCheckPercent` among 220 records with `windGust>=10mph` is 54.5%, and 87 of 89 `rxCheckPercent<20%` records stayed calm at 0–4 mph. **Design:** `rxCheckPercent<20%` AND `windGust>10mph` → null the wind triple + derived `ET`/`appTemp`/`windrun`, checked at archive-record time (not loop-packet time — `rxCheckPercent` isn't final until the interval closes), which is also the correct choke point: `DewpointCacher` sits in `process_services`, confirmed via `weewx.conf`'s own `[Engine][Services]` order to run before `archive_services` (the DB write) and `restful_services` (every uploader — Wunderground, CWOP, PWSWeather, OWM, WOW, Ogoxe, Influx). Explicitly does NOT reach Wunderground's RapidFire feed (publishes pre-archive-close; a live ticker, not an archive of record). 11 new tests, both incidents replayed verbatim as positive controls; 397/397 full suite, ruff/mypy clean. Ships with the same ~08-23 v2.0.14 build already gated for #225 — baked into the image, no urgency to force an earlier cut | Accepted (design + build) · **follows** ERR-0004, ERR-0006 · **implements** BACKLOG.md's proposal | 2026-08-20 (S98) |
| DEC-0111 | **`influx.py` NAS-LEASE courtesy yield — the observer/downshift half DEC-0108 deliberately left unbuilt.** `InfluxThread` gains an opt-in `lease_dir` param (default `None`, off unless `weewx.conf` sets it) and a thin `skip_this_post` override: while another tenant's `NAS-LEASE.md` lease is held and unexpired, `post_interval` rises to 1800s, safe per DEC-0092's own data-integrity analysis (`stale=None`, `max_backlog=1,000,000` — a 30-min deferral queues ~30 records against a million-record cap and loses none). **Mechanism verified against the real weewx 5.5.0 source** (downloaded to a scratch dir for inspection, not recalled): `self.post_interval` is read fresh per queued record inside `RESTThread.skip_this_post`, so a runtime mutation needs no thread restart. Fail-safe direction is load-bearing: any lease-file read/parse failure, or weewx's own held lease, is treated as not-held — normal operation, never a defensive slowdown. 10 new tests (first-ever dedicated `influx.py` test file), 407/407 full suite, ruff/mypy/secret-gate clean and positive-controlled. Two NAS-side steps remain outside this DEC's scope: the `LEASE_DIR` bind-mount and `weewx.conf`'s `lease_dir` setting, both deferred to the ~08-23 build-night event alongside `#224`/DEC-0110/the NAS-LEASE holder adoption | Accepted (design + build) · **follows** DEC-0099, DEC-0104, DEC-0108 · **implements** the `post_interval` yield lever both named | 2026-08-21 (S99) |
| DEC-0112 | **Adopt `CLOSEOUT-TEMPLATE.md` step 7 (OPS-DEC-0114) as this repo's closeout step 8** — a session names itself as the last closeout action, `set_session_title("self")` in the grammar `weewx S<nums>[ ✓] · <slug>`. The `✓` attests only that this session's own closeout (steps 1–7) actually ran — not that a PR merged, not that the window is still resumable. Renumbered to 8, not 7, because this repo's closeout skeleton (DEC-0052) already had a 7th step (commit + push) before the generic template existed here. Owner decision on weewx-rtldavis#264, following coffeeradar/dashboard/HLF's prior adoption (DEC-0190/0276/0193) | Accepted | 2026-08-21 (S99) |
| DEC-0113 | **WeatherLink pressure poll tightens hourly → 5 min, backed by the measured API ceiling, not a guess (#144 item 3).** `fetch_interval=3600` dated to early deployment, set to "what I thought was as fast as the free tier would allow" (owner) but never checked. WeatherLink v2's documented limit is **1,000 calls/hour + 10/s**; 300s uses 12/hour, ~1.2% of quota. Chosen over tighter still because the SQLite archive is built from loop packets at a fixed 60s interval regardless of poll rate — polling faster than that buys nothing. **Queued, not live**: `fetch_interval` lives in the MOUNTED `weewx.conf` (confirmed via `nasctl conf` — the file already sets it explicitly, code default is a fallback only), so activating it needs a `weewxd` restart, deferred to the same ~08-23 v2.0.14 event as everything else behind Campaign B's comparability discipline (DEC-0059/0064/0092). New `CONSTANTS.md` deviations-table row + `BOOT.md` job-list entry so it isn't dropped. **Item 1 (the ~0.03 inHg console-vs-METAR offset) reviewed and closed with no change**: owner confirms the console applies WeatherLink's own elevation-based sea-level correction for the surveyed 550 ft as designed (the DEC-0086 mechanism); the residual is small, already absorbed by HLF's per-source correction by design, and #144 itself called leaving it alone "also fine" | Accepted (research + queued config change) · **resolves** #144 · **applies** DEC-0086 · **queued behind** DEC-0059/0064/0092 | 2026-08-21 (S99) |

| DEC-0114 | **The ~08-23 v2.0.14 build event ships; NAS-LEASE adoption locks.** Built natively on the NAS from `origin/dev`@`efeeebd` under `ops/nas_build.py`'s holder wrapper, tagged `:v2.0.14`, carrying DEC-0110/DEC-0111/#233/#224/DEC-0113's queued config edit and weewx 5.4.0→5.5.0 (pinned since S88, first release to actually ship it). **Two real problems found and fixed live**: `docker` wasn't on the non-interactive SSH PATH (first attempt crashed instantly); a retried build genuinely hung 70+ minutes at 0:00 CPU on a `weectl` syslog-handler crash mid-Step 7/30 (confirmed via `ps aux`, not log silence alone) — killed, stale lease cleared, retried clean in ~360s. **A third, subtler miss**: `influx.py`'s DEC-0111 code never reached the running container despite a fresh image — it's a MOUNTED file, so the NAS-side mount source still held the old `ws.1` code until a separate `scp` fixed it; caught by checking the live banner post-deploy, not assumed from a clean build. **NAS-LEASE adoption locks** (the §5 event DEC-0104/DEC-0107 deferred): `RENEWAL_FLOOR_S` 600→420 (measured ~360s + margin), `TTL_S` held at 3600 deliberately (this session's own hour-plus hang argues against tightening it). Live contention with HLF's `daily-maintenance` resolved via direct session-to-session coordination (new SOP this session), not passive waiting | Accepted (design + build event) · **locks** NAS-LEASE.md §5 (deferred by DEC-0104/DEC-0107) · **ships** DEC-0110/DEC-0111/#233/#224/DEC-0113 | 2026-08-23 (S101) |
| DEC-0115 | **Campaign B adopts gain 496 (arm B) as the new RF baseline — at exactly DEC-0059's adoption bar.** The apparatus log pools 6 aborted/restarted attempts by default; re-scoped to the real completed run (`--since` 2026-08-15T00:05, the tool's own pooling warning caught this). Clean 32/32-block square: **A (372/ex0, incumbent) 72.83% · B (496/ex0) 74.83% (+2.00) · C (372/ex50) 73.28% (+0.45) · D (496/ex50) 74.77% (+1.94)**. Gain axis clearly favors 496; extraction axis a wash, matching the S97 interim read now confirmed complete. **The margin is exactly 2.00, not comfortably above the bar** — put to the owner directly; adopted anyway given the consistent direction across both readouts and both extraction levels. DEC-0102's overnight-iowait confound (a separate investigation track) was still open as of `BOOT.md`'s last note — recorded as an interpretive caveat, not treated as invalidating. **Narrower sweep near 496 explicitly declined for now** — pilot data suggests a flat curve there, not a hidden nearby peak; each additional value costs its own multi-day square. Deployed to both live `weewx.conf` and `weewx.conf.rx-baseline` (the DEC-0080-taught lesson: a live-only edit gets silently wiped by the next campaign's own restore path) | Accepted (data + config change) · **resolves** DEC-0048/DEC-0059's designed RX experiment · **closes** DEC-0017 · **supersedes** the `73.3%, sd 4.67` baseline in `CONSTANTS.md` | 2026-08-23 (S101) |

| DEC-0116 | **`loop_json_writer.py`'s mount was stale for four weeks despite v2.0.14 shipping — caught closing #172/#204, fixed live.** An ops session asked to confirm #144/#172/#204/ops#179 were live and close whichever were done. #144 checked out. #172 and #204 did not: both features (`barometer_fetch_epoch` DEC-0091, `current_interval` throttle DEC-0093) live in `loop_json_writer.py`, a MOUNTED file per `CONSTANTS.md`'s deploy-layers table. The deployed copy was still the 2026-07-27 version — confirmed via mtime, hash mismatch, a grep for both features finding neither, and a live `current.json` read missing the field entirely. Same trap DEC-0114 caught for `influx.py` three days earlier, just outside that event's own verification scope. Fixed: deployed current `dev`'s `loop_json_writer.py`, hash-verified, `docker kill`+`start` (DEC-0008), confirmed live post-restart on the first WeatherLink poll. **Lesson: an image bump proves nothing about a mounted file's state unless that file was specifically checked** — the deploy-layers table's other mounted files remain independently unverified | Accepted (verification + deploy fix) · **corrects** BOOT.md's S101 "in sync" claim · **recurs** DEC-0114's `influx.py` miss | 2026-08-25 (S102) |
| DEC-0117 | **Gain / receive-window hot swap: a validated control file, not a command string — plus the post-swap watchdog grace that makes it safe.** S89's finding held up: nothing prevented a hot swap but the trigger — gain and `-ex` are startup-only CLI flags on the Go binary, `ProcManager.startup(cmd, …)` already takes the command as a parameter, and the 150 s watchdog exercises kill→respawn routinely. Built as a watched control file accepting **only** bounds-checked `gain` (0–496) and `ex` (0–1000) integers, spliced into the existing command — **never a command string**, because `cmd` reaches `shlex.split()` → `Popen` and the path is a NAS share on a public-repo project. **The hazard the backlog note missed:** `time_last_received` is a local in `genLoopPackets` and is not reset by a respawn, while a fresh child is legitimately silent for the US 133 s init period — a naive swap trips the 150 s stall watchdog mid-init and tears the driver down, reintroducing the very abort-on-unhealthy-swap failure class the feature exists to retire. Every swap now resets the four watchdog counters and widens the threshold to `HOTSWAP_GRACE_S = 240` until the first packet (150 s left only 17 s over the init period). Plus rollback to the last known-good command on a failed startup, an atomic ack file recording the measured respawn gap (self-measuring ops#179's unmeasured constraint 4), and the control file honored at init so a container restart cannot silently revert a swapped gain — DEC-0116's shape. Loop tests mutation-verified with a positive control. **Default off; `rtldavis.py` is BAKED, so prod is unchanged until an image rebuild** | Accepted (design + implementation) · **closes** `BACKLOG.md` hot-swap item + [ops#179] · **does not** convert `ops/rx_experiment.sh` | 2026-08-26 (S103) |
| DEC-0118 | **Production migrates from the NAS to marvin; a live incident during cutover was the USB controller, not RF.** Moved up from a planned Saturday to Friday night 2026-08-28 on the owner's call, coordinated live across four repos. Transparent to consumers by design — marvin's NFS export overlay-mounts directly over the NAS path eh-proxy/HLF already use, so no compose edits; the archive copy had to happen *before* the overlay landed (it hides the original dir). A ~90-min live incident (packets every 42-48s, not sustained) was chased through gain (496→372, no change), fc/ppm (no historical data exists to test — DEC-0064 held both at 0 through every real campaign, deliberately never measured), and a process-freeze read via DEC-0067's own discriminator (initially fit, self-corrected once the watchdog started visibly cycling) before the real cause surfaced: every USB port on the B850 chipset's own controller breaks hop-tracking under sustained streaming (confirmed with `rtl_test`, not weewx) while the CPU-attached controller is clean — plausibly AM5's shared, QoS-less chipset PCIe uplink. **Bonus corroboration for DEC-0067/0081**: the 150s-raise+~60s-respawn cycle matched their predicted arithmetic exactly and fired *only* under the bad controller, never once in an hour of subsequent healthy operation — supports reading those freeze episodes as environmental/hardware, not a software regression, without closing the general question. Gain stays at 372 but is NOT re-validated by this incident (the controller was the actual defect); a proper re-sweep at marvin's RF position is backlogged. NAS-LEASE's courtesy-yield mount is a deliberate no-op on marvin for now (`MARVIN-DEC-0063`). A live SQLite backup gap (`weewx.sdb` continuously written into marvin's 03:30 restic window) was caught and closed same night with a `.backup`-API dump timer at 03:15 | Accepted (infra event + incident) · **corroborates, does not close** DEC-0067/0068 · **does not re-validate** DEC-0115's gain · **does not change** the deploy-layers model in `CONSTANTS.md` | 2026-08-28/29 (S105) |
| DEC-0119 | **ops#183's Influx outage: root cause was external, the backfill tool had two real bugs, and this repo's own alerter was blind the whole time.** weewx's InfluxDB uploads were down 00:14→12:08:27 ET (~11h54m) because `eaglehunt-ops` deleted a token it didn't know `influx.py` also wrote with (`OPS-DEC-0162`) — weewx's own config was never at fault, confirmed unchanged since 08-23. Fixed with a new dedicated token (marvin-side session, direct owner auth — this repo's own `marvinctl` access has no arbitrary file write by design). **`ops/backfill_influx.py` had two real bugs** found running it live for the first time: a committed `INFLUX_ORG` placeholder that would fail every POST, and a read-write `sqlite3.connect()` that fails against a read-only export — both fixed, plus `--server-url`/`--db-path`/env-var-token hardening, on PR #282. Backfilled clean: 712 records, 0 errors; the one apparent residual gap was two counts disagreeing on an inclusive-vs-exclusive hour boundary, not a missing record; the two genuine shortfalls (14 and 6 records/hour) are unrecoverable source-side loss from `hardware`-mode archive generation during the RF fight and the restart/reboot windows. **`weewx_monitor.py` (this repo's own artifact) sent ~14h of false "STILL DOWN" alerts** because it watches a hardcoded Foundation log path the marvin NFS overlay doesn't cover (covers `weewx-data/`, not `logs/`) — DEC-0074's file-vs-process lesson one level up, and the false alarms' outage-shaped noise is what delayed catching the real Influx outage. Disabled on Foundation (no marvin path exists to repoint to); a second stale watcher (`usb_watchdog.sh`) found in the same sweep, filed on `eaglehunt-ops`#233; neither has a marvin-side equivalent yet. All twelve publish legs verified healthy via the newly-live `marvin-weewx` alias, this repo's own session's first real use of it | Accepted (incident response + tooling fix) · **relates to** DEC-0118 (followed it within hours) · **applies** DEC-0074 (file-vs-process, one level up) · **does not decide** a marvin-side alerter replacement | 2026-08-29 (S106) |
| DEC-0120 | **Rebuild the alerting for marvin: input staleness is now its own state, and the remedy is selected rather than assumed.** The ~14 h of false "STILL DOWN" mail on 08-29 was **not** simply a wrong path — every threshold in `weewx_monitor.py` is of the form "nothing seen for N seconds", and a frozen input satisfies all of them at once, so the monitor structurally could not tell *the station is down* from *I am blind*. Repointing the path fixes the instance and leaves the mechanism. Blindness is now checked **before** any threshold, on the worse of the log's mtime and the newest parsed line's timestamp (the second catches content going stale behind a fresh mtime — the "right path, wrong host" shape), reported as a **distinct alert class**, and it **suspends** uploader/reception judgement while it holds. **ops#233's framing corrected:** marvin's `logs/weewx.log` is alive, local and healthy — the input problem exists only when looking from Foundation, so jobs 1–2 port on an env var. **`REMEDY_MODE` replaces the assumed USB reset**: `usb_reset` (Synology, kept as DEFAULT — this is a published extension and our efficacy evidence is from our hardware), `restart_unit` (marvin — `weewx.service` is `docker run --rm` + `ExecStartPre=docker rm -f`, so a restart IS the full container recreate that resolved ERR-0005), `none`. The Foundation body is deliberately **not** ported: it unbinds a hardcoded Synology bus path, and `MARVIN-DEC-0051` reassigned controller roles, so on a two-tenant box it would no-op or reset someone else's device. Plus a **campaign inhibit** (a campaign restarts weewx per arm; a remedy mid-arm corrupts the block) that logs the action it *would* have taken. **Nothing deployed** — the unit ships at `REMEDY_MODE=none` until it has run a full day and `t-weewx` actually holds a restart grant (DEC-0061's configured-but-never-exercised trap). Found in passing: marvin's `weewx_monitor.py` is **stale vs `dev`** (ops#214 family). Open: the archive DB's WAL/`?mode=ro` readability as an unprivileged user — unverified, do not assume | Accepted (design + implementation; **deploy deferred**) · **answers** `eaglehunt-ops`#233 · **follows** DEC-0118/DEC-0119 · **generalizes** DEC-0074 · **acts on** DEC-0081's closed finding | 2026-08-30 (S107) |
| DEC-0121 | **Campaign C: the apparatus learns marvin, and refuses to run without its safety net.** `ops/rx_experiment.sh` rewrites the live config and restarts prod unattended overnight; checked against the new host, four things were wrong and **three fail silently**. (1) `DOCKER` hardcoded to the NAS path. (2) **The restart mechanism takes prod DOWN**: it does `docker kill`+`docker start`, but marvin's unit is `docker run --rm` with `ExecStartPre=docker rm -f`, so the kill DESTROYS the container and the start has nothing to start — `RX_RESTART_MODE` now selects `docker` (NAS, unchanged default) or `systemd`. (3) **The abort tripwire had no input** — `ABORT_PCT` and the pause guard both read `weewx_monitor.log`, which no host writes on marvin, so the campaign would never abort however bad reception got, DEC-0120's shape one layer up. (4) A pre-migration baseline snapshot sits on marvin (latent: `install` already refuses while one exists). Fix is a **refusal, not a repair**: new `preflight` gates `install`, requires a FRESH monitor log, and has **no `--force`**. **The pre-registered order was also wrong and the clock caught it**: the morning notch is hours **07–09 at 2–3.5 pts** — larger than the 2.0-pt effect — and the first order put blocks 8+9 both on B, loading **1.67 of 2.0 notch block-equivalents onto gain 496**, the arm expected to win. Balancing a linear trend is not balancing a localized dip. New order splits it **1.00/1.00** with drift still 27/28. Now machine-checked with a positive control asserting the old order still reads lopsided; campaign B's tests are **guarded, not deleted**. **Reverses this session's ordering: the monitor deploys BEFORE the campaign, because it IS the abort tripwire's input** | Accepted · **extends** DEC-0059 to a second host · **depends on** DEC-0120 · **follows** DEC-0118 | 2026-08-30 (S107) |
| DEC-0122 | **Campaign C launches tonight (08-30), not tomorrow — the wait's own rationale evaporated.** DEC-0121 pre-registered a 08-31T20:00 launch specifically to let the freshly-deployed monitor prove itself across a log-rotation boundary before trusting it as the abort tripwire. Checked directly on marvin mid-session: **marvin has no logrotate configured for `weewx.log` at all** — there is no rotation boundary to prove the monitor against, so waiting longer could never satisfy the precondition. Owner's call: launch tonight instead, with a fresh hands-off-guest declaration scoped to tonight's window (MARVIN-DEC-0088, PR #290). `SCHEDULE=` shifted by a pure −1 calendar day — same 10 blocks, same clock times, same `A B B A B A A B A B` order — so DEC-0121's notch balance (1.00/1.00, drift 27/28) is untouched; a calendar shift, not a redesign. **Not fixed here:** the logrotate gap itself remains, checked-clear for tonight (DEC-0123) but still owed as a durable job before the next long campaign | Accepted (schedule change) · **follows** DEC-0121 · **depends on** the monitor deploy (BOOT job 1) | 2026-08-30 (S108) |
| DEC-0123 | **`weewx-rx-experiment.timer`: campaign C's tick/guard scheduler, missing since the host move, ported to marvin.** DEC-0118 moved prod to marvin without porting Foundation's DSM cron that drove `ops/rx_experiment.sh tick`/`guard` every 5 min for every prior campaign. Block 1 was launched by hand tonight and then sat un-advanced — nothing called `tick` again, and `guard` (the only abort-on-bad-reception check) never ran at all. DEC-0121's `preflight` checks the monitor log is fresh; it does not check anything keeps calling `tick`/`guard` after launch — a gap in the gap-catcher, found live. New `ops/weewx-rx-experiment.service`+`.timer` (5-min cadence, marvin-pinned env overrides — `RX_BASE`/`RX_DOCKER`/`RX_RESTART_MODE=systemd`/`RX_RESTART_UNIT`, since the script's bare defaults assume the NAS and would otherwise fail or take prod down per DEC-0121 finding #2), **runs as root** (mirrors the Foundation cron and tonight's manual launch — `t-weewx` has no restart grant yet, BOOT job 4). **Installed and confirmed firing on marvin since 22:18:16 ET** — first fire self-healed the overdue block 1→2 swap; `guard` has run every cycle since. **Not settled:** whether the timer should be disabled between campaigns or left inert (DEC-0096's empty-`SCHEDULE=` no-op makes either safe). Same failure shape as the logrotate gap — something never ported in DEC-0118's move, not something pointing at a wrong host | Accepted (design + implementation, deployed) · **follows** DEC-0121 · **extends** DEC-0118's host move to a piece it missed | 2026-08-30 (S108) |
| DEC-0124 | **Campaign C completed clean (all 10 scheduled rows, 2026-08-30T21:24 → 2026-08-31T11:00 ET, no aborts, self-terminated to BASELINE exactly on schedule) — but the 372-vs-496 verdict at marvin's RF position is NOT decided.** A coarse proxy (monitor's 5-min aggregate, not the sanctioned metric): A 70.76% (n=76) vs B 72.63% (n=70), B +1.87 pts — under DEC-0059's 2.0-pt bar and explicitly not the call, since DEC-0069 exists precisely because this coarse metric absorbs freeze-contamination bias. The sanctioned per-minute readout is blocked: `ops/campaign_analyze.py` is NAS-only, never ported to marvin; `marvinctl` has no SQL verb ([ops#235](https://github.com/WeatheredScientist/eaglehunt-ops/issues/235), filed S107). This session tested `marvinctl exec-ro` as a workaround (owner go-ahead) and confirmed, with a positive control, it has no working path — stdin forwards nothing, `-c` argv rejects quotes/parens even at zero literal whitespace, so no code can be expressed through it; findings added to ops#235 rather than duplicating. **ops#235 is now a priority** — it blocks a live RF-gain decision, not a convenience. Housekeeping: `ops/rx_experiment.sh`'s `SCHEDULE=` stood down to the empty form (DEC-0096) now that the terminator passed; suite green after (457 passed / 14 skipped) | Accepted (measurement session; adoption undecided) · **superseded by** DEC-0125 · **follows** DEC-0121/DEC-0122/DEC-0123 | 2026-08-31 (S110) |
| DEC-0125 | **Campaign C's real per-minute verdict: 496 does NOT clear the adoption bar at marvin's RF position; 372 stays.** ops#235's stdin-pipe fix landed and was verified end-to-end with the actual blocked read (1333 archive rows, exit 0). Ran DEC-0069's own `campaign_analyze.py` logic unmodified against the fetched rows: **A (372) 72.82% (n=368) vs B (496) 73.98% (n=350), B +1.16 pts — under DEC-0059's 2.0-pt bar**, and *smaller* than DEC-0124's coarse proxy (+1.87), not larger. Per `BACKLOG.md`'s S107 pre-commitment (written before any data existed), this is the "does not clear" outcome: no config change — prod already runs 372 — and the finding is that marvin's position changed the answer versus Foundation, not that DEC-0115 was wrong for Foundation. One clean overnight run stays directional; a real multi-day campaign is the next step if the question is worth resolving further, not another single-night pull. Also verifies `BOOT.md` job 4 (archive DB opens `mode=ro` cleanly — `journal_mode=DELETE`, no `-shm` complication) | Accepted (measurement + decision) · **resolves** DEC-0124's open verdict · **does not re-open** DEC-0115 | 2026-08-31 (S111) |
| DEC-0126 | **Campaign D pre-registered: a marvin-site gain pilot, six arms including 207, launching 2026-08-31T21:00 ET.** Triggered by DEC-0125 — Foundation's pilot-derived shortlist {372, 496} was never actually validated for marvin, so this re-runs the shortlisting step there. Six gain-only blocks HIGH→LOW: 496, 449, 402, 372, 328, 207 (`-ex 0` fixed). First five reuse Foundation's original pilot points; **207 is new** — campaign C dropped it as "Foundation's known-worst," the same shape of assumption DEC-0125 just showed doesn't transfer. 45-min blocks, 21:00→01:30 ET, clear of the site's notch hours (07-09, 19) on both ends. HIGH→LOW is a deliberate pilot-safety property (an abort on a weak low arm leaves the useful arms already harvested), not just precedent — no Latin-square balancing needed since a pilot is arm-selection input only (PRINCIPLES §3), never adoption evidence. **No USB-reset safety net on marvin, none needed** — the fault it existed for doesn't recur on marvin's CPU-attached xHCI (MARVIN-DEC-0064/0100). Shipped: `arm_cmd()` gains `P207`; `SCHEDULE=` populated; `campaign_analyze.py`'s `LEGENDS` gains `"D"`; `tests/test_rx_experiment.py` gains `_require_campaign_d()` + 3 structural tests, and `_require_campaign_b()`'s gate is corrected (was "any P* row," now "the H hold specifically") — the old gate would have misfired campaign B's assertions against campaign D's pilot-only shape. Full suite green (465 passed / 9 skipped). Pre-reqs before launch, not yet done: fresh hands-off-guest declaration, `logs/campaign.inhibit` | Accepted (design, pre-registered) · **triggered by** DEC-0125 · **extends** DEC-0064's pilot design to marvin | 2026-08-31 (S111) |
| DEC-0127 | **Full-history rewrite + force-push (2026-09-01): privacy scrub of private infrastructure identifiers and personal email addresses.** The S112 public-accessibility audit found real private-infrastructure identifiers in a small number of historical file versions (long since scrubbed from HEAD, but permanently fetchable from public history) and a personal email address in early commit metadata — a pseudonym-to-identity link. Owner's explicit call: **privacy and security outrank history immutability** — rewrite and force-push rather than accept the exposure. Executed via `git-filter-repo` (`--replace-text` + `--mailmap`) on a fresh mirror: all 661 commits rewritten, every branch and tag force-pushed, branch protection lifted and restored with its exact original config, local clone re-pointed. Verified clean with content scans **and positive controls** on every axis (file contents, commit messages, author/committer metadata). Zero forks existed, so no fork network retains the old objects; a GitHub Support purge request covers the server-side cached/unreachable objects and `refs/pull/*` pins. `SECURITY.md` carries the public re-clone notice — deliberately generic (the guard rails go public; the exposure specifics do not, per S41 doctrine). Specifics recorded in the gitignored local-infra doc. **No credential, key, or token was ever committed on any branch** — verified across full history | Accepted (owner-directed, executed) · **supersedes** the S41/S42 partial scrubs (`DEC-0050`-adjacent) which cleaned HEAD but could not clean history | 2026-09-01 (S112) |
| DEC-0128 | **Campaign D closes the gain axis at marvin: the curve is flat, no candidate shortlists, 372 holds.** Ran exactly as pre-registered — six 45-min gain-only blocks HIGH→LOW, 2026-08-31 21:01 → 09-01 01:30 ET, no aborts, self-terminated and restored prod at 01:30:39. DEC-0069's `campaign_analyze.py` logic unmodified (763 rows, 255 in-block). **P496 74.65 · P449 73.79 · P402 74.98 · P372 74.97 (incumbent) · P328 73.29 · P207 68.17.** Gain 328–496 is **one plateau — 1.70 pts of spread against a ~1.61-pt per-arm SE**, best delta **+0.01**, nothing near DEC-0059's 2.0-pt bar: a shortlisting pilot that shortlists **nothing**, so there is no candidate left worth a confirmatory square. **207 is the one real result (−6.80, t=−3.75)** and agrees with the physics — campaign A's near-parity for 207 was **LNA-in**, and DEC-0017's "207 optimal" is a with-preamp finding; LNA out since 08-02, 207 is simply too little front-end gain. Three independent 372 anchors (pre-baseline 74.85 · in-block 74.97 · post-baseline 74.17) span **0.80 pts across 6 h**, which also retires DEC-0126's knowingly-accepted HIGH→LOW ordering risk: the 45 min after P207, back at 372, read 74.17%, so 207's deficit is the gain and not the clock. **Larger, undesigned finding:** like-for-like full-diurnal campaigns, LNA out both — **372: Foundation 72.83 vs marvin 72.82 (−0.01); 496: 74.83 vs 73.98 (−0.85)**. DEC-0118 moved the receiver measurably closer with fewer walls and reception did **not** move. With the `-ex` axis already a wash (+0.45/−0.06), that is **three axes — gain, receive window, siting — all flat at ~73–75%**: the missing ~25% is **not SNR-limited and not reachable by tuning**. The denominator is every ISS transmission (`max_count = period // loop_times[x]`, `rtldavis.py:1622`, no hop discount), so the ceiling sits in decode/hop-tracking or in the denominator's own realism. Caveats: D's absolute level runs ~2 pts high because it deliberately sat in the site's best hours and never touched the 07–09 notch (**D's arms compare to each other, not to B/C absolutes** — the cross-site table uses B/C only); n≈33/arm resolves ~4 pts, so a 2-pt effect is **not** excluded, only a large one; one night. **No config change** — the pre-committed reading was arm-selection-only and it selected no arm. `SCHEDULE=` emptied to DEC-0096 stand-down (also unblocks the TZ-corrected staleness tripwire). **Gain axis closed at marvin — do not re-sweep without a new reason.** Method gap: `campaign_analyze.py`'s `fetch()` is still NAS-hardwired (pre-DEC-0118) — transport hand-assembled via `marvinctl exec-ro` for the second campaign running; port it before a third | Accepted (measurement + decision) · **executes** DEC-0126 · **closes** the gain axis (DEC-0017, re-opened for marvin by DEC-0125) · **does not re-open** DEC-0115 | 2026-09-01 (S113) |
| DEC-0129 | **The ~25% loss is real, structural and OURS: denominator honest, variance is pure counting noise, `ppm`/`fc` axis dead.** DEC-0128's two free read-only checks, both run, neither touching prod. **(a) Denominator honest — hypothesis rejected.** `loop_times` is exactly Davis's `(41+id)/16` s for ids 0–7, counting transmissions the ISS really made, no hop discount owed (a tracking receiver can decode every one): **the missing ~25% is undecoded packets, not a measurement artifact.** *Incidental:* `max_count` is recoverable from published values (`100 × count/max_count`, both integers) and is **not the constant it should be** — across 753 records whose predecessor is exactly 60 s earlier it varies **19–23**, where `iss_channel=1` → id 0 → 2.5625 s → it would be **23 always** (23 is prime, so ~95% of records would reduce to 23; observed **7.9%**). So the driver's `period = curr_ts − last_ts` is **not** the archive interval; period jitter vs transmitter-id mismatch is unresolved and needs the `ARCHIVE_STATS` line, which is `logdbg` and **not currently emitted** (marvin's live log greps zero, positive-controlled). *Incidental:* **per-minute variance is entirely binomial** — at ~20–25 packets/min and p≈0.73 the predicted sd is 9.2–9.9; measured **8.80** over 747 clean minutes. **No excess variance**: minute-to-minute reception is indistinguishable from constant-p Bernoulli, so the sd≈9 the campaigns fought was counting statistics on ~20 trials, never RF weather — which is also why single nights cannot resolve 2-pt effects. **(b) `ppm`/`fc` — BLOCKER 4 CLOSES on measurement, no campaign spent.** The standing prior (freqError centred) is **wrong**: 657 samples, mean **+2206 Hz**, median +2247, **zero negatives**, p05 +1406 — a systematic **+2.41 ppm** offset while the driver runs `-ppm 0`. But reception across freqError quintiles is **flat** (Q1 72.92 · Q2 72.54 · Q3 74.39 · Q4 73.27 · Q5 74.42; corr **+0.075**, sign slightly positive = noise) over a **10× offset range** — the AFC absorbs it completely, so setting `ppm` changes nothing measurable. Same result makes `-noafc` **contraindicated**, not merely untested. **The ceiling's signature:** constant ~25% loss, no excess variance, unresponsive to gain, receive window, siting **and** frequency offset — that is deterministic/structural, not marginal-SNR. **Owner's field observation, and the strongest input this investigation has had:** a real Davis console at comparable distance from the same ISS runs **single-digit packet drop** — the signal is there for the taking, so the ~20-pt gap is **ours, not the link's**. *Leading hypothesis, explicitly untested:* the US band is 26 MHz/51 channels and an RTL-SDR sees ~2.4 MHz, so unlike a console it must **retune per hop**; hops where the retune doesn't settle are lost deterministically regardless of signal strength, matching every measured property (25% ≈ 1/4 is suggestive, nothing here tests it). **Next step, bounded and cheap:** the deployed Go source is **publicly fetchable** (`Dockerfile:46` pulls `src.tgz`) — read its hop-tracking/retune path; no prod access, no campaign, and `CONSTANTS.md` already notes the deployed source has never been read directly | Accepted (measurement) · **follows** DEC-0128 · **closes blocker 4** · **sharpens blocker 6** | 2026-09-01 (S113) |
| DEC-0130 | **S113's debug window, harvested and reverted: transmitter ID 4 resolves the `max_count` jitter, and the miss histogram is clustered, not uniform.** S113 (2026-09-01, ~10:46–10:51 ET) raised `debug=1` + the `[[[user]]]` logger to `DEBUG` on marvin, two owner-authorized Class C edits with an explicit revert obligation; S114 harvested and reverted it the same session (`weewx.conf` restored from `.s113-debug-backup`, sha256-verified identical, `weewx.service` restarted 20:55:08 ET, post-restart log is INFO-only). **(a)** The window's startup line shows `actChan=[4]` — this ISS is **transmitter ID 4**, not the driver's default-assumed ID 0, so `loop_times[4]=2.8125 s` (confirmed independently: `Init channels: wait max 149 seconds` = 53 × 2.8125). `max_count` jitters in whole 2.8125 s quanta around the 60 s archive interval, landing on **19–23** — exactly DEC-0129's observed spread. **Blocker 7 closes: not a defect**, a transmitter-id fact. A representative `ARCHIVE_STATS` snapshot (20:32:14 ET): `max_count=20 count=17 missed=3 pctGood=85.00` — one interval, not a campaign statistic. **(b)** The final cumulative `missed per freq` histogram (51 channels, ID:4) is **not uniform**: 48 channels sit in a 37–93 band, but three adjacent-index channels (idx 46–48) run **128/182/149**, roughly double. Leans toward "specific frequencies/hop positions," not a blanket timing problem — one session's counts, not yet reproduced. Also logged **4 re-inits** in ~9h42m, each costing up to ~149 s of near-total loss. **Blocker 6 stays open**; the deployed Go source read (DEC-0129's next step) is unchanged, now sharpened to also check whether channels 46–48 are structurally distinct in the hop table | Accepted (measurement) · **resolves** DEC-0129's period-jitter-vs-transmitter-id question · **closes blocker 7** · **sharpens blocker 6** | 2026-09-01 (S114) |
| DEC-0131 | **Read the deployed Go source: the retune-doesn't-settle hypothesis is weakly supported by the code, and channels 46–48 are frequency-adjacent but hop-sequence-scattered.** Pulled the actual deployed source (`Dockerfile:46`'s tarball → `main.go`+`protocol.go`, the only files carrying demodulator logic) to test DEC-0129's leading hypothesis. **(a)** Retuning is real (`SetCenterFreq` per hop, in a separate goroutine so reads don't stall) but the code shows **no haste**: no PLL-lock wait exists, and every hop gets the ISS's ~2.5–3 s transmit slack **plus** a 300+ ms `receiveWindow` cushion — orders of magnitude bigger than typical R820T2 PLL lock time. Nothing on paper explains a rushed retune; if settling is the mechanism it's the tuner misbehaving, not driver impatience. **(b)** Mapped DEC-0130's elevated histogram indices (46/47/48) through the channel-frequency table: **925.500/926.002/926.503 MHz**, three back-to-back physical channels near (not at) the top of the US band — channels 49–50 (927.0/927.5 MHz) were unremarkable in the same data. These three are **scattered across the hop sequence** (visited at cycle positions 49, 5, 24 — not adjacent), ruling out a sequence-position/clock-drift artifact and pointing at something tied to the **frequency itself**: an interferer, an antenna/feedline null, or tuner gain rolloff near the band top — indistinguishable from source alone. **Blocker 6 stays open. Next step, bounded and cheap:** a spectrum capture (`gqrx`/`rtl_power`) over 924.5–927.5 MHz from the same antenna, no campaign, no owner-mediated step | Accepted (measurement) · **follows** DEC-0129 · **sharpens** DEC-0130 · **does NOT close blocker 6** | 2026-09-01 (S114) |
| DEC-0132 | **S114 spectrum capture: transient wideband bursts up to +34 dB, spanning 925.15–927.48 MHz — not confined to 925.5–926.5 MHz. RFI as mechanism strengthens; the exclusive tie to channels 46–48 weakens.** Ran DEC-0131's proposed capture (`rtl_power -f 924.5M:927.5M:10k -g 37.2 -i 10 -e 5m`) through `marvinctl exec-ro`, gain 37.2 dB matching prod's `-gain 372` (tenths-of-dB). First live exercise of ops#253's `exec_devices` grant beyond its `rtl_test` smoke test. `weewx.service` stopped first (own-tenant tier-2 self-service) and restarted right after — **outage ~19:34–19:40 ET (~6 min)**, inside the pre-registered bound, confirmed back up. **60 rows (30×10s integrations × 2 half-band segments)**, noise floor flat and stable all 5 min (-38.6 to -38.8 dBFS median, no drift, no rolloff shape) — that stability itself argues against a static gain-rolloff explanation. **16 of 60 rows (27%) carry a transient peak >5 dB above floor** (5.2–34.5 dB), frequencies spanning **925.15–927.48 MHz** — virtually the whole capture band, not one slice. A loose majority (7/16) fall in/near DEC-0130's flagged 925.5–926.5 MHz channels 46–48, but comparable-or-larger bursts also land at 925.15 MHz (three separate windows, +12–13 dB) and 927.15–927.48 MHz (the single largest event, +34.5 dB, 927.34 MHz peaking -3.07 dBFS — essentially full-scale). **Every spike is transient** — no frequency repeats across consecutive 10s windows — the signature of intermittent Part-15 ISM traffic sharing 902–928 MHz, not a fixed narrowband interferer or static null. **Strengthens** RFI over a static rolloff/null as the mechanism (bursty + wideband is RFI's signature); **weakens** the exclusive tie to 925.5–926.5 MHz specifically — if this traffic drove DEC-0130's miss cluster, 925.15 MHz and 927.15–927.48 MHz should show elevated misses too, checkable for free against the existing 51-channel histogram (not done this session). One 5-min capture, one evening, no concurrent reception data (weewx was down for the window) — cannot correlate burst timing against actual packet loss. **Blocker 6 stays open.** Raw CSV kept in session scratch, not committed (data artifact, not a documented interface) | Accepted (measurement) · **follows** DEC-0131's "next step" · **sharpens** DEC-0130/DEC-0131's channel-46–48 finding · **does NOT close blocker 6** | 2026-09-02 (S114) |
| DEC-0133 | **S115 cross-reference: with the receiver's ±134 kHz passband applied, S114's RFI explains channels 46–48 cleanly (a ~400 kHz-comb FHSS neighbour) — but that cluster is only ~2 pts of the ceiling; the bulk of the loss is a fixed wall-clock period (~7.75 s, or an alias) not locked to the hop clock.** In-passband exposure picks out exactly 46/47/48 (8/6/6 bursts; channels 45/49 have zero and are unremarkable), so DEC-0132's "weakens" was an artifact of comparing bare MHz. Excess misses at 46–48 total ~250 of 3,806 (6.6%, ~2 pts); the other 48 channels sit flat at 69.7 ± 10 — **the ceiling is frequency-independent.** The 3,047 miss timestamps carry a strong fixed-period structure: gaps peak at 3 hops, autocorrelation peaks at multiples of 2.755 hops undecayed to 60 hops, consecutive misses are anticorrelated, and a Rayleigh scan peaks at **T = 7.7495 s (z = 97)** vs z = 4 for the hop-locked 11/4-hop alternative; folding mod 11 hops is flat. Smooth modulation (miss probability ~0.17 → 0.5 over the cycle), phase stable to ~0.05% with slow wander. Isolated single misses carry 21.9 of the window's 32.2 pts; pairs 5.3; runs ≥10 (re-inits, blocker 2's class) 4.3. Host stalls ruled out (all cgroup pressure zero; loss identical to 0.01 pt across Foundation and marvin); in-band 924.5–927.5 RF ruled out; hop-locked mechanisms ruled out. Survivors: dongle/USB geometry, Go pipeline, antenna, ISS, outdoor path — a frequency wobble or a slow path modulation fits every flat axis measured. The received-packet half of the debug log rotated away unharvested. **Next: a designed ~25 min dongle-exclusive capture** — `rtl_test` at the Go geometry, standalone `rtldavis -v` for per-packet freqError, `rtl_power -i 1` over the full band. Owner confirmed at S115: the single-digit-loss console receives *this* ISS at *this* property — ISS and outdoor path exonerated, the periodic loss is in our receiver chain. | Accepted (analysis) · **follows** DEC-0132 · **explains and demotes** DEC-0130/0131's channel-46–48 finding · **reframes** blocker 6 · does NOT close it | 2026-09-02 (S115) |
| DEC-0134 | **Blocker 6 RESOLVED: the ~25% "loss" is the Go demodulator discarding legitimately received repeat packets as duplicates and booking each as a miss; real RF loss measured at 0.3%.** DEC-0133's designed capture ran (prod down 20:42–21:09 ET, 26.5 min, self-service): `rtl_test` at the Go geometry — zero lost samples; the deployed `rtldavis` standalone with `-v` for 15 min — 295 hops, 214 decoded, 81 missed (27.5%, prod's number with nothing but the binary in the loop) **and 89 `duplicate packet` lines**: 89/89 byte-identical to the previous decoded packet, 84/89 arriving exactly one ISS interval later on the next channel (the ISS re-sends the packet), and **80 of the 81 misses preceded by one of them within 0.6 s** — the loop `continue`s on a duplicate without hopping, so the pending timer fires 0.363 s later into the miss branch. The one unexplained miss is 1/295 = 0.3%. Explains every flat axis (gain, window, siting, offset, frequency, host — 72.83 vs 72.82 is the ISS's repeat fraction measured twice), the console's single-digit loss, the flat 51-channel histogram, the wall-clock (not hop-locked) period that moved between nights (ISS repeat cadence ≈ 7.75 s Sep 1, ≈ 8.34 s tonight), and DEC-0035's double-decode as the harmless within-burst sub-case (5 per 15 min). `rxCheckPercent` has under-reported a ~99% link as ~73% since the receiver was built. Full-band `rtl_power -i 1` (5 min) kept for the RFI picture: busy band, continuous carrier at 921.6 MHz, no periodic level anywhere. **Fix (S116, needs a Go rebuild):** time-gate the duplicate check (< 500 ms = double-decode; ≥ 1 s = new transmission, emit and hop) — ~3 lines in `main.go`; then the driver must tolerate identical consecutive packets, every ~73% baseline goes stale (dashboard via ops), DEC-0117's marvin-build question is on the critical path, and an upstream issue/PR is owed (draft only). Captures in local `ARCHIVE/s115-capture/` | Accepted (root cause, measured) · **closes blocker 6** · **supersedes** the DEC-0128→0133 mechanism search (measurements stand) · **explains** DEC-0035 · fix pending | 2026-09-02 (S115) |
| DEC-0135 | **The duplicate filter is time-gated, the repeat is suppressed one layer up, and the campaigns are not re-run: the fix unbiases the statistic, it does not improve reception.** DEC-0134 rested on payload identity and timing; one alternative would have inverted it — a stale buffer **replaying** a decode rather than a fresh reception, in which case the miss booking was honest. Settled free from the existing capture: every decode carries its own correlation magnitude `m1` and 16-symbol vector `x`, and **80 of 80** long-gap duplicates have distinct ones — fresh samples, different channel, after a retune. Supporting: the two populations median **2.1 ms** vs **2.8117 s** (= `idLoopPeriods[4]`) with **nothing between 0.05 s and 2.5 s**, a 1300× gap; and **89/89** consecutive same-type pairs are byte-identical while no same-type pair is ever not — a deliberate re-send, not coincidence. **Go fix** (`patch/rtldavis-dupgate.patch`, 5 hunks): `lastRecTime` beside `lastRecMsg`, drop gated on `curTime - lastRecTime < dupWindow`, survivors take the normal counted/logged/hopped path and log `repeat packet:`; `lastRecTime` advances **only on acceptance** so a chain of re-decodes cannot ratchet the window forward. Default **500 ms** (`-dupwindow`): ~240× above the re-decode cluster, below the shortest loop period (2.5625 s, id 0), so no real transmission can be swallowed on any id. **Python fix:** `self._last_pkt` **has been dead code since it was written** — `data` carries `curr_cnt0..3`, cumulative counters that advance every packet, so the comparison was unconditionally true; hidden because Go was dropping repeats itself. Extracted as pure `dedup_key()` excluding them (metric untouched — `_update_stats()` consumes them first), the latent `else: if packet:` **NameError** fixed in the same edit, `repeat_count` mirroring `dup_count` so suppression is measured not silent. **Suppress, not emit** (owner's call): the payload is byte-identical, so forwarding it would add ~37% loop packets/InfluxDB points/loop-JSON writes for zero information. **Ships as a tracked patch applied in the Dockerfile** — chosen over forking upstream or vendoring 3 MB into a WeeWX extension; smallest reviewable public diff, the upstream PR verbatim, and it **fails loud** if `weewx-contrib`'s **unpinned `refs/heads/main`** `src.tgz` shifts (`--batch --forward` + a `grep -c dupwindow` assertion). **Build-host question ANSWERED: `marvinctl build` is a tier-2 own-resource verb** — self-service, no NAS, no `docker save`/`load`. **What it does NOT do:** improve reception. Data was always correct; the one candidate benefit (`maxmissed` re-inits) is not real — `chAlarmCnts` reached **max 2 against a threshold of 51** in the S115 window; re-inits are blocker 2, untouched. **Campaigns not re-run, but DEC-0134's 'negative results stay valid' is TOO STRONG** — a flat result from an insensitive instrument is not evidence of flatness (Campaign B's own scatter, sd 8.47/4.67, exceeded the real signal), so A–D are **demoted to *untested***. Still not worth re-running: **~6 pts total headroom** (0.3% + ~2 pts RFI + ~4 pts RF-dead) vs DEC-0059's **2.0-pt bar**, both mechanisms identified and **not gain-responsive**. **Re-baseline by observation instead** — free, no apparatus; the real prize is that **blocker 2 becomes measurable for the first time** against a flat ~99% baseline. Validation is pre-registered and needs no deploy: `duplicate` 89→~9, `repeat` ~80 appears, `missed` 81→~1, decoded 214→~294. **Consequence recorded:** `rxCheckPercent` steps ~73%→~99% mid-archive — a metric-definition change, logged as a dated discontinuity in `docs/DATA_ERRATA.md` so it is not later re-read as a real event. Gates: ruff clean · 466 passed/17 skipped (+9) · mypy 67 files (+1) · secret gate 0 with a firing positive control; the Go patch **compiles** (real cgo build, positive-controlled) and the 9 tests are **mutation-tested 5/5**. *Two repo lessons bit again and shaped this:* the first test draft asserted against a **copy** of the dedup expression (structurally blind, DEC-0035/0071) — hence the imported pure function; and `check_secrets.sh | tail` reported **exit 0 while the script exited 1**, the documented false-zero, met while running the secret gate itself | Accepted (design + implementation; deploy pending) · **implements** DEC-0134's pending fix · **hardens** its root cause · **narrows** DEC-0035 to its real population · **demotes** campaigns A–D to untested · **answers** DEC-0078/DEC-0117's build-host question | 2026-09-02 (S116) |
| DEC-0136 | **DEC-0135 deployed and confirmed in production: `missed` 81 → 0, and three reception denominators turn out to measure three different things.** `v2.0.15` built on marvin from `origin/dev`@`2fa80a4`, prod cut over **2026-09-03 07:17:53 EDT**, outage 16m09s, gain unchanged at 372; `REMEDY_MODE` flipped `none` → `restart_unit` at 07:58:59 in a second receiver-free step. **Validation met every pre-registered number**: `missed` 81→**0**, `repeat` 0→**79**, `duplicate` 89→**6**, accepted 214→**274** over a like-for-like 15:00 capture with `dupWindow=500` in the banner. **Confirmed on production data**, which DEC-0135 could not do — the prod instrument is the driver's INFO frame counters, not `ARCHIVE_STATS` (DEBUG-level, absent at `debug_rtld=1`): duplicate frames/period **6.23 → 0.57** while repeat frames appear at **5.81**, population conserved (6.38 vs 6.23), so **91% of what was called a duplicate and discarded was a real re-send**, and 5.81/21.33 slots = **27.2% of transmissions** against DEC-0134's ~27%. **What `276` counts, read from the upstream Go source not inferred:** a hop fires only on an accepted packet or a loopTimer expiry, plus init, so **hops = accepted + missed + init** — reconciling both captures exactly (274+0+2; 214+81+2). Three instruments answer three different questions and are not interchangeable: `rxCheckPercent` over hops, slot arithmetic over wall-clock ÷ 2.8125 s, and the monitor's `WINDOW` over `WU_RF_EXPECTED=21`. **Two corrections to DEC-0135's own reading:** (a) the apparent 85.6% slot shortfall was **cold-start acquisition** — 273 gaps, median 2.800 s, max 2.900 s, *zero* above 4 s, with `Init channels` + `TRANSMITTER 4 SEEN` showing **128 s** before the first packet, so the steady-state window is 767.8 s and 274 accepted against 273.0 expected is **~100%**; (b) a repeat **falls through to the normal path** and emits a `msg.ID=` line, so the 274 "decoded" already include the 79 repeats — **unique payloads are 195**. **Reverses a standing assumption:** the monitor's ~73% thresholds do **not** go stale — measured 15.38/21 (73.2%) before vs 15.86/21 (75.5%) after, where a real +28% jump would read ~19.7 — because `len(set(epochs))` already counts hop packets and saturates (DEC-0024's mechanism from a new direction). `WU_RF_MIN_PCT=60` stays valid; only `rxCheckPercent` consumers need re-keying. **Four self-service gaps found by doing:** no tree transport (no checkout, no `git_branch` — this release rode a one-off owner-authorized `scp -r` of a `git archive` export, sha256-verified both ends), no image-tag control (a literal in a `0644 root:root` `ExecStart`; an `EnvironmentFile` with `IMAGE=` would fix it but hands a tenant control over what root launches, so marvin records that reading first), no config write, no ad-hoc archive read. **Stale campaign residue:** `campaign.inhibit` outlived its campaign by 2.5 days and, checked at `:705` *before* the `REMEDY_MODE` check at `:712`, would have made the flip a **silent no-op**; its documented lifecycle at `ops/weewx-monitor.service:84` **does not exist in any code**. `weewx-rx-experiment.timer` likewise still armed. **Verification note:** DEC-0078's `BUILD-EXIT` marker belongs to `ops/nas_build.py` and does not exist on the `marvinctl build` path, so the artifact was proven directly instead — `exec-ro` running `rtldavis -h`, at **zero outage**, printing `-dupwindow … (default 500)`. Generalizes: when the sanctioned proof does not cover the path taken, prove the artifact, not the pipeline | Accepted (deployment + measurement) · **deploys** DEC-0135 · **confirms** DEC-0134 on production data · **corrects** DEC-0135's reading of its own counters · **corrects** DISC-0001's consumer list · **arms** DEC-0074's remedy path | 2026-09-03 (S117) |
| DEC-0137 | **#317: `rxCheckPercent` denominates by ISS slots between received packets, not `floor(wall-clock period / loop period)`; >100% becomes structurally impossible, not just clamped.** DEC-0136 named the mechanism: a 60 s archive period floors 21.33 real transmissions to 21, a 59 s period (the archive event's own jitter) floors to 20, so a fully received minute read 101–105% even after PR #315's clamp — the clamp masked the over-count, it didn't stop it being computed. The 2026-09-03 12:00–18:00 email measured the scale directly: **197 of 360 records (55%) read over 100%** pre-fix. Fix: `_update_stats` now records each transmitter's last accepted-packet arrival time; `_update_summaries` denominates by `round((last − prev) / loop_time)` — the ISS's own clock (S115: 2.8124 s mean, 1.0 ms sd) — instead of the archive engine's period, so `count[i] <= max_count[i]` holds by construction. Counter resets clear both timestamps so a discontinuity can't manufacture a spurious baseline. `tests/test_slot_count_denominator.py` (new): the four synthetic cases from #317, the reset guard, a 500-period randomized invariant sweep; `test_reception_stats.py` + `test_issue_225_qc_fixes.py` reseeded. Squash-merged PR #319 → `dev`@`e158741`. Gate: ruff clean · 475 passed/17 skipped (+6) · mypy clean, 68 files · secret gate clean. **Deploy is the remaining half, not a formality** — a `dev` merge is a silent no-op in prod (`CONSTANTS.md` deploy layers); #317 stays open until `v2.0.16` ships and is verified live, routing through the same one-off owner-authorized tree transport DEC-0136 used (`ops#257` — no self-service `git_branch`/checkout on the tenant yet) | Accepted (implementation; deploy pending) · **root-causes** #313, superseding PR #315's clamp (tracker-recorded, no DEC) with a fix at the source · **implements** the design `BOOT.md`'s S120 job list wrote into #317 | 2026-09-03 (S120) |
| DEC-0138 | **DEC-0137 built and deployed as `v2.0.16`: 31 s cutover, artifact verified directly before touching prod, statistical proof still pending the next monitor cycle.** Same transport shape as DEC-0136 (ops#257 still open): local `git archive` of `dev`@`73acc3d` → owner-authorized `scp` to marvin (sha256 `1ca2e768…` verified both ends, Class C) → owner `sudo tar` extraction into `/srv/docker/weewx/build-v2.0.16/` (a **second**, distinct Class C wall found by hitting a plain `Permission denied` after the transfer's own token was already spent — the owner's `marvin-admin` account has no write access to the `t-weewx`-owned tenant root). `marvinctl build ... -t weatheredscientist/weewx-rtldavis:v2.0.16` then ran self-service, no gate. **Verified directly, not from the pipeline's exit** (`docs/GOTCHAS.md` §1): `exec-ro … grep -n last_pkt_ts` confirmed the slot-count logic was actually baked in before the unit file was touched. Cutover: owner root-edited the `ExecStart` image literal (ops#257 limb 2, `EnvironmentFile IMAGE=`, still open) + `daemon-reload` + `restart`; journal shows **31 s** stop-to-started, a same-host recreate — nowhere near DEC-0136's 16m09s host-move wait. Container sha matches the build; driver banner unchanged (`0.20+ws.5`); `weewxd` published its first records within seconds; no `CRITICAL` since restart, the one `ERROR` (WOW 429) matches a pre-existing rate-limit pattern. **Deploy-verified is not metric-verified** — #317 stays open per `BOOT.md`'s own sequencing until the 18:00/00:00 ET monitor email shows the 55%-over-100% pre-fix baseline reading ~0%, continuing the DEC-0135→DEC-0136 two-step pattern | Accepted (deployment) · **deploys** DEC-0137 · **advances** #317 to deploy-verified, does not close it | 2026-09-03 (S121) |
| DEC-0139 | **DEC-0138 confirmed by the metric: the first fully-post-cutover monitor window reads 0 records over 100%, down from 197/360 (55%); #317 closed.** Read the 6-hourly `RECEPTION SUMMARY (rxCheckPercent)` log across the three windows spanning the `v2.0.16` cutover (20:29:06 EDT): 12:00–18:00 (fully pre-fix) 197/360 (55%) over 100%, matching `BOOT.md`'s S121 figure exactly; 18:00–00:00 (spans the cutover) 86/358, already dropping; 00:00–06:00 (fully post-fix, the first clean window) **0 of 360 — no "Records reading over 100%" line at all**, every hour exactly 100%, mean reception 100%, `dropped (est, lower bound)` 4 of 7,680 transmitted — a real small loss, not a divisor artifact. `round((last − prev) / loop_time)` makes `count[i] <= max_count[i]` hold **by construction**, so one clean window is sufficient — the fix is structural, not statistical — and the 197→86→0 step-shaped progression across the cutover is itself corroborating. `docs/DATA_ERRATA.md`'s `DISC-0001` gets a second boundary (same treatment as DEC-0135's ~73%→~99% step); #317 closed with this measurement. Completes the DEC-0135→0136→0137→0138→0139 verification chain | Accepted (measurement) · **confirms** DEC-0137/DEC-0138 on production data · **closes** #317 | 2026-09-04 (S122) |
| DEC-0140 | **`campaign_analyze.py`'s `rx > 100` backstop stays exactly as it is — not raised, not removed (#314 closed as overtaken by #317).** Filed under v2.0.15 (2026-09-03 12:41 ET), when DEC-0135 had unmasked the driver's floored denominator and a fully received minute read 101–105%, so the backstop excluded most good minutes. Eight hours later #317 (DEC-0137, `v2.0.16`) replaced that denominator with `round((last_pkt_ts − prev_pkt_ts) / loop_time)`, which scales with real elapsed time — so post-cutover `rx <= 100` holds by construction (0/360 over, DEC-0139) **and** an absorbed multi-minute record reads ~100 rather than 200. The rule is therefore correct on every campaign A–D row (pre-v2.0.15, ~73%, never >100 except the genuine 2026-07-29 03:10 absorption record), dormant on everything post-v2.0.16, and over-excludes only in v2.0.15's ~13 h — a span no campaign ran in. Three options weighed on Fable: (A) docstring-only, (B) raise to 150 + clamp, (C) drop the rule. A chosen: B is future-proofing against a regression nothing predicts and its clamp half is a no-op today; C would re-admit the 2026-07-29 record to any Campaign A re-read unless `gap_adjacent()` independently catches it (likely, unproven). The Sonnet-tier first pass recommended B — it had not checked whether #317 already changed the ceiling; the escalation is what caught it | Accepted (decision, no code change) · **closes** #314 · **depends on** DEC-0137/DEC-0139 | 2026-09-04 (S123) |
| DEC-0141 | **InfluxDB leaves Foundation for marvin as a stopped-server raw tree copy into a weewx-owned `weewx-influxdb.service`; consumers change one URL each.** ops#260 step 3 under OPS-DEC-0188 — HLF and EHWD cut over 2026-09-04, InfluxDB is the last weather workload on Foundation and its last continuous write, and ops posted the owner's ask for weewx's plan. Measured, not recalled: `influxdb:2.7` = engine **v2.7.12**, **16.4 MB across 64 shards** (`/metrics`), 8086 host-published (the compose comment saying otherwise is stale), data dir `0700 uid 1000` (so `nasctl ls` returns a permission **false-zero**, GOTCHAS §1); all three consumers — weewx `influx.py` (`weewx.conf` **and** `weewx.conf.rx-baseline`), eh-proxy `proxy.env`, dashboard `event-detect.env` — already on marvin by Foundation's IP; HLF none. **Design:** (1) raw copy of `data/`+`config/`, not `backup`/`restore` — same engine, so org, buckets, every token, the `eh-daily-rollup` Task and the CLI operator config travel byte-for-byte and each consumer changes exactly the URL; (2) cutover copy from a **stopped** server — at 16 MB the outage is gesture time, and the live pre-copy's only value is a dark-parallel test, which stage 1 runs and wipes; (3) container + unit **`weewx-influxdb`** under `/srv/docker/weewx/influxdb`, `weather.slice` — inside the weewx manifest globs, so self-service lifecycle with no manifest change; own tenant rejected; (4) `influxdb:2.7.12` pinned, `--pull=never` (dashboard's Flux Task is verified against this engine); (5) `--user 996:986` = `t-weewx`, tree chowned — uid 1000 on marvin is a human account; the one deliberate divergence, host-mapping only; (6) no `INIT_*` secrets in the unit; (7) `-p 8086:8086` like-for-like, tightening later; (8) Foundation stopped with `kill -s TERM` (DEC-0008), `--restart=no`, **retained** until ops#260 step 4; (9) archive gap backfilled from SQLite with the DEC-0119-fixed `backfill_influx.py` (marvin's copy is the old one); (10) nothing rides along. Consequences: order of operations is load-bearing (pause event-detect → kill → copy → start → repoint weewx both files + restart → repoint dashboard → resume → backfill); the store gets its **first backup ever** (restic + a stage-3 `influx backup` timer); the NAS-LEASE courtesy yield becomes **moot** (zero NAS I/O left — `BACKLOG` item closes on ship); `weewx.conf.rx-baseline` must be edited too or `restore_baseline` reverts the URL. Runbook `docs/INFLUXDB-MIGRATION.md`, unit `ops/weewx-influxdb.service`. Nothing executed this session. | Accepted (design) · implements ops#260 step 3 · depends on DEC-0100, DEC-0008, DEC-0119, MARVIN-DEC-0110/0111 | 2026-09-04 (S124) |
| DEC-0142 | **DEC-0141 executed the same night: InfluxDB serves from marvin since 2026-09-04 22:35:02 ET, Foundation hosts no weather workload, and a 29-record gap (22:14–22:42) is the one open item.** "Daytime" in the runbook was a default, not a requirement; the owner said go at ~21:50 ET and the move ran 22:08–22:43 with the owner attending. **Deviations, each recorded in `docs/INFLUXDB-MIGRATION.md` §8:** (1) copy route was `docker cp <ctr>:<path> -` streamed to tar files on the marvin-data share — `nas-admin` has no non-interactive sudo and the daemon reads the `0700 uid 1000` tree as root; works on the stopped container too; two Class C mints total; (2) equality test = the two user buckets' shard counts + bytes (`weewx` 16 / 16.87 MB, `eh_rollup` 16 / 212,744 B), not the raw 64 — `_monitoring`/`_tasks` drop expired empty shards on first start; (3) marvin's stage-0 installer swept in `weewx.service`'s `:marvin-live` re-pin and marvin tagged `v2.0.16` → `:marvin-live` the same turn (MARVIN-DEC-0121/0122) — weewx's restart ran from that alias, banner `0.20+ws.5` confirmed; ops#257 limb 2's tag step is done by that hand; (4) `weewx.conf.rx-baseline` lives at the tenant ROOT — the runbook's path was wrong, the four-file `sed` exited 2 on it and the `&&`-chained verification grep never ran; `weewx.conf` had in fact flipped (redacted read), `.rx-baseline` re-issued alone; (5) influxd exits 2 on SIGTERM on both hosts — `SuccessExitStatus=2` added to the unit; (6) backfill deferred to S125 (needs the write token in a root shell; SQLite has every record). Verified: 22:40:43 `Data will be uploaded to http://<MARVIN_IP>:8086`, 22:43:16 first `Influx: Published record`, Foundation `:8086` refuses, container stopped `--restart=no` and retained. The 16 MB size estimate was right about the outage shape and 9× low on bytes (152 MB tar — bolt/sqlite/WAL are outside `storage_shard_disk_size`). | Accepted (executed) · executes DEC-0141 · ops#270 stage 2 done, stage 3 + step 9 open | 2026-09-04 (S124) |
| DEC-0143 | **Closeout skeleton gets a step 0: the `BOOT.md` pointer, `CHANGELOG.md` line, and DEC row ride the merge/promotion PR itself, never a deferred post-merge pass** (OPS-DEC-0195, ops#218 — adopted, not declined). ops's ops#218 catalogued the failure shape across three repos' sessions: EHWD S261 shipped and merged PR #570, then idled post-merge and dropped out of a peer session's `ListAgents` visibility (an auto-archive-on-PR-merge race) before its own closeout ritual ran — a second EHWD session had to notice the stale `BOOT.md` pointer and wait for a manual repair. **weewx hit the identical shape twice already, just not fatally**: S120 (DEC-0137, PR #319 merged) and S122 (v2.0.16 promoted to prod) both let the closeout ritual sequence *after* the merge/promotion instead of inside it, and both were only repaired because S121 and S123 happened to run next and caught the debt — not because anything prevented the gap. **The fix inverts the order, not the content**: nothing in DEC-0052's 8-step skeleton changes: what changes is *when* steps 2–4 (pointer, CHANGELOG, DEC row) execute relative to a merge or a prod promotion — before or riding the same PR, never queued for "the closeout pass at the end," because a session doing genuine post-merge work (deploy, verification) can be auto-archived out from under itself before that pass runs. **Also folds in the ritual's own idle-wait pattern**: a session that has already verified its own work does not block on a live recheck window (a 6-hourly monitor email, a ping) to confirm a foregone conclusion — it writes the pending re-verify into `BOOT.md`'s job list as a named next-session action and closes clean, exactly the shape DEC-0138→DEC-0139 already used for the `v2.0.16` cutover (deploy-verified now, metric-confirmed by the next session, never blocked open). No code changes; this amends `CLAUDE.md`'s session ritual only (step 0, before DEC-0052's step 1) | Accepted (process, no code) · **adopts** OPS-DEC-0195 (eaglehunt-ops `CLOSEOUT-TEMPLATE.md`) · **closes** weewx's half of ops#218 (HLF answers separately) · **amends** DEC-0052's closeout skeleton · **generalizes** the pattern DEC-0138/DEC-0139 already used once | 2026-09-05 (S125) |
