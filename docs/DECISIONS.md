# Decision Log (INDEX) — weewx-rtldavis

**Status:** Source of truth (index) · **Tier 1 session read** (DEC-0030)
**Last updated:** 2026-08-14 (S82 — DEC-0090; full bodies in DECISIONS-FULL.md)

One row per decision — scan this at session start; **do not re-litigate** anything listed. The
complete append-only bodies live in `docs/DECISIONS-FULL.md`; grep the DEC id there whenever a task
touches a settled decision's actual text (*"working near it" means read it*). New decision = append
the full body there + add a row here. A decision is never edited in place — it is **superseded** by
a later entry.

> **Provenance note:** entries DEC-0001…DEC-0009 are *reconstructed* from project history (the
> pre-governance chat sessions, roughly Apr–Jun 2026); their dates are approximate. DEC-0010 and
> up were made live under governance (S16 onward) and are exact. This log covers the
> **weewx-rtldavis driver/Docker** only; dashboard decisions belong to `eaglehunt-weather-dashboard`'s
> own log. **DEC-0018–0020 were never assigned** (numbering gap, no bodies exist).

## Index

| ID | Title | Status | Date |
|----|-------|--------|------|
| DEC-0001 | Passive 915 MHz interception via RTL-SDR | Accepted | ~2026-04 (S1) |
| DEC-0002 | Ubuntu 26.04 / Python 3.14 multistage Docker build | Accepted | ~2026-05 |
| DEC-0003 | Same-repo tags for versioning (Option 2) | Accepted | ~2026-05-26 |
| DEC-0004 | Volume-mount extensions for hot iteration; bake stable code | Accepted · **driver half superseded** by DEC-0031 | ~2026-06 |
| DEC-0005 | Custom `loop_json_writer.py`; weewx-loopdata rejected | Accepted | ~2026-06 |
| DEC-0006 | Null-on-rejection filter philosophy (**runtime only** — see DEC-0032) | Accepted · **clarified** by DEC-0032 | ~2026-06 |
| DEC-0007 | Upload services use the WeeWX RESTThread pattern | Accepted | ~2026-05 |
| DEC-0008 | `docker kill`, never `docker stop` | Accepted | ~2026-05 |
| DEC-0009 | Dedicated limited user for the NAS-side monitor | Accepted | ~2026-06 |
| DEC-0010 | Adopt Eagle Hunt nine-file governance model | Accepted | 2026-07-04 (S16/S17) |
| DEC-0011 | Branch model: `main` = production truth, `dev` = work | Accepted | 2026-07-04 (S16) |
| DEC-0012 | Public repo: structural secret hygiene | Accepted | 2026-07-04 (S16) |
| DEC-0013 | Session numbering continues the shared lineage at S16 | **Superseded** by DEC-0023 | 2026-07-04 (S16) |
| DEC-0014 | No-Rewrite Rule | Accepted | 2026-07-04 (S16) |
| DEC-0015 | Graft Python tooling from hyperlocal-forecast | Accepted | 2026-07-04 (S17) |
| DEC-0016 | Claude Opus 4.8 at high/xhigh as the Claude Code driver | Accepted | 2026-07-04 (S16) |
| DEC-0017 | Gain held at 372 pending an averaged re-test | Accepted (interim) · open, but **absorbed into DEC-0048's designed RX experiment** | 2026-07-04 (S16) |
| DEC-0021 | Rain-counter glitch filter (the false-rain fix) | Accepted · released v2.0.3 | 2026-07-04 (S18) |
| DEC-0022 | Sensor-QC hardening deferred to a dedicated pass | **Resolved** by DEC-0029 (S33) | 2026-07-04 (S18) |
| DEC-0023 | Independent per-repo session counter | Accepted · **supersedes** DEC-0013 | 2026-07-04 (S20) |
| DEC-0024 | RF-reception metric ~150%: freqError channel packets published as loop packets | **Fully resolved** — Layer A (S22/S31) + **Layer B shipped (S43)**: freqError merged onto the next real DATA packet instead of yielded standalone | 2026-07-04 (S21; upd. S22/S31/S43) |
| DEC-0025 | Known-bad data: preserve-and-flag, never delete | Accepted | 2026-07-05 (S29) |
| DEC-0026 | v2.0.3 confidence gate waived: cut on tests + live evidence | Accepted | 2026-07-05 (S29) |
| DEC-0027 | Lint scope: enforce `ruff check`, not `ruff format`; exclude vendored uploaders | Decided · local pre-commit **brought back in sync** (S43 — `ruff-format` had silently contradicted this since S31) | 2026-07-08 (S31; upd. S43) |
| DEC-0028 | Leaked credential in pushed public history: rotate immediately, don't rewrite | Decided | 2026-07-08 (S32) |
| DEC-0029 | Decode-layer sensor plausibility filter (temp/humidity/wind/UV/radiation) | Accepted · **resolves** DEC-0022 · cause **confirmed** by DEC-0033 | 2026-07-08 (S33) |
| DEC-0030 | Docs diet: tiered session read, DEC index+full split, CHANGELOG roll, STATUS prune | Accepted | 2026-07-09 (S35) |
| DEC-0031 | The driver is BAKED into the image, never bind-mounted (supersedes DEC-0004's driver half) | Accepted | 2026-07-12 (S36) |
| DEC-0032 | Retrospective correction: correct to the KNOWN value, flag it in-band (`rain_qc`) | Accepted · **clarifies** DEC-0006 | 2026-07-12 (S36) |
| DEC-0033 | Glitches are CRC-valid multi-bit corruption from spurious duplicate frames (upstream #15) | Accepted · **confirms** DEC-0029 · **confirmed locally** by DEC-0035 | 2026-07-12 (S36) |
| DEC-0034 | State the fork honestly: modification notices, `+ws` version, CHANGES-FROM-UPSTREAM | Accepted | 2026-07-12 (S37) |
| DEC-0035 | Duplicate-frame mechanism CONFIRMED here (~722/day); the test that said otherwise was broken | Accepted · **confirms** DEC-0033 · **permanent counter shipped (S43)** | 2026-07-12 (S37; upd. S43) |
| DEC-0036 | The 7h18m freeze: trigger known (bare `docker logs` wedged the daemon), **mechanism OPEN**; mitigations banked | Accepted · **mechanism open** | 2026-07-13 (S37) |
| DEC-0037 | A retrospective correction must propagate to every derived field | Accepted · **extends** DEC-0032 | 2026-07-13 (S37) |
| DEC-0038 | An image tag denotes exactly one tree: publish **v2.0.5**, don't rebuild "v2.0.4" | Accepted · **extends** DEC-0031/0034 · *prod deliberately one patch behind* | 2026-07-13 (S38) |
| DEC-0039 | Secret gate: every allow term **anchored or positioned**; a gate ships with its planted-payload test | Accepted · **extends** DEC-0012 · adopts dash DEC-0063/0100 | 2026-07-13 (S38) |
| DEC-0040 | The cross-repo gap is an **enforcement** gap, not a documentation gap — **no master repo** | Accepted · owner-confirmed (S38) | 2026-07-13 (S38) |
| DEC-0041 | **StdPrint removed** — the v2.0.5 console-handler fix was necessary but NOT sufficient | Accepted · **completes** DEC-0036 · corrects DEC-0038 | 2026-07-13 (S38) |
| DEC-0042 | The phantom **rainRate is ISS-side** (condensation trips the reed switch, never tips the bucket) — not RF, not the driver | Accepted · **bounds** DEC-0033/0035 · closes the rainRate thread | 2026-07-13 (S38) |
| DEC-0043 | Override the **root** logger, not just `weewx`/`user` — weewx's default root points at a syslog socket a container does not have | Accepted · **completes** DEC-0036/0041 | 2026-07-13 (S39) |
| DEC-0044 | The **nibble theory is not supported** by the archive, and the archive can never settle it — **instrument, don't filter** | Accepted · **bounds** DEC-0029 · **parks** the coupling filter | 2026-07-13 (S39) |
| DEC-0045 | **A comment is not an exemption** — the secret gate scans comments like code. A commented-out credential in a public repo is still leaked, and the gate's *own test* had asserted it was fine | Accepted · **amends** DEC-0039 · **extends** DEC-0012 | 2026-07-13 (S40) |
| DEC-0046 | **The baked config is shadowed by the prod bind-mount** — an image-only config fix reaches downstream users and *never* reaches prod. The exact mirror of DEC-0031 (where the *image* wins and the mount is the no-op) | Accepted · **mirrors** DEC-0031 · completes the delivery half of DEC-0043 | 2026-07-13 (S41) |
| DEC-0047 | **The secret gate guards commits, not reads — the transcript is an egress path.** A `sed -n '…,+44p'` overran its section and dumped credentials into the transcript; no control existed because nobody had modeled reading as a path. Guard + redacting reader + transcript scanner, all in `~/.claude/`. *(Rotation specifics: gitignored local-infra doc.)* | Accepted · **extends** DEC-0012 · completes DEC-0039/0045 (write path only) · applies DEC-0040 | 2026-07-13 (S41) |
| DEC-0048 | **Reception testing is a designed experiment, not a pile of image tags.** The ad-hoc `rw*-test` images are retired; a proper RX test (hypothesis, control arm, averaged window) is deferred and will settle DEC-0017's gain question in the same run | Accepted · **absorbs** DEC-0017's pending sweep · applies DEC-0038 | 2026-07-13 (S41) |
| DEC-0049 | **The ISS hardware is new and inspected — the phantom rainRate is not a broken part.** DEC-0042's "inspect the bucket + reed switch" action is CLOSED and came back clean, which excludes a defective part and sharpens DEC-0042: it is working hardware reacting to condensation. Anemometer replaced ~16–17 Jun 2026 | Accepted · **bounds** DEC-0042 | 2026-07-13 (S41) |
| DEC-0050 | **The station gets a master for its IDENTITY (and only that): `eaglehunt-ops`** — DEC-0040's revisit triggers fired (5 shared executables versioned nowhere; the gate fix re-derived 4×; dash DEC-0106 = the predicted casualty). Private coordination repo per the S38 §Etiquette litmus test: canonical `station-identity.env` + drift check (first run 8/9 within 19 m, the 9th a real HLF finding), NAS runtime contract, the `~/.claude/` guards versioned with tests, issues as the cross-repo inbox. NOT a master repo; deletion clause. Also: `soak_check.sh` identifiers scrubbed to fail-fast placeholders (they were live on public `dev`; CI is structurally identifier-blind — `.identifiers` is gitignored), and **pre-commit was configured but never installed in ANY of the three clones** — now actually installed | Accepted · **executes** DEC-0040's revisit clause · owner-approved (cross-repo round, dash S74) | 2026-07-14 (S42) |
| DEC-0051 | Cold-load Fix B ships (`current.json` alongside `loop-data.txt`); `windchill` added — closes issue #44 | Accepted | 2026-07-15 (S43) |
| DEC-0052 | Adopt eaglehunt-ops' shared closeout skeleton, adapted — docs-diet + DEC log kept as-is, model-tier restore check added as step 5 | Accepted · **adapts** OPS-DEC-0016 · closes weewx#56 | 2026-07-19 (S44) |
| DEC-0053 | **Provenance audit:** loop-JSON cache bounded per-field (was unbounded — a dead sensor looked live); InfluxDB station identity + SQLite correction state documented as deliberate gaps, not closed | Accepted · **applies** DEC-0006 to the real-time surface · closes weewx#45 | 2026-07-25 (S48) |
| DEC-0054 | **Frame-level co-rejection:** a SensorQC *bounds* failure (positive corruption proof) nulls every weather field of the same frame and skips the rain-counter without resync; a *delta* trip never triggers it. Zero free parameters — NOT the parked DEC-0044 coupling filter. Motivated by ERR-0004 (phantom 39 mph gust rode a frame whose own humidity decoded to 144.9%) | Accepted · **extends** DEC-0029 · ships v2.0.9 · closes #76 | 2026-07-27 (S52) |
| DEC-0055 | **The outside-temperature field is SIGNED** — decoded unsigned, a −5 °F reading became 404.6 °F, tripped the −40…65 °C bounds and (post-v2.0.9) co-rejected the whole frame, so ordinary winter would have saturated the corruption alarm. Ships true two's complement (`− 0x1000`), **not** meteostick's one's-complement `^ 0xFFF` (0.1 °F warm; maps `0xFFF` and `0x000` both to 0.0 °F) · adopts meteostick's missing `0xFF8` sentinel | Accepted · **fixes** ops#105 R1 · **bounded by** DEC-0054 · **released v2.0.10, live in prod (S55)** | 2026-07-28 (S54) |
| DEC-0056 | **`MAX_PLAUSIBLE_TIPS` 60 → 16 (ops#105 R2)** — evidence-bounded (70-day archive: worst real minute 7 tips, worst 3-min window exactly 16, gaps during rain never > 60 s, filter idle at 60; StdQC already capped the system at 30, so the newly exposed band is 17–30, never occupied). Ships as a package: cap + reframed rejection email as tripwire + WeatherLink recovery playbook + predefined revisit trigger (any rejection on a wet day). Confirm-on-reject held as the designed escalation | Accepted · **amends** DEC-0021 · rides `dev` until the next image cut | 2026-07-28 (S55) |
| DEC-0057 | **ROADMAP.md joins the closeout ritual** — a same-session update whenever a DEC ships/closes/reprioritizes a ROADMAP line (closeout step 5), plus a next-check-due tripwire inside ROADMAP.md itself, after it was found 20 sessions / 8 releases stale (S35 → S56) | Accepted · **extends** DEC-0052 | 2026-07-28 (S56) |
| DEC-0059 | *(**AMENDED S58**: its "no detectable diurnal cycle" is true at 6 h resolution, **false at 1 h** — a reproducible ~2-pt notch at hours 07 and 19 was averaged away. Campaign validity unaffected; the Latin square balances it. See BACKLOG §Durable RF findings.)* **The RX experiment gets an apparatus** (`ops/rx_experiment.sh` + tests). Key finding: **`-ex N` ≡ `receiveWindow 300+N`** (upstream sums them), so the window axis is mounted-config-cheap and **no arm needs an image rebuild** — the `rw*` images were redundant, not just misnamed. Measured baseline replaces the stale "~67–70%": **73.3%, sd 4.67**, zero autocorrelation, no diurnal cycle → 24h/arm resolves 1.1 pts, so DEC-0017's "1–2 weeks" was ~7× overkill. Design: 2×2 factorial (gain × ex) as a Latin square, 6h blocks, run twice — LNA in, then LNA physically removed (blocked, not bracketed — owner's call; drift measured from campaign A instead). Adopt only on ≥2 pts with no duplicate-frame regression. All 7 pre-governance sweep scripts deleted (two were silently broken) | Accepted · **executes** DEC-0048 · **amends** its "same run" clause · **absorbs** DEC-0017 · **supersedes** DEC-0008's `set_gain.sh` exemplar · **DEPLOYED, campaign A running (S57)** | 2026-07-28 (S56; upd. S57) |
| DEC-0060 | **`debug_rtld` alone doesn't enable driver debug logging** — the `[Logging][[[user]]]` logger must also be at DEBUG, or `dbg_rtld()`'s `log.debug()` calls are silently dropped regardless of verbosity. Explains Phase 0's first ~7h attempt producing nothing. Fix: a scoped `[[[user.rtldavis]]]` entry, not the broader `[[[user]]]` (keeps other `user.*` services at INFO) | Accepted · **extends** DEC-0043 · **explains** DEC-0059's Phase 0 first attempt | 2026-07-29 (S57) |
| DEC-0062 | **Logs are an egress path the read-guard does not cover — never log key material.** `pressure_service.py` logged `api_key[:8]` at INFO on every startup, into `weewx.log` + 30 rotations; DEC-0047's guard covers *configs*, not logs, so the most routine operation here (tail the log to check a restart) walks past it into a transcript — twice on 2026-07-29. Log the *answer* (`present`/`MISSING`), never a fragment; resolve flags into locals before the call so no credential appears in a log argument at all (**a checker with exceptions is a weaker checker** — the fix belonged in the code, not the test). AST-guarded with a positive control. Also re-proved DEC-0046's layer question: the file is **BAKED** (`Dockerfile:117`), NOT mounted as first assumed — an `scp` would have been a silent no-op. **Deploy deferred to the next image release: rebuilding mid-campaign would confound DEC-0059's arms** | Accepted · **extends** DEC-0047 to logs · **applies** DEC-0046 · **fixed in repo, not deployed** | 2026-07-29 (S57b) |
| DEC-0061 | **Campaign A died of two defects a hermetic suite could not see.** (1) `health_ok`'s 90s budget was too small BY CONSTRUCTION — a restart needs boot (~25s) + up to a full 60s archive interval + ~30s write lag ≈ 115s; measured, the abort fired **3 seconds** before the record landed. Arm B had won the same coin flip. Now 180s, with the *arithmetic* asserted, not the literal. (2) `send_mail` sourced the env file without exporting, so its `python3` child died on `KeyError` — every alert the script could ever send was inert, same shape as DEC-0060 a day earlier: **configured-looking, never exercised, therefore never disproved.** Schedule regenerated for a clean 07-30 start (the 07-29 run damaged 3 Latin-square cells); stale state reset so the first tick couldn't record baseline config as arm-B data; a comment promising a `schedule --generate` mode that never existed corrected | Accepted · **fixes** DEC-0059's apparatus · **extends** DEC-0045 to runtime budgets | 2026-07-29 (S57b) |
| DEC-0064 | **Campaign B: the no-LNA RX campaign** — pre-registered end to end so the swap night is execution, not derivation. Owner-gated swap sequence (nothing touches the container/bias tee until an in-chat GO with the owner at the dongle; antenna-disconnected window = the 20–40 s SMA swap); overnight pilot 08-07 00:35–04:20 (gain-only, 45 min/arm, HIGH→LOW 496…328, **arm-selection input only, never adoption evidence**; an abort = the cliff found, high arms already harvested); H hold (arm-A settings, own harvest tag) through Friday; square 08-08→08-16, gain **{372, 496}** (372 = cross-campaign anchor; 496 = R820T max, pilot-adjustable) × ex {0, 50}, `-fc/-ppm 0` unchanged from A to keep the LNA contrast clean; abort floor 55→**50%**. Archive forensics replaced the assumed baseline: two flat plateaus (Jun 67.45 sd 3.22 @207; Jul 74.83 sd 4.13 @372), **both LNA-in (owner-confirmed)** — S29's "pre-LNA baseline" label was wrong, no honest no-LNA telemetry exists anywhere, the pilot is the first real measurement; bonus: the plateau contrast retroactively answers DEC-0017 (207→372 = +7.4 pts, same hardware, uncontrolled). v2.0.12 ships `BIAS_TEE` env (default 1, public image unchanged) + DEC-0062's deferred redaction, landing *between* campaigns so B runs on one image. A stays unread/unadopted through the gap | Accepted (design) · **executes** DEC-0059's second campaign · **carries** DEC-0062's deploy · runbook `docs/CAMPAIGN-B-RUNBOOK.md` | 2026-08-01 (S61) |
| DEC-0063 | **Adopt the eaglehunt-ops session-context tiering standard** (`BOOT.md`/`CONSTANTS.md`/`MANIFEST.md`/`ARCHIVE/`), migration to execute next session. ops#130 offered it while arguing *against* it ("the case here is genuinely weak"); the premise failed measurement — the tree is at **~25.5K not ~21K**, the saving is **~19K not 6–8K**, and decisively Tier-1 grows **~1.1K tokens per session close** by ritual (DEC-0052 steps 2–3), so "leanest in the forum" describes a moment, not a trajectory. Both siblings already migrated. **Addendum, this repo only:** ops is PRIVATE and this repo is PUBLIC, so `CONSTANTS.md` is self-sufficient for anyone who can clone it and any `ops/CONSTANTS.md` pointer is an owner-only supplement, never a prerequisite — §3's trio rule would strand every external contributor | Accepted · **supersedes** DEC-0030's Tier-1 table · **executes** ops#130 · **MIGRATED S60** — always-load 91,806 B (~25.5K tok) → 25,819 B (~7.2K), `docs/STATUS.md` retired | 2026-08-01 (S59; exec. S60) |
| DEC-0058 | **ROADMAP.md trimmed to P0–P3 only** — P4 + "Longer horizon" (credential hygiene, multi-source adaptability, template harvest, ops#110 winter build) moved to a new "Long-term direction" section in BACKLOG.md, so the actively-sequenced plan isn't buried under uncalendared/aspirational items | Accepted · **extends** DEC-0057's same-session doc discipline | 2026-07-28 (S56) |
| DEC-0065 | **The watchdog escalates and hands off; it does not acquire a bigger hammer.** ERR-0005 measured the old loop failing in a specific way: **9 USB resets, 0 effective**, 17 emails in 80 minutes, and not one distinguishing the 9th attempt from the 1st — while reset #10 preceded a strictly worse failure mode by 46 s (dongle still enumerating for `rtl_biast`, `rtldavis` unable to claim it). **Detection was never the deficiency** and is unchanged: RECEPTION ALERT fired correctly 8 minutes in. Now: `watchdog_poll()` judges each reset by whether reception recovered (`RESET_VERIFY_S` 180 s, deliberately **shorter** than `RESET_CD` 300 s so no attempt is counted before its verdict); 3 ineffective resets → stop resetting, escalate **once**, loudly; `watchdog_not_running()` never resets on that mode (a USB reset can't fix a binary that dies on startup and plausibly caused it) and escalates immediately; only the first reset of an outage emails. Escalation lands ~18 min after the first reset — for ERR-0005, ~00:29 instead of reset #10 at 01:27. **Auto-recreate deliberately NOT built**, against the owner's initial ask and applying the owner's own "proven fix" bar: n=1, and we cannot explain why `kill;rm;run` worked at 01:48 when the apparatus's own `kill;start` at 00:08:33 did not — and `rm` is not reversible. `build_recreate_cmd()` derives the command from **live `docker inspect`** (never the stale NAS compose file), returns `None` rather than a half-right line, redacts credential-shaped env values (public repo, DEC-0062), and ships it in the escalation email. **Related thread checked and CLEARED same session:** the apparent campaign-A abort near-miss (arm D read 71% when declared dead) was not one — `health_ok()` waits for an *archive* record, and the last before the abort was 00:04:20 with the next at 01:24:24, 80 min later; the ~180 s budget ran against a genuine absence. RapidFire loop publications are not archive records. DEC-0061's arithmetic holds; **campaign B is not gated on it** | Accepted · **caused by** ERR-0005 · **applies** DEC-0062 · **upholds** DEC-0061 · deploys by `scp`, not the image | 2026-08-02 (S62) |

| DEC-0066 | **Campaign B is HELD until the instrument is trusted** — prepared to launch 08-03 (LNA already out, schedule shifted −4 days, v2.0.12 built and verified, apparatus green), then held after prod went deaf **three times on 08-02**: 105 min (ERR-0005, unexplained), 3 min at 13:47 (unexplained), 10 min at 19:45 (`database is locked` + three uploader threads refusing to shut down). **Held, not cancelled; DEC-0064's design is unchanged.** The load-bearing argument is *not* abort risk — an abort is a safe failure that restores baseline. It is that campaign B **measures reception**, and a receiver intermittently losing 50–100% of packets for unexplained reasons yields **noise shaped like a result**: arms differ, means compute, and the difference may be an artifact of when the deafness fell. A contaminated dataset is worse than an abort because it does not announce itself. Cost of holding is honest: schedule slips and the `SCHEDULE=` literals now sit in the **past** (an `install` against them would jump into the middle of the square — regenerate first). Against that, prod stays LNA-out so the accidental H-hold keeps accruing (n=1106, mean **72.0%** vs A's pooled 72.4%; still not a clean comparison, A pools the gain-207 arms). **Do not treat "the apparatus is ready" as the launch condition** — it has been since S61 | Accepted · **defers** DEC-0064's execution, design untouched · **caused by** ERR-0005 | 2026-08-02 (S62) |
| DEC-0067 | **The recurring dropouts are process freezes, not RF loss — and the driver's own watchdog proves it.** `get_stderr()` is bounded at 10 s, so a *running* main thread that hears no RF returns to `genLoopPackets()`'s `while` top and raises `rtldavis process stalled` at 150 s — as it did **21 times during ERR-0005**. Across the silent 208–218 s gaps it **never fired**, which means the main thread was not executing: these are **process freezes, and the receiver was fine**. Measured: genuine RF loss is confined **entirely to ERR-0005** (21 detections on 08-02, **0 on every other day**); freezes recur ~1/day at ~3.5 min, independent of it. **One occurred 07-30 with the LNA still in — so they are NOT new to the no-LNA regime** and that standing watch closes. Three consequences: (1) the monitor's metric counts *published output*, so a frozen process and a deaf receiver both read `WINDOW: 0/21` — every "unexplained dropout" was scored by an instrument that cannot make the distinction the watch was about; (2) packets are stamped at **parse** time, so a freeze *misdates* what it recovers — 03:01–03:03 have no records and the next record absorbs ~3.5 min, distorting exactly the counters campaign B measures, down then up; (3) DEC-0066's bar is now partly met — the recurring class is explained in kind, **bounded** (~0.4 % of wall-clock), and pre-dates the LNA, while ERR-0005 is demonstrably a **single incident**, not the tip of a pattern. **B stays held**, but the launch condition is now mechanical (detect and exclude freezes) rather than open-ended. Ruled out with evidence: NAS-wide stall (influxdb's timer fired mid-freeze, sub-ms on schedule), the S37 stdout wedge (live config has **no console handler**), CPU-quota throttling (DSM 4.4 exposes no `cfs_quota_us`), `pressure_service` (82 fetches, worst 8.99 s), the monitor's 6-hourly read, and the HH:04 cluster (campaign-A swaps). **Open: why it freezes** — all threads stop and nothing is logged, consistent with a thread blocking on the bind-mounted log volume while holding the logging lock (box is at **18.6 % cumulative iowait**); unproven, `D`-vs-`S` capture pending. Upstream saw it and worked around it undiagnosed (`get_stderr()`: *"a hangup will occur regularly, sometimes of more than a minute"*). **Lesson: a watchdog that does not fire is telling you something** | Accepted · **reclassifies** DEC-0066's evidence · **corrects** ERR-0005's framing of the 13:47 event · design of DEC-0064 untouched | 2026-08-03 (S63) |
| DEC-0068 | **Coffee-radar is a confirmed contributor to some process freezes, not a full explanation.** This NAS also runs coffee-radar and hyperlocal-forecast, and HLF has its own measured incident class here (its DEC-0162: coffee-radar's scheduled runs cause page-cache-eviction stalls). An overnight watcher (`ops/freeze_watch.sh`, now committed) caught two distinct freezes the same night: **freeze #1** (17:48:59–17:52:37 EDT, ~4 min) with coffee-radar not running and loadavg normal (0.3–0.7); **freeze #2** (19:13:43–19:15:41 EDT, ~2 min) with **loadavg spiking to 12.39** and coffee-radar **confirmed running the entire time** via `nasctl inspect` — its scheduled job never sets `--name`, so it runs Docker-auto-named and is invisible to any check that greps `docker ps` by name instead of image, a gotcha this session's own first check fell into. Its 19:00:16 EDT start matches coffee-radar's documented 19:00 daily run almost exactly once corrected to **local time (EDT), not UTC**. **Not the sole cause**: freeze #1 shows the identical symptom (all `S`, brief `R` ticks, `weewxd` never `D`) with neither signal present, so this is n=1 correlated out of 3 total captured freezes, not a base rate, and the 4 historical freeze timestamps still show no clean match against the corrected schedule. Mechanism unconfirmed — `weewxd`'s main thread stays `S`, never `D`, even during the load-12 freeze, so this isn't literal I/O-blocking. Campaign B's DEC-0066 gates are unchanged either way | Accepted · **extends** DEC-0067's open question · design of DEC-0064 untouched | 2026-08-05 (S65) |
| DEC-0069 | **The campaign metric moves to per-minute `rxCheckPercent`; freeze exclusion is structural, not magnitude-based.** DEC-0066's second gate, closed — and the estimate that made it a gate was ~60× too large. `harvest()` scrapes the monitor's **5-minute** `RECEPTION:` aggregate, so one frozen minute drags a whole bucket (**measured 16 % and 27 %** against a ~72 % neighbourhood), destroying four good minutes with the one bad one — `(72−16)/72 ≈ 0.78` pts on a 6 h block, exactly BOOT's ~0.8. The same measurement exists **per minute** in the archive DB as the driver's own `rxCheckPercent` (S31's honest metric), where the same freeze damages **one** record: ~0.18 pts on a block, **±0.03 measured** on a pooled arm against a **2.0** adoption bar. **The source change is ~4× of the fix; the exclusion rule is the remaining sliver.** Measured signature over 10 988 records / 33 gaps: three non-overlapping classes — *freeze* (`rx` before the gap 4–17, vs population p01 **56.5**; after, normal), *arm swap* and *lock/outage* (both: `rx` before normal, after **NULL**). **The contaminated record is the one ADJACENT to the gap, not the minutes inside it** — those are simply *absent rows*, which is what shrank the design (BOOT had assumed they scored as zeros). `interval` stays `1` on the bad record, so it cannot self-identify and detection must be structural. Rule: drop the record either side of any gap (symmetric — catches truncation *and* absorption without knowing which), plus NULL, plus non-physical `rx > 100`; ~3.5 % of records. **A magnitude threshold is rejected outright** — it would discard genuine deep fades and bias every arm upward, the exact confound the Latin square removes (positive-controlled in tests). **Corrects DEC-0067 in both directions:** its "up" is real — 2026-07-29 03:10 reads **200.0 %**, a record that absorbed 2 min while stamped `interval=1`, and `summarize_reception_rows()` applies no cap so it pushes a block **up** ~0.35 pts — but it is *conditional*, ~1 in 8 days against ~10 freezes, and an initial two-freeze reading that concluded "no up" was too small a sample. **Campaign A recomputed** (12 balanced blocks): A 74.81 / C 74.37 / D 74.17 / B 73.87, spread **0.94 pts**, no arm near adoption; the ~1.9-pt offset from BOOT's 72.4 matches `weewx_monitor.py`'s own documented "~1–2 pts optimistic" note, so the two metrics validate each other — **and A-vs-B must be read on the same one**. *Sealing note:* DEC-0066 sealed A's arm winner until after B; validating the tool necessarily unsealed it. Design uncompromised (DEC-0064/0059 pre-date this), but it was a side effect, not a decision. Two build-time defects, both of which would have printed confident wrong numbers: a bare run **pooled campaign A's aborted 07-29 attempt** with the campaign proper (arm B +60 records, unbalanced square, tidy table, no indication) — now detected mechanically, since a docstring would not execute (DEC-0040); and deriving the query bound locally dragged the **entire archive** over ssh (does not finish in 120 s) — now bounded NAS-side. New tool `ops/campaign_analyze.py` + 14 tests; **`ops/rx_experiment.sh` deliberately untouched** | Accepted · **closes** DEC-0066's metric gate · **corrects** DEC-0067's impact framing · **applies** DEC-0045's positive-control rule · design of DEC-0064 untouched | 2026-08-05 (S66) |
| DEC-0070 | **The `database is locked` defect is a 5-second default with a 10-minute penalty; WAL is the real fix and a cross-repo mount blocks it.** Not a weewx bug — `weedb/sqlite.py:136` reads `timeout = to_int(argv.get('timeout', 5))`, the live config set none, and the archive runs `journal_mode=delete` where a reader's SHARED lock blocks the writer. A reader holding it **six seconds** therefore yields `CRITICAL Database OperationalError`, weewx's **hardcoded 120 s wait**, and a restart = **5–10 min of outage**; the penalty is three orders of magnitude past the cause. (The CRITICAL logs *after* the teardown — weewx shuts services down in the exception path first, which is why the 08-02 trace reads "Unable to shut down OWM thread" → `rtldavis killed` → CRITICAL.) **Shipped: `timeout = 30`** in the live `weewx.conf` `[[SQLite]]` — 30 not 60 so it stays under the 60 s archive interval and records can't pile up behind a wait. Verified *in the running system* (DEC-0046), not the artifact: resolved `database_dict` carries `'timeout': '30'`; restart healthy with a new record in **106 s**, inside DEC-0061's ~115 s worst case. **Honest cost:** weewx now *blocks* ≤30 s instead of erroring — a good trade (a stall loses one record, a restart loses ten minutes) but such a stall is indistinguishable from a DEC-0067 freeze to `freeze_watch.sh` and gets excluded by `campaign_analyze.py`'s gap-adjacent rule. Both correct; neither a bug to chase. **WAL blocked by `hyperlocal-forecast-api`'s single-FILE mount** (`DST=/data/weewx/weewx.sdb RW=false`) — WAL's `-wal`/`-shm` siblings can never appear beside a single-file bind. **A first reading of this was wrong and the correction matters:** the assumption that the mount must also become *writable* (granting a read-only consumer write access to the archive dir — a real regression) was **tested and disproved** on the container's own SQLite 3.46.1 against a live writer: directory mount read-only works with `-shm` present *or absent*; only the single-file case fails, and it fails as **`no such table: archive`**, not as stale data. So `RW=false` stays and the fix is *only* file → directory, with HLF's in-container path unchanged and **no HLF code change**. Filed **ops#141** (`repo:hlf`) with a staged order — change the mount and verify HLF under the existing `delete` journal *first*, so a failure there is unambiguously the mount; then flip WAL (reversible via `PRAGMA journal_mode=DELETE`). **Drift risk recorded:** `weewx.conf` is the mounted layer and is never committed, so this setting lives **only on the NAS** with no repo artifact and no CI — a stock recreate would silently revert it; hence a `CONSTANTS.md` row. **Also corrects DEC-0067's reader list** — it named "the dashboard"; a scan of every container mounting a weewx path finds only `hyperlocal-forecast-api`, `eh-proxy` (parent dir, RO), and weewx itself. **Guard finding:** the NAS mutation that wrote live prod config **did not trip the Class C guard** — `ssh <nas> "python3 -" < script` hides the mutation in stdin while the guard matches the command *string*, and that is the shape CONVENTIONS actively recommends; meanwhile the *read* guard fired three times on `grep`/`tail` against files with no secrets. Owner-authorized in chat, so nothing improper — but the mechanism didn't enforce it. Belongs to ops (OPS-DEC-0060) | Accepted · **bounds** DEC-0066's last gate (capped, not closed) · **applies** DEC-0046 · **corrects** DEC-0067's reader list · real fix filed as ops#141 | 2026-08-05 (S66) |
| DEC-0071 | **WAL was tried and rolled back — the mount was never the only blocker, and DEC-0070's own test that said otherwise was structurally blind.** HLF shipped the directory mount (its S235); WAL went live 06:56 EDT and was **rolled back 07:24**. DEC-0070's four-scenario table concluded a **read-only** directory mount works with WAL, so `RW=false` could stay. Wrong: the test ran `os.chmod(tmp, S_IRUSR|S_IXUSR)` on the **directory only**, leaving the files inside read-write, so SQLite could still write `weewx.sdb-shm`. A Docker `:ro` bind makes the **files** read-only too; a WAL reader must write `-shm` to join the WAL, so HLF fell back to the main DB — which in WAL mode stops advancing except at auto-checkpoints — and **froze on a stale snapshot within minutes**. DEC-0035's lesson recurring: *a passing test proves nothing if it is structurally blind to what it tests.* **Second blocker no mount change can fix:** SQLite creates `weewx.sdb-wal` mode **0555** (vs 0777 on the DB), so even a read-write mount leaves a non-root reader unable to write it — also why a non-root SSH user got `attempt to write a readonly database` trying to checkpoint. **Rollback was the hard part:** `PRAGMA journal_mode=DELETE` needs an EXCLUSIVE lock, weewx holds a persistent connection (`database is locked`), and with the container stopped there is no `docker exec` either — resolved by making weewx do it via `[[[pragmas]]] journal_mode = DELETE`, applied by weedb on connect as root at startup. **Pragma left in place deliberately** — re-pins `delete` every start so an accidental flip can never again silently strand a reader. **Self-inflicted outage:** that pragma was first written as the *scalar* `pragmas = journal_mode = DELETE`; weedb iterates it as a **mapping**, so configobj needs a subsection — the scalar yields `TypeError: string indices must be integers` and weewxd **crash-looped ~6 min** (CRITICALs 07:18:58, 07:20:22). Two process failures behind it, both this session's: the first rollback opened with a `SELECT COUNT(*)` that **had already timed out once the same session**, and the config shape was assumed from the field name rather than checked against the consumer whose code had been read an hour earlier. **Consequences:** WAL not viable as scoped (ops#141); the DB lock stays **bounded not cured** by DEC-0070's `timeout = 30`, which was most of WAL's practical benefit at none of this risk; HLF did **not** self-recover and needs a container restart (left to an HLF session); campaign B unaffected. **Lesson: two of three failures repeated lessons already on file here — having them written down is not the same as applying them on an incident path** | Accepted · **bounds** DEC-0070's WAL recommendation · **corrects** its published test · **re-proves** DEC-0035 | 2026-08-06 (S66) |
| DEC-0072 | **The MANIFEST indexes classes, not instances — and that makes every script's header a load-bearing artifact.** One row per artifact cannot hold a fixed cap: the row count tracks a population that only grows, so the file crosses any cap *by construction*, not by neglect (measured S67: **1948 tok against a 1000 cap, 197%** — worst of the four repos in the ops sweep). Fix is neither a bigger cap nor fewer artifacts: where a set shares a load-time convention, **one row names the set and the convention** and the instances self-describe at their source. `ops/*` + `scripts/*` went from five per-artifact rows to one — *the script's header comment is its manual*. **Verified before being relied on** (rule 9 is only sound if the convention actually holds): the existing headers already state why each script exists and which lying symptom it catches — **richer than the rows that duplicated them**, i.e. STANDARD rule 5 applied to the index itself. **Coverage went UP**: the old index carried 5 of 11 harness scripts; six had no row at all. **The obligation this creates, and the reason it is a DEC rather than a chore:** a class row is a promise about every *future* member of the set — a new `ops/`/`scripts/` file must ship with a header answering "why does this exist, when would I load it?", because a bare shebang silently falsifies the index and **nothing will fail to say so**. Four facts that had drifted into the index were moved back to their scripts (campaign A's `--since`; campaign B being the loaded schedule + `install` refusing a stale one; `EXPECT_IMAGE` tracking the deploy). **Not done:** `docs/` rows stay per-artifact — their "when to load" genuinely can't be inferred from name or location, which is rule 9's own reserved exception. **Result:** BOOT 3734 → 2161 tok, MANIFEST 1948 → 970, verified by running `checks/tier-sweep.sh` itself against fixtures — the tool that files the issue is the tool that confirms the fix. Landed at **3% headroom deliberately**, not at 100% of cap: the dashboard's S193 diet hit exactly the cap and was over again within a session | Accepted · **amends** DEC-0063's tier-file structure · **applies** ops STANDARD rule 9 (OPS-DEC-0098) · closes ops#145 | 2026-08-06 (S67) |

| DEC-0073 | **Supervise the USB watchdog, make its absence LOUD, and model its resets before campaign B.** `ops/usb_watchdog.sh` was deployed 2026-05-22, hand-started once, never supervised (no crontab entry, no pidfile, no boot task) — dead by the 07-08 reboot at the latest, absent for three qualifying stalls on 08-06, while `BOOT.md` recorded it "deployed and live" for ~2.5 months. **Two defects, and the second is the one that mattered:** it isn't supervised, *and* its absence is invisible — a watchdog that isn't running writes exactly the same log as one with nothing to do, so no observation distinguishes them and it could only ever be found by accident, which is how it was. **Decision: (a)** adopt `weewx_monitor.py:102-115`'s proven PID guard (read pidfile → exit 0 if `/proc/<pid>` lives → else claim, release via `atexit`) plus a 5-minute scheduled re-launch; the guard makes re-launch idempotent so the scheduler carries no state. **(b)** heartbeat file touched each tick, so liveness is an mtime check not an inference (*not* a periodic log line — unbounded growth, buries real events). **(c) the structural half — `ops/soak_check.sh` asserts it**, because that script exists to ask "healthy, or does it just look Up?" and did not ask it of the watchdog; without this we fix one instance and leave the class. **(d)** rising reset rate reaches the alert path — a watchdog that papers over a degrading dongle is worse than none. **Campaign-B interaction, the real call:** `campaign_analyze.py`'s three-class gap taxonomy (freeze / arm swap / lock-outage) was validated over 07-29→08-05 — **a window in which the watchdog was dead** — so a USB-reset gap is a FOURTH class it has never seen, and by shape would be absorbed into `freeze` and silently excluded. Possibly the right treatment, but right *by accident*, which is DEC-0035/DEC-0071's exact failure. Enabling it also alters a pre-registered apparatus (DEC-0048). **Owner call: watchdog ON, analyzer taught the fourth class**, reading the watchdog log and excluding reset-adjacent minutes explicitly. Rejected: unmodelled resets (B's headline number could be shaped by an intervention nobody accounted for) and watchdog off (an unattended multi-night run with no USB recovery is what it exists for). **Verified first:** dongle still on `1-3` (`0bda:2838`), `syno_vbus_reset` present, script byte-identical to repo, monitor's guard read not remembered. **Does NOT explain why the stalls happen** — mitigation, not diagnosis. **Lesson: a sha match proves the FILE, never the PROCESS** | Accepted (design; implementation follows) · **gates** campaign B · **extends** DEC-0069's taxonomy · **re-proves** DEC-0035 | 2026-08-06 (S67) |

| DEC-0074 | **Retire `ops/usb_watchdog.sh` — the monitor IS the watchdog, and DEC-0073 solved a problem that did not exist.** DEC-0073 said the watchdog was dead *and* that three stalls on 08-06 went unhandled. **The second half was false.** `weewx_monitor.py` (pid 5015, alive) carries `reset_dongle()` (l.342) and `watchdog_stall()` with escalation (l.354), wired at l.692, and handled all three within seconds — `09:53:40 / 10:11:13 / 10:32:52 STALL detected → RESET → done, idVendor=0bda`. The script is a **superseded standalone predecessor**; the monitor does strictly more (verifies whether a reset *worked*, escalates after 3 ineffective ones, distinguishes 'stalled' from 'not running', records that a reset may have caused ERR-0005). **Deploying it would have added a second uncoordinated resetter to the same dongle**, unshared cooldown, beside a monitor whose source records nine resets in 75 minutes on 08-02 — a regression, not a fix. **How the error happened, since the shape recurs:** the "not running" evidence was sound and stands; what was never checked was whether anything ELSE did the job. Three sources were consulted — the watchdog log, `weewx.log`, the process table — all silent about the monitor; `weewx_monitor.log` was never read and holds the answer in plain text. **DEC-0031's lesson turned on its author: "this component is dead" and "this capability is missing" are different claims needing different evidence.** **Survives from DEC-0073:** *a sha match proves the FILE, never the PROCESS* — now generalized to absence-of-function too; and the `soak_check.sh` criterion, **repointed at the monitor** (live pid + log <300 s; a live pid with a stale log means *wedged*, not dead). **The real defect DEC-0073 walked past:** all three resets FAILED — `RESET ineffective (1/3)` each time, bad windows climbing 8→10→15. The monitor works and is reporting that **the remedy does not**. New `USB RESETS INEFFECTIVE` criterion; open and unexplained. **Bigger consequence for campaign B:** reset gaps are not a *new* class B would introduce — the monitor fired **nine on 08-02, inside the 07-29→08-05 window `campaign_analyze.py`'s taxonomy was validated against**, so reset-adjacent gaps are already inside campaign A's recomputed numbers. That makes the fourth-class question one about a result already published in DEC-0069, not a pre-launch nicety. **No NAS change made or needed; blocker 4's deploy gate is void**. ⚠️ **AMENDED S68b (#147): the lesson holds, the INSTRUMENT does not.** This DEC's replacement probe — process start via `nasctl ls /proc/<newpid>` vs file mtime — is unsound: proc-dir mtime is **access** time, measured reporting 17 s for a 2.88-day-old process. Use a **startup log line** after the file mtime (what actually carried S67's and S68's verifications), `/proc/<pid>/stat` field 22 vs `/proc/uptime`, and new-pid-with-old-pid-gone. *A DEC written because logs named an operation that never ran adopted a probe that would report a restart that never happened* | Accepted · **supersedes DEC-0073** · **corrects** the S67 blocker-4 finding · **re-proves** DEC-0031 · **probe amended S68b (#147)** | 2026-08-07 (S67) |

| DEC-0075 | **Photograph the reset instead of reasoning about it — capture from inside the sudo grant, host-side through `/proc`.** The resets fire and do not work (3/3 on 08-06, 9/9 on 08-02) and the hypothesis is explicitly unestablished: the reset may treat the **device** while the fault is the **consumer's grip** — unbind/rebind is not a power cycle, so the dongle stays enumerated and nothing makes the stalled `rtldavis` drop its libusb handle. `ops/usb_forensics.sh` is built to see either predicted signature — **stale container view** (host gains a new `devnum`, container still shows the old node) or **surviving grip** (an fd on the pre-reset node afterwards) — and, equally, to return the negative: both clean means the stall is **not a USB fault at all** and the reset treats the wrong thing entirely. **Built before the evidence exists, necessarily:** the capture needs a live stall (~1/day, unpredictable), and there has been **none since the corrected reset code went live 08-07 19:28** — zero `RESET`/`stalled` lines across the 08-07 and 08-08 monitor logs, both greps positive-controlled against 1440/521-hit `WINDOW` counts. Nothing to read retroactively. **Three design calls. (1) Host-side `/proc`, not `docker exec`** — this fires *during a stall* and a wedged container can block an exec indefinitely, so the capture would hang on the very event it records; `/proc/<pid>/root` and `/proc/<pid>/fd` cannot block, one pid found by `comm` yields everything, and `rtldavis` being **absent** is itself a finding. **(2) Pre/post fire from inside `usb_reset.sh`** — both decisive reads are root-only, `weewx_monitor.py` runs as the unprivileged `weewx-monitor` user (which is *why* the reset goes through sudo), and the sudoers grant is scoped to `usb_reset.sh` alone, so capturing there needs **no new grant**; the monitor fires only the `+RESET_VERIFY_S` capture, host-side and **self-labelled DEGRADED**, because an unreadable fd section must never be mistaken for a released handle — DEC-0074's absence-is-not-evidence trap. **(3) Capture-only** — DEC-0065's refusal to automate the container recreate is untouched; mixing a remedy change into the measurement meant to justify it would confound both. **An escalation was introduced and closed:** executing a helper from `usb_reset.sh` runs it as root under the NOPASSWD grant, and mode 777 is common on this NAS, so a helper writable by `weewx-monitor` would convert that narrow grant into arbitrary root execution. The script now verifies the helper is root-owned and root-only-writable, **refuses loudly** otherwise while still resetting, and `do_reset()` logs its output on a zero exit so the refusal cannot become a silent no-op — checked, not documented (DEC-0040), and positive-controlled by neutering the check and watching the helper run. **Evidence is never load-bearing over the watchdog it observes:** missing, broken or refused, the reset survives intact. **DEPLOYED + verified 2026-08-09** from merged tip `ad7e5a4` (root-owned as required, monitor 3870 → 8810, process evidence not a sha). The post-deploy smoke test then caught a defect in the shipped script — `/proc/<pid>` mtime is **access** time, so `started=` claimed a 2.88-day-old `rtldavis` was 17 s old; fixed in PR #146, and **DEC-0074's own documented probe has the same flaw (#147)** | Accepted · **advances** DEC-0074's blocker 4 · **corrects DEC-0074's verification probe (#147)** · **applies** DEC-0040 · **leaves DEC-0065 untouched** | 2026-08-08 (S68) |

| DEC-0076 | **The secret gate missed `GMAIL_PASS`-shaped keys — the fifth hole, and the first found by routine control rather than by audit.** The key list held `password` and `passcode` but nothing for the `_PASS` abbreviation, so `GMAIL_PASS = "..."` was undetected **in every spelling** (spaced, unspaced, colon-separated) — and `GMAIL_PASS` is the exact variable `weewx_monitor.py` uses for its Gmail credential, so the hole sat squarely under this repo's own secret. **Nothing was ever leaked through it:** no `_PASS` literal in the tracked tree (the sole match is README's documented `NOPASSWD:` sudoers line) and no `GMAIL_PASS = "<literal>"` on any ref in the full history. A future hole, closed. **How it surfaced is the part worth keeping** — not an audit, but DEC-0045's routine positive control before an *unrelated* commit: the planted payload carried an `api_key` line and a `GMAIL_PASS` line, the gate reported only the first, and the asymmetry was free. The rule that costs one command per commit found the fifth instance of this gate being wrong. **Two detectors, each proven necessary by removing it and watching its payloads leak: (a) bare `pass`** in the key list — deliberately *not* `passwd`, which matches README's `NOPASSWD: /volume1/...` and would report the binary path as a credential; bare `pass` cannot, because the detector requires `[:=]` immediately after the key and `NOPASSWD` has `WD` there (same reason Python's `pass` statement and `passed = True` do not trip it); **(b) a literal matcher for the four-group app-password form**, which slips past the assignment detector *even with* `pass` in the key list, because that detector needs **8+ consecutive** value characters and the four-group form breaks every 4 — the shape Google displays and people paste. **The false-positive half nearly shipped and is half the fix:** `PASS` is listed separately from `pass` because detection is case-insensitive and the allow-list deliberately is **not** (that asymmetry is what distinguishes a constant reference from a literal, bug class 1); without the uppercase spelling, widening the key list made the gate report `weewx_monitor.py`'s own `os.environ` lookup and README's placeholder as credentials, and the tracked-tree check failed. The harness caught it pre-ship — a gate that cries wolf on its own repo's source gets switched off. Harness **41 → 51** cases, tracked tree still clean | Accepted · **extends** DEC-0039/DEC-0045 · closes a future hole, not a live one | 2026-08-08 (S68) |

| DEC-0077 | **Reset gaps do NOT contaminate campaign A's figures — the exclusion is class-independent, and the outage left no rows to mis-average.** Blocker 5, closed on measurement. Every rotated monitor log spanning campaign A (`.11`=07-29 … `.4`=08-05) grepped: **11 resets, all on 08-02**, 00:11:23 → 01:27:20, seven of eight days empty — independently corroborating DEC-0067's "0 detections on every other day". The archive across the incident reads **00:04 = 72.73% (normal) → 80 rows ABSENT → 01:24 NULL → 25 rows ABSENT → 01:51 NULL → 01:52 back in range**, which is exactly the tool's documented **lock/outage** signature (*rx before normal, rx after NULL*). **The load-bearing point: classification is descriptive, exclusion is structural.** DEC-0069 drops the record either side of *any* gap plus every NULL plus `rx > 100`, never consulting the class — so 00:04, 01:24 and 01:51 were already excluded and the 105 absent minutes contribute nothing (absent rows are not zeros; that is what shrank the DEC-0069 design in the first place). **DEC-0074's framing was that reset gaps got "sorted into freeze/swap/lock"; they did — into lock/outage — and it changes nothing.** The real exposure was never mislabeling but a reset outage producing **present-but-low** rows, which nothing would exclude because the tool refuses magnitude thresholds by design (a threshold discards genuine deep fades and biases every arm upward). **No such rows exist** — the transition is normal → absent → NULL → normal with nothing low-but-present at either edge. **What IS wrong, narrowly:** the taxonomy's completeness claim holds for *shapes* but not *causes* — a USB reset is a fourth cause producing the lock/outage shape. Treatment keys on shape, so **no analyzer change is warranted**; recorded so campaign B's reading does not re-open it. **Two bounded residuals, neither gating:** 01:52 (57.14%) survives the rule because it neighbours a NULL *row* not a gap, worth ≈**0.04 pts** on a 6 h block against a 0.94-pt observed spread and a 2.0-pt bar; and 105 min vanished from one arm's block (~2.4% of 12 × 6 h), costing **precision, not bias** — during a receiver outage the arm's RF performance is unmeasurable and a USB failure is not a property of the arm, so exclusion is correct treatment rather than concession. **Correction to the record:** the log shows **11** resets, not nine — ERR-0005 and DEC-0065 both say "nine in 75 minutes" and call 01:27:17 "reset #10"; it is the **11th**, span 76 min. Nothing downstream depended on it (DEC-0065's argument is about unbounded retry, which 11 strengthens), but it appears in two decision entries | Accepted · **closes blocker 5** · **bounds** DEC-0074's concern · **amends** DEC-0069's completeness claim (shapes yes, causes no) · **corrects** ERR-0005 / DEC-0065's count | 2026-08-09 (S68d) |
| DEC-0078 | **Image builds move to the NAS** — the arm64 laptop's linux/amd64 cross-build fails deterministically (tar ENOSYS under emulation), and the failure hid behind a pipeline exit 0. Build natively on the NAS from a `git archive` of the merged tip, success = the explicit `BUILD-EXIT` marker (v2.0.3 precedent); the deploy consumes the NAS-local image; Hub publish decoupled — `save` → laptop → `push` after prod proof, so **Hub lags prod until pushed** (CONSTANTS documents the window). CI builds = the structural fix, backlogged | Accepted · **amends** release mechanics (RUNBOOK §Release, CONSTANTS §Release) | 2026-08-10 (S70) |
| DEC-0079 | **Opt into the ops-wide `.claude/transient-state` convention (ops#113)** — a tracked, one-line-per-entry file (`<revert-by-epoch> <tracking-ref> <description>`) for anything intentionally put into a non-default, reversible mode with a planned revert; SessionStart surfaces overdue entries, and deleting the line is the whole close mechanism. Force-added past the local `.git/info/exclude`, same precedent as `settings.json`. Left empty at creation — no current state meets the motivating shape (a short, easily-forgotten flip) that isn't already carried prominently in BOOT.md/CONSTANTS.md; first real entry waits for the next genuine short-lived flip | Accepted · **adopts** ops#113 (commit e5094a4, OPS-DEC-0067) | 2026-08-10 (S71) |
| DEC-0080 | **Solar diode-floor correction: exact-code `StdCalibrate` zero at the config layer — not a driver patch, not an almanac service.** At true zero irradiance the VP2+ solar sensor decodes to exactly `sr_raw=1 × 1.757936 ≈ 1.758 W/m²` (only 0 / 1.758 / 3.516 representable near zero), inflating every dark hour into the archive, InfluxDB and all nine uploads (~76 kJ/m²/day); the June 2026 fix was dashboard-only and regressed at the chart layer on refactor — the second per-path filter loss. Fix at the source (owner, S71): `radiation = radiation if radiation is None else (0 if 1.75 < radiation < 1.77 else radiation)` — exact-window, None-guarded, range form (only `math.*` confirmed in the eval namespace). Lives in the live NAS `weewx.conf` AND `weewx.conf.example` (the versioned public artifact — the anti-regression mechanism the June fix lacked) AND a DEC-0070 deviations row at apply time. Option B (almanac elevation-gated service) rejected: it also needs a `process_services` live-config edit so it escapes no fragility, it bakes one station's calibration into the public image, and it buys correctness below the sensor's own resolution (~minutes/day, one quantization step) for a service + tests + rebuild — design preserved in the S71 handoff, could ride the #144 rebuild if ever wanted. **APPLIED 2026-08-11 (S73), post-GATE 2 as planned — to the live conf AND `weewx.conf.rx-baseline`**: the apply step surfaced that `restore_baseline` copies the snapshot over the live conf at every campaign abort/end, so a live-only apply would be silently wiped — both-files is the durable form (CONSTANTS deviations row carries it). Runtime-verified same morning: weewx 5.4.0 boots clean with the line and publishes. Dark-hours-read-0 verification due S74; if 3.516 appears, extend per-code, never a loose threshold. Historical step-change at cutover accepted, no retro rewrite; the dashboard's surviving `eh-ui.js` filter goes vestigial — retirement is dashboard-repo work, note filed at apply | Accepted · **resolves** the S71 radiation-floor handoff · **relates to** DEC-0029 · **applies** DEC-0070 at apply time | 2026-08-10 (S72) |
| DEC-0081 | **The stall class is RF-dead episodes, not USB faults: resets demoted to one hedge, the driver self-classifies every event, and an episode ledger feeds the LNA verdict (ws.5 / v2.0.13).** The S73 forensics differential (three capture sets from DEC-0075's apparatus + the full night timeline + cross-repo load correlation, three read-only subagents + main-thread synthesis) established: the rtldavis child goes **silent** while the device stays enumerated (devnum unchanged across every reset since Aug 2 — DEC-0075's stale-devnum hypothesis was a measurement-design error, driver unbind/rebind never re-enumerates); the driver's own 150 s watchdog **fires and respawns correctly** (kill → ~60 s weewx retry → fresh child, three times overnight 08-10/11); during an episode the respawned children stay silent across multiple gain configs and recovery correlates with **time, not with any reset or restart**. Across ~17 captured reset attempts (08-02 ×11, 08-06 ×3, 08-10/11 ×3) none demonstrably fixed anything; ERR-0005 suspects one caused the worse dies-on-startup mode, and its recreate-fixed-it now reads as coincidence with episode end — **DEC-0065's refusal to automate the recreate is vindicated**. Changes: `RESET_MAX_TRIES` 3→1 (one hedge for the never-yet-observed genuine dongle wedge, then escalate); driver ws.5 adds **child reaping** (ProcManager killed by pidof+SIGKILL and never wait()ed — the engine's per-retry fresh ProcManager stranded every corpse; three zombies under one weewxd captured), a **STALL DIAGNOSIS** line at every 150 s raise (raw-stderr count 0 = child mute → process/USB class; >0 = emitting → RF class) + stderr tail, and a paced **DATA DROUGHT** line for the RF-quiet mode that never trips the watchdog (hop packets reset it); monitor writes an **episode ledger** (`logs/episodes.log`, one row per ALERT→RECOVERY: onset/duration/stalls/resets/respawns/droughts/worst-avg/cmd) — the pre-registered datum for whether episode susceptibility differs LNA-in vs LNA-out (owner's public write-up for ~50–70 m/trees/walls sites). `soak_check.sh` ineffective-reset criterion reframed FAIL→WARN (an ineffective hedge during an RF episode is expected; alarming = a mute-class diagnosis). Guard aborts during episodes are **the system working**; auto-restart rungs rejected — restarts show the same evidence pattern as resets. Root cause of the episodes themselves (interference vs front-end vs site) deliberately **left open** for post-campaign characterization with A×B data. Reaping + diagnosis upstreamable (CHANGES-FROM-UPSTREAM rows 12–13). Deployed same day: v2.0.13 built on the NAS from `1530971`, swapped mid-H-hold, ws.5 banner verified live; monitor deployed, respawn pending the owner's path-scoped-sudo kill. ⚠️ **AMENDED S76 (DEC-0083): the LNA dates are wrong** — "08-02 and 08-06 were LNA-in" should read **08-06 was LNA-OUT** and 08-02 only *straddles* the removal (the LNA came out mid-ERR-0005, early 08-02). The clause's point survives on 08-02 alone; don't carry the 08-06 claim into the LNA characterization | Accepted · **supersedes** DEC-0074's open question and DEC-0075's hypothesis set · **vindicates** DEC-0065 · **amends** DEC-0073's framing · **relates to** DEC-0067/0068 (freezes remain a separate phenomenon), ERR-0005 · **LNA dates amended S76 (DEC-0083)** | 2026-08-11 (S73) |
| DEC-0082 | **Campaign B square recovery: shift the whole schedule +24h rather than accept a lost block.** A third same-day RF-dead-episode abort (18:05, 08-11) left the STOP sentinel blocking every tick overnight, straight through the scheduled 00:05 A-arm swap — `due_arm()` would have silently joined the square wherever the fixed grid says "now" on any clear, with no error. `test_schedule_is_a_balanced_latin_square` rules out a same-day off-grid restart (every arm must land on exactly 00:05/06:05/12:05/18:05 twice each); the earliest valid slot for arm A is one day out. Shifted all 33 square rows +24h verbatim, preserving the exact arm sequence and full 8/8 balance — 17/17 tests pass unmodified, none loosened to fit. Deployed to the NAS and STOP cleared same session, after the `scp` hit three independent guard/classifier layers (one looks like a bug: `secret-read-guard.sh` re-blocked with its own documented `command`-prefix escape hatch already applied) | Accepted · **relates to** DEC-0064 (campaign B design), DEC-0081 (RF-dead episodes) | 2026-08-12 (S75) |

| DEC-0083 | **The stall rate is measured, not eyeballed — the alarm was right and its evidence was not.** S75's "trending hot" (`episodes.log` 2→4 rows in ~18 h) **survives measurement**: over 30.5 d / 31 rotations, the 48 h and 72 h windows ending now hold **6 episodes each — the record maximum** (98.3rd/98.2nd pct); 24 h is 96.1st pct but off its peak of 5, so the burst may be easing. **The unit had to be fixed first:** a stall *line* is not an event — the 150 s watchdog re-raises every ~3 m 40 s for an episode's whole duration, so 08-02 is **21 lines and one event**; clustering gives 15 episodes, stable at 30/45/60 min, and **reproduces DEC-0081's independently-derived boundaries** for the 08-10/11 night exactly. `rtldavis process is not running` excluded as a separate class (20 lines, all 08-02, none on any burst day). **Three corrections to S75's reasoning: (1)** onset is **08-10 23:56**, not ws.5 — the v2.0.13 container started 18:05 local 08-11, so **5 of 6 burst episodes predate it**; the ledger's field of view was mistaken for the phenomenon's onset. **(2)** not a simple LNA effect — LNA-in 0.40/day → LNA-out 08-02→08-10 **0.13/day, the quietest stretch in the record** → 08-10→now **2.43/day**. **(3)** the "2→4 rows" evidence compared **two instruments**: row 3 is drought-only and `DATA DROUGHT` appears **zero times in every pre-ws.5 log**, so RF-quiet episodes were structurally invisible before ws.5. **New sanctioned readout `ops/stall_baseline.py`** — states its left-censored window and threshold sensitivity on every run; building it exposed a bias in the first cut (anchoring "current" on the last stall guarantees the window contains it), fixed to anchor on now. **The transferable lesson: a red signal resting on wrong evidence that happened to be TRUE** — the ops#147 catalog's third variant and the most dangerous, since being right retires the question that would expose the method. **Secondary sweep (job 3):** the **freeze rate is measured at 1.49/day, median 240 s** (45 freezes / 30.3 d) against BOOT/DEC-0067's inherited *"~once/day, ~3.5 min"* — right order of magnitude, **understates both by ~40%**, refines rather than overturns; **a confounder worth 60% had to be removed first** — the S37 backfill wrote `interval=15` rows that read as 28 phantom 900 s "freezes", caught only because individual events were printed instead of just the rate. **Co-rejection (DEC-0054) still 0, re-verified through 08-12 and positive-controlled** (same pipeline returns 2308 for a present token) against a stale "through 08-01". Phantom-rainRate already has a live instrument in `soak_check.sh`; humidity-spike correctly left pinned to DEC-0044 | Accepted · **amends** DEC-0081 (LNA dates, ws.5 attribution) · **executes** ops#160 · **answers** ops#159's weewx bullet | 2026-08-12 (S76) |

| DEC-0084 | **Secret gate hole class 6 — the app-password detector required quotes.** `_assign` needs **8+ consecutive** value chars and a Google app password breaks that run every 4; `_apppw` (added S68 for exactly this shape) required **quotes**. So a `GMAIL_PASS` assigned the bare four-group form was missed in every spelling — spaced `=`, env-style, and ConfigObj-style indented — all verified MISSED before the fix (the literal is described, never written: a decision log earns no exemption, DEC-0045 — and the new detector went red on this row's first draft, proving itself live). **Unquoted is the NATIVE form of the two files this repo must never commit**: `weewx.conf` is ConfigObj (bare values are the norm) and `monitor.env` is an env file, so the gate would have missed this project's own credential in the format its own config writes it. Gitignored (`.gitignore:5`), so nothing leaked — a future hole, as DEC-0076 was. **Why it survived S68:** that fix planted the QUOTED literal (holes 25/26), went green, and never asked the neighbouring spelling — **the fix certified its own blind spot**, DEC-0045 one level down and the sixth time this gate has been wrong. **Fix:** new `_apppw_assign` with quotes optional but **anchored on `_key` + `[:=]`** — the anchor is load-bearing, since four consecutive lowercase 4-letter words occur in ordinary English and an unanchored shape match would flag prose repo-wide (ops#147 item 6). **One allow-list widening refused:** the rule flagged `monitor.env.example`'s own `xxxx xxxx xxxx xxxx` placeholder; since **five of the six historical holes were allow-list defects**, the placeholder moved to `YOUR_GMAIL_APP_PASSWORD` (the convention CONSTANTS already mandates) rather than trading a good detector for a new excuse path. Harness: holes 27–29 added, **54 passed / 0 failed**, tree clean | Accepted · **extends** DEC-0012 · **continues** DEC-0039/0045/0076 | 2026-08-12 (S76) |

| DEC-0085 | **The freeze-rate one-off becomes a tool — `ops/freeze_baseline.py`.** DEC-0083 measured the freeze rate (1.49/day, median 240 s) as scratch work, and both BOOT.md and BACKLOG.md flagged the same risk in the same words: the number decays unless someone re-derives it. The new tool classifies archive gaps >150s by BACKLOG's rule (RF-dead checked first: stall line within ±5min; else a scheduled campaign swap slot, :05 past 00/06/12/18 per `rx_experiment.sh`'s own `SCHEDULE`; else freeze), **reusing** `stall_baseline.fetch()`/`.stamps()` for stall data and `campaign_analyze`'s DB constants rather than re-deriving either — the abandoned prototype this replaces (found in S76's own scratchpad, never shipped) had instead loaded stalls from a static file that would go stale. `window_start()` extracted out of `stall_baseline.py` (+2 tests) so both tools share the same left-censoring boundary. Both of DEC-0083's traps carry forward: `interval != 1` rows dropped (S37 backfill trap), individual events always printed. **New:** a rolling-window placement section for the freeze side, which DEC-0083 never produced (only the stall side got that treatment) — first read: unremarkable across 24h/36h/48h/72h (36.6–78.3rd pct), moving independently of the same-day record-max stall reading. **Validated**: live run reproduces DEC-0083 almost exactly — 21 RF-dead/12 arm-swap/45 freeze exact match, median 240s exact, rate 1.48 vs the recorded 1.49/day (the small drift is ~14h more window, not a method difference) | Accepted · **completes** DEC-0083's flagged follow-up · **executes** BOOT S77 job 3 | 2026-08-12 (S77) |
| DEC-0086 | **`barometer_inHg` is an unflagged, already-corrected WeatherLink passthrough — documented for the first time.** Owner question: does barometer carry a correction we apply and flag, or is it raw? Neither. The VP2+ ISS never transmits pressure over 915 MHz at all — `pressure_service.py`'s `DavisPressureFetcher` polls WeatherLink's own cloud API hourly and prefers `bar_sea_level`, **already sea-level-corrected by WeatherLink's own cloud side**; this repo applies no correction of its own, it relays the value as-is. **No `_qc` flag marks this** (DEC-0032's mechanism covers only `rain`/`rainRate` today), so the field is indistinguishable on the wire from every RF-derived field beside it, even though its correction happened entirely upstream and invisibly. Secondary finding: the same fetched value backfills the internal `pressure` and `altimeter` loop-packet keys when null (`pressure_service.py:92-97`) — collapsing three normally-distinct quantities into one borrowed number; harmless externally since only `barometer_inHg` is in the published contract, but a trap for a future reader of the driver internals. Documented in `docs/INTERFACES.md` §1; cross-posted as a heads-up (not a change request) to `eaglehunt-weather-dashboard#377` and `eaglehunt-ops#162`. Whether barometer ever warrants its own `_qc`-style flag is left open, deliberately undecided here | Accepted · **documents** `docs/INTERFACES.md` §1 · **relates to** DEC-0032, DEC-0053 | 2026-08-12 (S77) |
| DEC-0087 | **RF-dead reception dips PAUSE the campaign instead of hard-aborting.** Twice now (DEC-0082's S75 incident, and an S79 arm-A abort fully reconstructed from the logs: clean swap `00:05:02`, a genuine 11-min RF-dead episode `01:40`–`01:51` with the monitor's own `RECEPTION RECOVERY` line at `01:51:33`, the lagging 30-min mean tripping `trip_abort()` four minutes later anyway) an ordinary, self-resolving RF-dead episode has produced a sticky STOP that then sat unattended for hours, spanning a scheduled swap slot. **Scoped to RF-dead only** (not freezes — "RF re-established" isn't a meaningful resume condition for a process-wedge event) **and to the guard's reception-floor path only** (`tick`'s own abort-on-write-failure / abort-on-unhealthy-swap are genuine operational failures, untouched). A floor trip now writes a non-sticky `rx_experiment.PAUSE` marker instead of hard-aborting — no config/container touched, since there's nothing to receive during a blackout regardless of which arm's gain is active. Every guard tick while paused checks for the monitor's own `RECEPTION RECOVERY` line newer than the pause start (→ auto-resume, no human needed) or a **120-minute ceiling** with no recovery (→ escalate to the unchanged `trip_abort()`) — the ceiling clears the 75.8-min ERR-0005 outlier, the longest RF-dead episode on record, with ~60% margin. **Schedule slots stay fixed** (a paused arm just gets fewer live minutes that block, captured automatically by existing `harvest()` accounting) rather than extending block end times, which would re-drift the whole square off its diurnal-control grid — the exact confound DEC-0082's fixed-slot design exists to prevent. No email on an ordinary pause/resume (DEC-0081 already called these "gates nothing"); the escalation path keeps its email. 9 new tests (pause-not-abort, healthy no-op, resume-on-fresh-recovery, non-resume-on-stale-recovery, ceiling escalation with baseline verified restored, STOP-precedence, `recovered_since()` in isolation), 26/26 `test_rx_experiment.py`, 233/233 full suite. Shipped as PR #173, independent of PR #171's schedule-shift fix for the same incident (disjoint regions of the same file) | Accepted · **relates to** DEC-0081, DEC-0082, DEC-0083, DEC-0085, DEC-0014 | 2026-08-13 (S79) |
| DEC-0088 | **`freeze_baseline.py`'s swap detection was schedule-only; ad hoc restarts were inflating the freeze rate.** S80's scheduled corroboration re-run (BOOT S80 job 3) found the 48h window S79 flagged had cooled, but 24h/36h had newly gone "elevated" — until the freshest event (2026-08-13 10:24-10:27) turned out to line up almost exactly with this session's own tick log (`10:25:01 tick: swapping A -> H`), the S79 abort's self-heal restart, not a freeze. `classify()`'s swap check (DEC-0085) only knew the four *scheduled* hours; it had no way to see a restart `rx_experiment.sh` triggers off-schedule — every abort's baseline restore, every DEC-0087 pause escalation, every tick self-heal. **Verified, not just inferred**: `logs/rx_experiment.log` confirms the 2026-08-12 "19:55→20:02 (420s)" event BACKLOG.md had counted as an independent freeze was actually `19:55:35 ABORT` → `19:55:36 RESTORING baseline snapshot`'s own restart footprint. Fix: `classify()` now also cross-references every logged `tick: swapping`/`RESTORING baseline snapshot` line (ground truth) alongside the fixed schedule, padded 3min back / 12min forward (`SWAP_SLACK_MIN`, reused); RF-dead precedence unchanged and re-tested against the new path. **Effect**: 7 of 47 "freezes" reclassified as swap — rate 1.54/day → 1.31/day, all four rolling windows (24h/36h/48h/72h) flip from elevated/85th-95th-pct to unremarkable (49th-67th pct). Not a one-off: DEC-0087 guarantees more ad hoc restarts going forward, so the bug's main damage was still ahead of it. 5 new tests (ad hoc detection, pad boundaries, RF-dead precedence, a positive control encoding the exact 10:24 event), 17/17 `test_freeze_baseline.py`, 238/238 full suite | Accepted · **corrects** a blind spot in DEC-0085's tool · **same shape as** DEC-0083 · **relates to** DEC-0087, DEC-0082, DEC-0045 | 2026-08-13 (S80) |

| DEC-0089 | **`recovered_since()`'s RECOVERY-edge check had the same lagging-signal blind spot DEC-0087 was built to fix.** DEC-0087's first live exercise (2026-08-13, 19:40:05 PAUSE on arm H, three short reception dips): reception then read healthy `[OK]` continuously for ~2 hours, but `recovered_since()` only checks for a `RECEPTION RECOVERY` line — an ALERT→RECOVERY *edge* — and reception never dropped low enough again to produce a fresh one. Zero RECOVERY lines confirmed in the whole window despite dozens of healthy reads. The pause rode the full 120-min ceiling into a needless `ABORT` at 21:45:01, and the resulting STOP blocked every tick for 10.5+ hours, straight through arm-A's `00:05` swap — discovered next session start. **Fix**: `recovered_since()` also checks the monitor's periodic `RECEPTION: NN% ... [OK]` line (a level signal, logged every ~5min regardless of ALERT state) as an independent, additive path — the edge check still resolves a sharp recovery faster; the level check catches a gradual one that never re-alerts. 4 new tests, including the exact incident fixture with its assertion flipped (the regression test). 30/30 `test_rx_experiment.py`, 242/242 full suite. **Recovery**: schedule shifted +24h a third time (DEC-0082's unchanged mechanism) — arm A now due `2026-08-15T00:05`, square `08-15 → 08-23T00:05`; fix + shift deployed together, STOP cleared only after | Accepted · **corrects** a blind spot in DEC-0087 · **applies** DEC-0082's recovery mechanism · **same lesson as** DEC-0088, DEC-0045 | 2026-08-14 (S81) |

| DEC-0090 | **The S82 state-machine audit: the DEC-0088/0089 blind-spot pattern was a class with more members — five apparatus fixes shipped** (resume aligned to the pause floor, closing the occupied [50,60) trap band; rotated-log reads in `recovered_since()` + the guard's floor mean; swaps defer during an active pause instead of swapping into the episode's health-check abort, BASELINE exempt; guard stands down after the self-terminator; tick/guard/abort serialize behind a lock — the 08-11 02:05:03 interleave was live evidence, and a full-budget health_ok outlives the cron period) **+ soak_check's reset counter dead since S67's rename** ("1 of 0" was the tell). Monitor-side trio (memory-only episode state, rotation-falsified reset verdicts, email-less reset-exception path) specced to #180, tier:mid. Every finding log-verified before fixing; two clean checks and one deliberate reliance recorded. Deployed same session (PR #179, sha-verified), before the square's first block | Accepted · **extends** DEC-0087/0088/0089 · **applies** DEC-0069/0077 · **continues** DEC-0074's rename lesson | 2026-08-14 (S82) |

## Open / deferred

- **DEC-0017** — gain 372 is interim; the averaged no-preamp sweep vs 207 is now **part of DEC-0048's designed RX experiment**, not a standalone errand.
